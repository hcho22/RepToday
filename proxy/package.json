{
  "name": "reptoday-llm-proxy",
  "version": "1.1.0",
  "private": true,
  "description": "Thin stateless key-holding proxy for Rep Today's LLM slices: Variety Language (US-N05) and the AI coach transport (US-AC01).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:runtime": "node test/workerd-auth.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250718.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@apple/app-store-server-library": "3.1.0",
    "asn1js": "3.0.7",
    "cbor": "10.0.12",
    "node-app-attest": "1.0.1",
    "pkijs": "3.4.0"
  }
}

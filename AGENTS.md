# AGENTS.md

Guidance for Claude Code when working in this repository.
`CLAUDE.md` is a symlink to this file, so the two can never disagree - edit `AGENTS.md`, never the symlink.

## Project Overview

Rep Today is a discipline-first micro-workout iOS app (5-60 min sessions) for busy, desk-bound adults.
The user says how many minutes they have, and a deterministic on-device engine generates a complete zero-equipment session blending bodyweight strength and mobility - no browsing, no choosing, no thinking.
Strength is earned, not the entry promise: every user starts in the **Discipline Phase** and earns the **Strength Phase** through sustained consistency plus demonstrated competence, so all MVP users resolve to Discipline with the `PhaseEvaluator` already in place.
The MVP is Apple-native with no custom backend behind the core loop (the `convex/` telemetry sink is beside it, not under it); AI/LLM features are deferred to Phase 2, do language only, and never generate or adapt a workout.

## Source of Truth

- **Strategic plan:** the v6.0 strategic PRD under `.claude/agent/tasks/` (supersedes v5, kept for reference).
- **Implementation PRD / live progress tracker:** `.claude/agent/tasks/prd-fitsnack-mvp-v6_0702.md` (~51 stories, US-A01 ... US-N05); acceptance checkboxes flip to `[x]` as stories land. Always check the story before building a feature.
- **Second, in-progress PRD - anonymous product telemetry:** `.claude/agent/tasks/prd-funnel-instrumentation_260803.md` (`US-T##`, tracked the same way); US-T01 (transport spike), US-T02 (the analytics seam), US-T03 (the Convex sink), US-T05 (the anonymous per-install identity on `AppState`), US-T04 (the live fire-and-forget `URLSession` transport that joins them), US-T06 (the opt-out flag, the Settings screen, and the onboarding disclosure), US-T07 (the first production emission call site: `app_install`, `day7_return`, `day30_return` at app entry), US-T08 (the second call site: `onboarding_started`/`onboarding_completed` through the onboarding flow), US-T09 (the third call site: `ready_screen_shown` with a measured `generation_ms` on the Ready Screen), US-T10 (the three session-lifecycle sites: `session_started`, `session_completed`, `session_abandoned` on the active-session player), US-T11 (`week_active`, emitted once per active week from the session-completion path), and US-T12 (the monetization funnel: `paywall_shown`/`trial_started`/`subscribe`) have landed - so **all 13 events now have their emission sites**. US-T14 (abuse-hardening the public `POST /logEvent`) has also landed: a client-embedded shared secret (`X-RepToday-Analytics-Secret`, per-configuration build setting -> `Info.plist` -> `LiveAnalyticsService`, checked against `process.env.ANALYTICS_SHARED_SECRET` in `convex/http.ts`; missing/wrong -> `401`, unconfigured deployment -> fail-closed `500`) plus per-install/per-IP rate limiting via a dedicated ephemeral `rateLimits` helper table (`convex/rateLimit.ts`, `internal.rateLimit.checkAndBump`), both run before the `logEvent` mutation so a rejected request never inserts; the `events` row shape is unchanged and it adds no emission site. Both the secret and the install key are honestly documented as cost-raisers, not guarantees (extractable binary; client-rotatable id). The remaining story is US-T13 (ground-truth reconciliation, a QA/verification story), which adds no emission site.
US-T13's re-runnable **harness** is pre-built (`tools/reconcile/` + the internal-only `reconcile:eventsForInstalls` read query in `convex/reconcile.ts`; see `tools/reconcile/README.md`), but US-T13 itself stays open and its acceptance boxes unchecked: the reconciliation report against real observed sessions still awaits the moderated cohort, the named non-founder coder, and a frozen rubric.
- **Third, in-progress PRD - continuous-circuit sessions:** `.claude/agent/tasks/prd-continuous-circuit-sessions_260814.md` (`US-CC##`). Specs the hands-free follow-along player that supersedes the manual tap-to-advance model (US-K01/US-K02/US-O03): auto-advancing work windows, circuit rounds ("Round N of M"), and one engine timing-model change - a uniform set count per training block with the between-round rest as the fit lever (reps stay the currency; progression/Adaptive Overload unchanged). **US-CC01 has landed** (the first slice, player-only): a rep-based `.strength`/`.primal` training set now counts down on an auto-advancing **work window** (`ActiveSessionViewModel.workWindow`, `ActiveSessionView.WorkWindowCountdownView`) that reads the engine's already-planned per-set seconds (`SessionAssembly.workSecondsPerSet`), records the set at zero, and flows into the *existing* US-K02 rest with no tap, plus a prominent **Done** to advance early - and timed holds (US-O03) kept their manual Start-hold path (bookend holds made hands-free later by US-CC05, below; training holds still manual). **US-CC03/US-CC04 have also landed** (the even-round engine change, engine-only, no player/UI change): a training block is now sized as a circuit of a **uniform round count** (every station's `sets` equal) with the retired per-exercise set-adjust lever replaced by a **bounded between-round rest** (`SessionAssembly.minRoundRestSeconds`...`maxRoundRestSeconds`, 30-75s) as the primary timing-fit lever and a fixed ~15s between-station transition as the distinct second gap - see the Step 7 note below and ADR-0003. **US-CC02 has also landed** (player-only): a training block (strength / dedicated primal, gated by `SessionAssembly.isCircuit`) is now played as a **circuit of rounds** - `ActiveSessionViewModel.nextPosition(fromIndex:set:)` rotates one set of each station per round (A, B, C, A, B, C, ...) instead of grinding all sets of one exercise first, the player surfaces **"Round N of M"** (`ActiveSessionViewModel.currentRound`/`circuitRoundCount` read off the block, rendered by `ActiveSessionView.setTracker`/`nextUp`; a station's r-th set *is* round r, so `currentSet` doubles as the round), the two engine gaps pace it through the US-K02 rest overlay (`transitionSeconds` between stations, the uniform per-block round rest between rounds), warm-up/cooldown bookends stay linear (no round), and aggregate logging is unchanged (`completedSets` keyed by prescription id); a **skip** inside a circuit removes the exercise from every remaining round (the only aggregate-correct reading of "skip this exercise" once the walk rotates); swap-across-rounds reconciliation landed later in US-CC07 (below). **US-CC05 has also landed** (player-only): a **warm-up/cooldown bookend** timed hold now auto-starts hands-free (never a Start-hold tap) and a per-side bookend flows side 1 -> a brief **"Switch sides"** beat -> side 2 with no tap (`ActiveSessionViewModel.autoStartHoldIfNeeded`/`currentStepIsBookendHold`, driven from every reveal transition and re-armed after a swap; the beat is a shared-`Countdown` rest carrying an in-memory `RestContext == .switchSides` that names itself and fires **no** completion cue, so exactly one cue per hold leg), while a timed **strength/primal training** hold keeps the US-O03 manual Start-hold path (the split is by block category, the complement of US-CC01's work-window gate); the US-O03 resume rule is unchanged (a leg is never persisted, so a resumed bookend re-opens idle and auto-starts the owed side fresh). **US-CC06 has also landed** (player-only): the quiet in-flow escape hatches with **no separate manual mode** - the distinctive new one is an explicit user **Pause** (`ActiveSessionViewModel.pause(asOf:)`/`resume(asOf:)`, surfaced as a Pause/Resume toggle in the shared `ActiveSessionView.SessionTopBar`, so one control covers whichever countdown is live - work window, hold, or rest - across both the player and the rest overlay) that freezes the on-screen countdown *and* its audio-cue timing (a paused `Countdown` never reports `hasElapsed`) and resumes from the exact remainder, **without backgrounding**; a user pause is a *foreground* state kept distinct from the scene-phase background pause (the new `pauseForBackground`/`resumeFromForeground` seam, so foregrounding never un-freezes a held user pause), and any advancing hatch taken while paused (Done/Skip/Skip rest/Swap/Stop hold) clears the stale pause via `clearUserPause()`. The other five hatches are surfaced as-is (Done US-CC01, Skip, Swap US-K03, round-rest +/skip US-K02; "+ More time" is the existing rest `extendRest` - the work-window/hold extension is US-CC14, out of scope here). No manual-mode toggle exists and `NoManualModeGuardTests` (unit bundle) fails the build if a `manualMode`/`ManualMode` identifier ever appears in the player sources. **US-CC07 has also landed** (player-only, a hardening/lock-in story with no engine change): a mid-circuit **skip** or **swap** holds for **all remaining rounds** and the block stays structurally even. Because the player keeps one `Step` per station (rounds are the re-visited `currentSet`), both were already structural once US-CC02/US-CC03 landed - skip passes over the station in every round, and a swap replacing the one slot carries the substitute through all later rounds while `ExerciseSwap` keeps the block's uniform round count M. The one product decision it settled (captain, 2026-08-15): **Option A, honest late-entrant** - a swap's substitute joining in round *r* logs only the `M-r+1` rounds it actually played (keeping the US-K03 discard-on-swap contract and US-CC09 no-fabrication), so it may end a round short of its peers rather than fabricating a pre-swap set; round-rest reshaping was scoped out (drift already bounded by `slotTolerance`). **US-CC08 has also landed** (engine-only; the coupling it exists to guarantee was already structural from US-CC01, so the player needed no new value): `SessionAssembly.workSecondsPerSet` is no longer a planning-only proxy but a **generous runtime pace**, via one documented constant `SessionAssembly.workPaceGenerosityFactor = 1.25` applied *inside that function*, so the same inflated number feeds both the planning wall-clock and the on-screen work window and the window can never be roomier than what the fit planned. It scales the **rep branch only** - a hold's per-second cost is *definitional* rather than estimated (prescribed seconds are elapsed seconds, so there is nothing to be generous about), so holds pass through at 1.0 while every rep-based set is paced wherever it sits, warm-up/cooldown stretches included, since the plan owes the slower user their real time whether or not that set runs under an on-screen countdown - and never touches Step 6's per-set target; the value stays a pure `asOf`-free function of the exercise and its target. Two measured, captain-confirmed (2026-08-15) consequences: a session fits **fewer rounds - and at 45 min, one fewer station** (15/20 min 6 -> 5 rounds, 30 min 7 -> 6, 5 and 60 min unchanged; at 45 min the strength block goes 7 rounds x 5 stations -> **8 x 4**, so the longest strength block trains 4 distinct movements instead of 5, narrowing its movement-pattern coverage, and with the block pinned at the `maxTrainingSets` (8) rail dropping a station is the fit's only remaining lever there - accepted as-is, a station floor or a higher rail being a separate follow-on; worst fit error 1s against the ±60s tolerance, no length loses its last round, and the round outcome is flat across 1.20-1.30) with a fast user finishing early and tapping **Done**; and the US-M03 `sitsLong` bookend bias now genuinely **binds** on the shipped catalog (rep-based stretches pace up while hold stretches do not, so a desk worker's 45-min warm-up costs ~32s more and the fit pays it out of a strength round), so that story's byte-identical-training-middle guard - always incidental rather than structural - is **rescoped, not relaxed**, to the real invariant (`SitsLongBookendBiasTests.testSitsLongChangesNoTrainingMovementOrTarget`: same movements at the same per-set targets, blocks still internally even, and equal bookend seconds must still produce identical round counts). Honestly a *scaled typical-case estimate, not observed runtime data*; a later story with real completion times can replace the factor with per-movement runtime fields without touching a call site, since every caller already reads this one function. **US-CC09 has also landed** (hardening/lock-in, no production change - the invariant was already structural from US-CC01/US-CC02/US-CC07): an auto-advanced set and a **Done** early-advance both record prescribed = performed via `completeSet()`/`recordSet` (identical to the pre-US-CC01 tapped path), only an explicit **Skip** logs an exercise not-done (`skipExercise` discards its `completedSets` and flags `skipped`), and no per-set "did they really do it" field exists - `CompletedSet` carries only `reps`/`durationSeconds` and the end-of-session `rate` (US-L02) stays the sole Ramp signal. Locked by `ActiveSessionViewModelTests.testWholeSessionHandsFreeWithOneSkipLogsCompletedExceptTheSkip` (whole session driven hands-free with one station skipped, reads the durable `WorkoutLog`) and the reflection guard `NoPerSetCompletionTrackingGuardTests` (pins `CompletedSet` to its two fields in the routinely-run unit bundle). Accepted consequence, documented: the US-T10 `>=80%` `session_completed` metric reads a touch more generously than the old tap-gated model (a window auto-records at zero without a tap), interpreted as "reached the end" not "verifiably performed every rep"; no emission site moved. **US-CC10 has also landed** (player-only): the single US-K02 feedback seam becomes a per-state cue vocabulary - `Utilities/SessionCuePlayer.swift` replaces `RestTimerFeedback` with a `SessionCue` enum (`.go`/`.halfway`/`.transition`/`.roundRest`/`.done`) and `SessionCuePlayer.play(_:suppressAudio:)`, so the hands-free flow is tellable apart by ear with no spoken callouts. The five states route onto the player's existing transition points (not new advance logic): `.go` from `startWorkWindow()`, a new optional `.halfway` from `fireWorkWindowHalfwayIfReached(asOf:)` on the work-window ticker, `.transition`/`.roundRest` at a rest's *start* (a `cue:` on `startRest`, sourced from a kind `nextPosition` now returns - a between-station beat vs a between-round rest), and `.done` at set/hold-leg completion; a rest *ending* fires no cue (the next window's `.go` is the boundary tone), and the US-CC05 switch-sides beat stays silent so each hold leg still lands exactly one `.done`. Tone source is distinct system sounds (no bundled asset, **no `docs/asset-attribution.md` row owed**). Ducking is real `AVAudioSession` `.playback` + `.duckOthers` (deactivated `.notifyOthersOnDeactivation`, an in-flight counter ducking once across overlapping cues); VoiceOver coordination reads `UIAccessibility.isVoiceOverRunning` at fire time (injected `voiceOverActive`) and withholds the *tone* (and its duck) while still firing the *haptic*, so a cue never talks over speech and a muted/VO user never loses a state change. The on-device audio/VoiceOver behavior is captain-verifiable **manual QA** the unit suite cannot fully cover (Simulator audio routing/VoiceOver are a proxy); the decision logic is unit-tested through the injected spy. **US-CC11 has also landed** (player-only, presentation): the rep-based work window is now **visual-primary** - `ActiveSessionView.WorkWindowCountdownView` pairs the movement illustration with a **compact** `CountdownRing` (now parameterized `diameter`/`lineWidth`/`font`; the hold/rest rings keep the full-size defaults) in the fixed `exerciseSlotCard`, so a text-and-ring-only window is rejected. The illustration source is shared, not duplicated: `ExerciseDemoView`'s Lottie-if-present-else-SF-Symbol content was extracted into `ExerciseIllustration`, so the US-O01 Lottie fast-follow seam is preserved by construction (drop ~71 clips later = a data change) and the work window inherits its Reduce-Motion stilling; the work-window glyph is passed **static** (`animatesGlyph: false`, the AC's "static illustration at launch") while a bundled clip may still play. The between-station **transition beat** (US-CC04) earns its prominent "Next: <exercise>" cue in `RestView.nextUp` (movement name in largeTitle/accent, spoken "Next: <exercise>") rather than by renaming the shared rest header (which stays "Rest"); it is told from a between-round rest via a retained-cue seam - `startRest` now keeps its started `.transition`/`.roundRest` cue on `ActiveSessionViewModel.restCue` (in-memory, cleared in `endRest`), exposing `isTransitionBeat`. No engine/model/persistence/advance/timing/cue-vocabulary change. Covered by `ActiveSessionViewModelTests.testTransitionBeatIsDistinctFromRoundRest` and the hosted-surface `VisualWorkWindowEvidenceTests` (asserts illustration+ring together and the prominent Next cue on the live a11y tree; PNGs under `artifacts/reports/us-cc11/`). **US-CC12 has also landed** (hardening/lock-in, test + docs only, no production change): an old paused session written by the pre-CC manual player (US-K04) resumes in the new continuous-circuit player from its exact saved position (step index, set, completed sets, skips, session-clock origin), and the migration was found **already structural** once US-CC02 kept `ActiveSessionState`'s `currentStepIndex`/`currentSet` unchanged - a station's r-th set *is* round r, so the saved position maps straight into round/station terms with nothing to convert. `init(state:)` copies `completedSets`/`skippedStepIDs` verbatim and clamps the position; an **uneven** pre-US-CC03 block resolves deterministically because `blockSpan` reads round count M as the **max** station set count and `hasSet(_:inRound:)` gates each station by its own count (a shorter station drops out of the later rounds, no work lost or double-counted); only the per-side `hold.side` restores (idle on the owed side, never a running leg, US-O03). Forward-safe by construction (no snapshot-schema change; `hold`/`exercisedMinutes` stay optional-and-defaulted). Proved by `ActiveSessionViewModelTests.testResumesAnOldUnevenPausedSnapshotPreservingWorkSkipsAndHoldSide` (the PRD validation shape: uneven push_up 3 / per-side side_plank hold 3 / squat 2, mid-block, some completed sets, one skip, `hold.side == 2`), `testResumedUnevenPreCircuitPlaysToCompletionWithoutLosingWork` (walk-to-completion guard), and `PersistenceTests.testActiveSessionStateDecodesWithoutTheHoldOrExercisedMinutesFields` (oldest resumable shape decodes unchanged). **US-CC13 has also landed** (player-only, presentation + one-shot flag; no engine/model/persistence/advance/timing/cue change): a single dismissible **first-run explainer** (`Views/ActiveSession/ContinuousCircuitExplainerView.swift`) shown at most once on first arrival at the continuous-circuit player, stating the four points - the session auto-advances, **+ More time** never rushes you, **Done** jumps ahead, tones mark state changes - in identity-framed, non-loss-framed copy (never "the manual mode is gone"). The persisted one-shot flag is on `AppState` (`hasSeenContinuousCircuitExplainer`; read via `shouldShowContinuousCircuitExplainer`, flipped via `markContinuousCircuitExplainerSeen()`, default unseen from `bool(forKey:)`'s never-written `false`). `ActiveSessionView` presents it as an **overlay layer** inside the player's `ZStack` (not a `.sheet`, so the entrance/exit transition can be stilled under Reduce Motion) driven by `showExplainer`, flips the flag the **moment** it decides to show (so a force-quit mid-explainer can't re-fire it), never gates session start (the player's own `onAppear` has already run `viewModel.start()`), and reads `AppState` from the environment as an **optional** so the evidence suites that host `ActiveSessionView` without one are unaffected. While the explainer is up the session is held on an explicit US-CC06 **user pause** (`presentExplainerIfNeeded()` calls `viewModel.pause(asOf:)` after the flag flip - after `resumeRest` in `onAppear` so it freezes a live countdown - and `dismissExplainer()` calls `viewModel.resume(asOf:)` before clearing `showExplainer`), so the freshly-started first work window / hold / rest and its `.halfway`/`.done` cue timing freeze behind the modal rather than elapsing, resuming from the exact remainder on "Got it" - a foreground pause, not the scene-phase `pauseForBackground`, a no-op on an idle Start-hold step and on the no-`AppState` hosted path, and still "never gates the start" (the session has started, merely paused); only the single t=0 `.go` cue fires before the pause. Accessibility: `.isModal` card, `accessibilityHidden` scrim, one combined element per point (markdown emphasis spoken as plain words), 60pt "Got it", `ScrollView` for Dynamic Type. Covered by `AppStateTests` (gating: unseen -> shows, seen -> never again, survives relaunch), the hosted-surface `ContinuousCircuitExplainerTests` (four points announced, "Got it" labeled/hittable, non-loss-framed copy, callback dismisses), and `ActiveSessionViewModelTests.testExplainerPauseFreezesTheFirstWindowUntilDismiss` (pause-while-shown freezes the first window and its cue, resume restores the remainder). **US-CC14 has also landed** (player/presentation-only; no engine/timing/persistence/advance/completion-logging/cue-vocabulary change), and with it **the continuous-circuit PRD is complete**: **+ More time** is now a first-class, always-available hatch on both auto-advancing countdowns, and those countdowns are accessible. `ActiveSessionViewModel.extendActiveCountdown(by:)` extends whichever of the live work window or hold leg is running by the **same +15s** the rest's "+ More time" (`extendRest`) uses, gated by `canExtendActiveCountdown` (`isRunningWorkWindow || isHolding`); it is surfaced in the player's secondary row via the shared `secondaryAction` (visible "+ More time", a11y label "More time", `+15s` hint, 60pt active-screen touch target) whenever that gate holds, so the rest overlay's own "+15s" never doubles up. It is a **pure display/timer affordance**: US-CC09 stands (the set still records prescribed = performed however long the window ran), it touches no logged value and no planned wall-clock, and - because neither the work window nor the hold leg is persisted - it writes no snapshot (unlike `extendRest`). Accessibility hardening lives on the shared `ActiveSessionView.CountdownRing`: it carries `.accessibilityAddTraits(.updatesFrequently)` so VoiceOver polls the remaining time **on demand** rather than announcing every per-second tick or stealing focus (the time stays queryable in the ring's own "<name>, N seconds remaining" label - so the existing `hasPrefix("Work window, "/"Hold, "/"Rest, ")` evidence assertions are unchanged), its sweep animates only when Reduce Motion is off (`reduceMotion ? nil : .linear`), the movement name / round-set label / target / primary control labels all wrap under Dynamic Type (`fixedSize(vertical:)` and `minHeight` on the prominent buttons), and the ring clock shrinks to fit (`minimumScaleFactor`). AC5 (an auto-advance never cutting off speech) is inherited from US-CC10's tone-withholding under a VoiceOver-running flag - the advance posts no screen-change/announcement of its own. Covered by `ActiveSessionViewModelTests` ("+ More time" suite: extend arithmetic on window and hold, no-op on a rest / completed session, and the US-CC09 no-log-change invariant under an injected clock) and the hosted-surface `MoreTimeAccessibilityEvidenceTests` (a hittable "More time" element on both the work window and an auto-started hold, the ring carrying `.updatesFrequently`, and activating "More time" the VoiceOver way lengthening the ring's spoken remaining time; PNGs + `validation.md` under `artifacts/reports/us-cc14/`). Captain-verifiable **manual QA** the unit suite cannot cover: the Reduce-Motion ring stilling (the `accessibilityReduceMotion` env is read-only, so not hostable - enforced by the animation gate) and live on-device VoiceOver focus/announcement behavior. Decisions recorded as ADR-0002 (per-interval pacer clock) and ADR-0003 (even-round timing); domain term "Continuous Circuit (planned)" in `CONTEXT.md`.
- **Fourth, in-progress PRD - round cap and wide circuits:** `.claude/agent/tasks/prd-round-cap-wide-circuits.md` (`US-RC##`). Caps every training-block exercise at **2-4 rounds** (down from `1...8`) and fills a longer session by going **wider** - more distinct movements, drawn from a strength pattern's *further* progression chains as accessories - instead of deeper. **US-RC01 has landed** (engine-only, the coupled core): `SessionAssembly.minTrainingSets`/`maxTrainingSets` are now `2`/`4`; `Builder.strengthBlock` seeds the reserve breadth-first over `ProgressionChainSelection.selectAll` (every pattern's primary chain first, stalest pattern first, then every pattern's further chains as accessories), and the extended blend's dedicated `primalBlock` does the same over its one pattern's two chains (the PRD's stated fallback, exercised because a beginner's 60-min session lands short without it); every promoted station still joins at the block's uniform round count (ADR-0003 unchanged). Landing this exposed and fixed two latent timing-fit bugs the round cap made load-bearing: `candidates()`'s whole-station promotion now searches the round count *jointly* with adding the station (`.addReserveWithRounds`) rather than pricing it at whatever round count the round-only lever had already walked to (which could turn the one available widening move into an overshooting jump); and `ProgressionChainSelection.selectAll` ranks every chain by freshness/withheld-status as a sort key rather than an elimination filter, so a pattern's non-frontmost chain no longer vanishes from the reserve just because its sibling chain was recently worked. `RoundCapWideCircuitsTests` is the story's own coverage; `SessionAssemblyTests.testEvidenceEvenRoundCircuitTranscriptAcrossLevelsAndLengths`'s precision bound loosened from ±3s to ±10s under the coarser, station-quantized lever (worst observed deviation 8s, still far inside the real ±60s `toleranceSeconds` gate). **US-RC02 has also landed** (test-and-docs-only, no production/engine change): `RoundCapInvariantGuardTests` (routinely-run `RepTodayTests` bundle, deliberately distinct from US-RC01's `RoundCapWideCircuitsTests`) is the standing regression guard - it assembles sessions across 5/10/15/20/30/45/60 min and all three fitness levels and asserts every training-block station's `sets` (circuit blocks per `SessionAssembly.isCircuit`; linear bookends exempt) sits in the **literal** `2...4` and each training block is internally even (ADR-0003). The range is bound to hard-coded `2`/`4`, *not* `SessionAssembly.minTrainingSets`/`maxTrainingSets`, so bumping a constant cannot silently loosen the guard - non-vacuity verified 2026-08-18 (reverting `maxTrainingSets` to 8 makes it fail, restoring `2...4` makes it pass). **US-RC03 has also landed** (test-and-docs-only, no production/engine change; `AccessoryProgressionParityTests`): a wide-circuit **accessory** progresses and is dosed exactly like a primary because the parity is **structural**, not added machinery - Step 5's `ProgressionChainSelection.selectAll` ranks every chain through one `selectInChain` (an accessory is simply a lower-ranked result of that same call, bound by the identical advancement, Start-Seed-band, and `varietyWindow` rules) and Step 6's `SessionAssembly` `appendTrainingItem` doses every station (seeded primary, other patterns' primaries, and every accessory) through one `AdaptiveOverload.target` call, so there is no separate accessory code path. The suite pins all three PRD behaviors over the real catalog's two push chains (`push_horizontal`/`push_vertical`): an accessory advances its own chain when its `advancementCriteria` clear (both at the `selectAll` seam and end-to-end across two `planBlocks` assemblies); an easier second-chain accessory earns proportionally more reps than a harder primary, each equal to `AdaptiveOverload.target` recomputed; and a recently-worked accessory chain is de-preferred by the `varietyWindow` sort key exactly as any station (flipping the window flips the ranking). **US-RC04 has also landed** (test-and-docs-only, no production/engine change; `AccessoryDepthMismatchTests`): the deliberately-accepted second-chain **depth mismatch** (a maxed primary can be harder than the deepest its shallower accessory chain reaches, and that gap is *not* gated - ADR-0004) is pinned as bounded and honest, both facts already structural from US-RC01. It is **bounded by the difficulty cap**: the one `ExercisePoolFilter.eligiblePool` pass inside `planBlocks` clamps *both* the primary and the accessory to the user's level band (beginner 1-2, intermediate 1-3, advanced 1-5), so a beginner (standard/floor-dips, both difficulty 2) and an intermediate (diamond/pike, both difficulty 3) top both push chains out at the cap ceiling for a **zero** maxed mismatch, while an advanced discipline-phase user maxes horizontal at archer (difficulty 4 - one-arm is Strength-phase-gated) and vertical at pike (difficulty 3) for an accepted **one-tier** mismatch; and it is **never seeded above an earned tier** - an untouched second chain enters at `selectInChain`'s no-history base tier (order 0), never nudged up to the pattern frontier the user cleared on the other chain (an advanced user deep on horizontal push still meets vertical push at floor-dips). Read off the fit-independent `planBlocks` reserve over the real push chains; the broad `testNoAccessoryIsEverSeededAboveTheUsersDifficultyCap` guard is bound to the live `difficultyCap(for:)`, so removing the cap or adding a cap-skipping accessory path fails it. **US-RC05 has also landed** (docs-only, no production/engine/test change), and with it **this PRD is complete**: `docs/adr/0004-round-cap-wide-circuit.md` records the round-cap/go-wider decision (the `2...4` rails, the second-chain accessory rule, depth-first fill, and the accepted bounded depth mismatch, all verified against source), `CONTEXT.md` gains the "Wide Circuit" term pointing to ADR-0004 and the owning code (`SessionAssembly`, `ProgressionChainSelection`), and ADR-0003's superseded 45-min "8 rounds x 4 stations" consequence is annotated forward to ADR-0004 (its even-round uniformity and ±60s tolerance kept intact; only the round rail moved).
- **Fifth, in-progress PRD - Phase-2 Strength Coach & analytics:** `.claude/agent/tasks/prd-phase-2-strength-coach-analytics_260825.md` (`US-SP##` first, later `US-AC##`/privacy slices). Makes the earned **Strength Phase** actually unlock harder work - it is fully evaluated today but gates almost nothing. **US-SP01 has landed** (engine-only): earning the Strength Phase now **lifts the difficulty cap**, resolving the "double-gate trap" where a phase-gated skill had to clear *both* `ExercisePoolFilter.isPhaseAllowed` and a difficulty cap derived purely from the self-reported `FitnessLevel` (beginner 1-2, intermediate 1-3, advanced 1-5) - and since every `.strength` skill is difficulty 5, a beginner/intermediate who earned the phase still saw nothing. One centralized `ExercisePoolFilter.effectiveDifficultyCap(for level:phase:)` is now the single source of truth: it returns the `FitnessLevel` band for `.discipline` and the full `1...5` catalog range (`strengthPhaseDifficultyCap`) for a user whose earned `user.phase == .strength`, so demonstrated competence overrides the conservative onboarding estimate. The one `eligiblePool` call site routes through the new `isWithinEffectiveDifficultyCap(_:for user:)`; the level-only `difficultyCap(for:)`/`isWithinDifficultyCap(_:for level:)` stay intact for callers reasoning about the conservative band directly, `.discipline` behavior is byte-identical (pinned by test), and the per-pattern soft-filter fallback and cold-start Start-Seed banding are untouched. Covered by `ExercisePoolFilterTests`. **US-SP02 has landed** (catalog-data + tests only, no engine change): the "fill the cliff" half - each of the three phase-gated chains now carries a **mid-tier `difficulty`-4 `phase == "strength"` bridge** between its discipline frontier (also d4) and its d5 summit, so a newly-Strength-Phase user climbs a rung at a time instead of jumping straight from difficulty 4 to 5. The chosen skills and wiring in `Resources/Exercises.json` (catalog now 74 movements, 6 gated skills): push `push_horizontal` gets `push_one_arm_assisted` (Assisted One-Arm Push-Up) at order 6 between `push_archer` and `push_one_arm` (bumped to 7); squat `squat` gets `squat_pistol_assisted` (Assisted Pistol Squat) at order 5 between `squat_shrimp` and `squat_pistol` (order 6); core `core_hollow` gets `core_one_leg_l_sit` (One-Leg L-Sit, a hold) at order 3 between `core_tuck_l_sit` and `core_l_sit` (order 4). Each new skill is zero-equipment, load-validated, matches its neighbours' `isHold`/`isPerSide` shape and `advancementCriteria` style ("3x6 clean reps per side" for the push/squat per-side bridges, "3x18s hold" for the core bridge), and rewires the chain into a clean doubly-linked ladder (frontier `.progressionId` -> bridge -> summit, with matching `regressionId`s). No bundled asset ships, so no `docs/asset-attribution.md` row is owed. The old "phase-gated == difficulty-5 summit" invariant in `ExerciseLibraryTests` was rescoped to the real one (a gated chain is a two-rung d4-then-d5 ladder). Covered by `MidTierStrengthSkillTests` (catalog validity + chain continuity + the Validation Test: a `.strength` user advancing each chain surfaces the d4 bridge before the d5 skill, and a `.discipline` user never reaches the bridge).
- **What is already built:** `docs/implementation-log.md`. **Test coverage map:** `docs/test-coverage.md`. **Third-party asset source/license ledger:** `docs/asset-attribution.md` (an asset with no cleared row never ships).
- **Domain glossary:** `CONTEXT.md` (root) - the resolved vocabulary of load-bearing terms (Pillar, Movement Practice, discipline-first), each pointing to the authoritative code or doc. **Architecture decision records:** `docs/adr/` - decisions that changed a term's meaning or the engine's behavior (ADR-0001 records the co-primary -> strength-primary shift). The dated task files above remain the strategic source of truth; `CONTEXT.md`/`docs/adr/` capture settled vocabulary and decisions rather than superseding the PRDs.

## Build & Run

```bash
cd ios/RepToday && xcodegen generate    # regenerate the project from project.yml
xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepToday \
  -sdk iphonesimulator -configuration Debug \
  -destination 'generic/platform=iOS Simulator' build
xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepToday \
  -destination 'platform=iOS Simulator,name=iPhone 16' test
xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepTodayUITests \
  -destination 'platform=iOS Simulator,name=iPhone 16' test    # XCUITest suite, run on demand
```

The `RepToday` scheme builds the app and runs `RepTodayTests` - that is the default run, and everything except the XCUITests lives in it; if the destination will not resolve, use `xcrun simctl list devices available` and pass `-destination 'id=<UDID>'`.
One suite in `RepTodayTests` is deliberately held out of that default run: `SessionGenerationBenchmarkTests`, the re-runnable session-generation latency harness behind the `<100ms` claim (`gtm/01-research/session-generation-benchmark-2026-08-10.md`), is **opt-in and skipped** (so the gate and CI never pay its ~17.5k-generation cost) unless you pass `RUN_SESSION_BENCHMARK=1` to xcodebuild - a build setting the `RepToday` scheme forwards into the Simulator process exactly like `REPTODAY_WRITE_EVIDENCE` below; it is re-targetable to a real device unchanged via `-destination`, and the Simulator run is an optimistic proxy, not the authoritative iPhone XS / iOS 17 number.
Target iOS 17.0+, Swift 5.9, Xcode 16.3, bundle id `com.reptoday.app`.
`RepTodayUITests` is a second scheme on purpose: it installs and launches the app in a booted Simulator and drives it out of process, so folding it into `RepToday` would make every unit run wait on an app launch and inherit its failure modes.
Run it when the touch path is what is in question - it is the only place a production control is actually pressed rather than hosted.
`DEVELOPMENT_TEAM` is set to the signing team, so entitlement-gated paths (Sign in with Apple, CloudKit sync, HealthKit writes, live purchases) build for device; they still verify only on real hardware, never in the Simulator.
Historical device notes in `docs/implementation-log.md` and the PRDs predate this and describe the team as empty - read them as point-in-time records.
The evidence suites (`PerSideSwapEvidenceTests`, `OnboardingBasicsEvidenceTests`, `ProgressTabSnapshotTests`, `HandsFreeBookendEvidenceTests`, `USCC08PaceEvidenceTests` - the last also writing a `pace-report.txt` transcript beside its PNG) render production screens to PNGs on every run, and `OnboardingImperialUITests` screenshots the running app the same way, but a plain `test` writes them to a per-run temporary directory so the worktree stays clean; append `REPTODAY_WRITE_EVIDENCE=1` (a build setting, since a Simulator-hosted test bundle inherits no shell environment - both schemes forward it) to regenerate the committed images under `artifacts/reports/<story>/`, or `REPTODAY_EVIDENCE_DIR=<root>` to send them elsewhere.
A suite never rolls its own path: `EvidenceOutput.directory(for:)` is the one resolver, and `REPTODAY_EVIDENCE_DIR` names a **root** with the story folder appended - never the final directory - so one redirected run keeps the same `<root>/<story>/<file>` shape as the committed baselines instead of scattering suites' renders into different layouts.
The XCUITest screenshots are produced on demand rather than committed: the suite always files them with its `.xcresult` too, so the two variables only decide where a second, browsable copy lands.
`TelemetryOptOutUITests` also renders the screens US-T06 added, but files them **only** as `.xcresult` attachments and reads neither variable - it wants a picture a reviewer opens on a green run, not a baseline to diff. So the PNGs committed under `artifacts/reports/US-T06/` are **not** written by any run: no suite resolves that directory, their names and count do not match the suite's attachment names, and `artifacts/reports/US-T06/validation.md` cites them as evidence - so refresh them deliberately and keep the existing filenames rather than expecting `REPTODAY_WRITE_EVIDENCE=1` to regenerate them.
Hosting a production surface to capture or read it likewise goes through `HostedSurface.host(_:size:)` and `AccessibilityTree`, so every suite gets the same run-loop settling rather than an ad-hoc copy that reads a half-drawn screen.
Both of those rules have exactly one known exception, and it predates them: `OnboardingBasicsEvidenceTests` still carries its own hosting controller, window, run-loop pump and accessibility lookup, captures at `format.scale = 2` against `HostedSurface.captureScale`'s 3, and resolves `REPTODAY_EVIDENCE_DIR` verbatim with no story folder appended - the one layout divergence the resolver rule exists to prevent. Adopting the shared seams there is owed work, recorded with its full scope under "Owed work" in `docs/implementation-log.md`; write new suites against the rules, not against the exception.
Read `AccessibilityTree.labels(in:)` as "what is written down" and `spokenStrings(in:)` as "what VoiceOver actually says" - the latter counts labels **and** hints together, which is the only way an assertion can catch a sentence that appears once but is announced twice.
Two more shared test seams exist for the same don't-roll-your-own reason: `DefaultsSnapshot.swift`'s `restoreAfterTest(_:in:)` for the few unit tests that must write `UserDefaults.standard` (the telemetry gate is bound to it, so a private suite would prove nothing) and would otherwise leak the key into every later test in the process, and `RepTodayUITests/HealthAccessPrompt.swift`'s `answerHealthAccessSheetIfPresented(in:)`, now a precondition for any XCUITest that reaches the main tabs, since they raise the Health share prompt on arrival.

`convex/` is a second, unrelated toolchain and no iOS command touches it: `npm install` once, then `npm run typecheck` (`tsc --noEmit` over `convex/`, against the committed `convex/_generated/`, so it needs no deployment) and `npx convex dev --once` to deploy to your own dev deployment.
There are two tsconfigs on purpose: `convex/tsconfig.json` is what the Convex CLI typechecks on every deploy, so it excludes `**/*.test.ts` and names no `types` array (naming one would switch off automatic `@types` inclusion for the deployed functions), and `convex/tsconfig.test.json` extends it with the test files plus the `vite/client` types; `npm run typecheck` runs both, so a deploy never depends on the test toolchain being installed while the suite stays typechecked.
`npm test` (`vitest` + `convex-test`, added by US-T04) is the behavioural gate over the `POST /logEvent` boundary: it runs the real functions in process against an in-memory database and no deployment, so it needs neither network nor `.env.local`. `convex/README.md` records what the suite covers and the one write-time residual it cannot reach.

**CI:** `.github/workflows/ci.yml` gates every PR into `main` (and pushes to it) on both toolchains in parallel jobs - `sink` (ubuntu: `npm ci` + `npm run typecheck` + `npm run test`) and `ios` (macos-15, Xcode 16.4, iPhone 16 / iOS 18.5 simulator). The iOS job `xcodegen generate`s then builds and **runs the `RepToday` unit suite** (the gate) and **build-for-testing-only** compiles the `RepTodayUITests` scheme (XCUITests are compile-checked, not run, since they launch the app out of process and are slow/flaky on a hosted runner - run them locally when the touch path is in question). It needs no secrets: the app's CloudKit/HealthKit/Sign in with Apple entitlements make a fully unsigned build (`CODE_SIGNING_ALLOWED=NO`) crash the app-hosted test runner, so CI uses cert-less ad-hoc signing (`CODE_SIGN_IDENTITY=-`, team/profile cleared) which loads entitlements on the simulator without any certificate. No new third-party package is added (Lottie stays the app's only dependency; xcodegen is a build-time generator). The local `no-mistakes` pipeline is unchanged and still runs the same `xcodebuild test`.

## Architecture

**Monorepo:** `ios/` is the whole MVP; `convex/` is the telemetry sink only - one append-only `events` table, one internal `logEvent` mutation, and one `POST /logEvent` HTTP action (US-T03, hardened by US-T04, abuse-guarded by US-T14) that is the sink's single entry point, deliberately dumb with no analysis in it, and now reachable by the shipped client though nothing in the app calls it yet; US-T14 also adds a second, ephemeral `rateLimits` helper table (its only non-`events` table) plus `convex/rateLimit.ts` and a maintenance cron (`convex/crons.ts`) - a throttle store, not an evidence surface (see the Source of Truth note above and `convex/README.md`). There is still **no backend behind the core loop**, which stays fully on-device and offline. Standard Convex layout, so the npm project (`package.json`) sits at the repo root and the functions in `convex/` - see `convex/README.md`. `proxy/` is a deploy-ready but unwired Cloudflare Worker for the deferred Variety Language LLM slice (holds the Anthropic key, stores nothing, receives only two pillar values).

**MVVM + protocol-based services.** Views read services from `@Environment(\.services)`; ViewModels are `@Observable`; services are protocols in `Services/Protocols/` with mock and real implementations; `DI/ServiceContainer.swift` wires them (`mock()` for tests/previews, `live(context:installId:analyticsGate:analyticsService:)` for the app, whose two optional trailing parameters carry the telemetry consent gate and let a test substitute the telemetry sink), so swapping in a real implementation is a one-line change.

**Persistence:** CoreData over `NSPersistentCloudKitContainer` with two stores - the **Cloud** configuration (`CDUser`, `CDWorkoutLog`, `CDSessionPolicy`) mirrors to the user's private iCloud database, and the **Local** configuration (`CDActiveSession`) is device-bound transient state that never syncs. The core loop works fully offline and with no iCloud account; sync is additive and falls back to local-only on any CloudKit failure. Domain models are plain `Codable` structs; nested fields persist as JSON-encoded `Data`.

**Integrations & navigation:** Sign in with Apple, CloudKit, write-only HealthKit, and StoreKit 2 (free unlimited core, premium depth only) never gate the core loop and degrade quietly; `AppState` (`@Observable`, UserDefaults-persisted) controls onboarding vs. main tabs, the selected tab, and the telemetry opt-out flag. The Profile tab is still the placeholder it has always been, with one real destination on it: `Views/Settings/SettingsView.swift`, a sectioned Settings screen with a Privacy section (telemetry opt-out) and a destructive **Account** section (US-AD01, the mandatory App Store 5.1.1(v) account-deletion path) - a later Profile story extends it with another `Section` rather than replacing it. Account deletion (`Services/Account/AccountDeletionService.swift`, orchestrated once from `AccountDeletionViewModel` behind an `isDeleting` re-entrancy guard) tears down every store in a fixed order - the CloudKit-mirrored Cloud records via `deleteCurrentUser()` + the wholesale, user-id-free bulk deletes `WorkoutLogServiceProtocol.deleteAllLogs()`/`SessionPolicyStore.deleteAll()`, the device-local `CDActiveSession` via `ActiveSessionStore.clearAll()`, then the reinstall-surviving Keychain identifier via `AuthServiceProtocol.signOut()` - then resets `AppState` to route back to onboarding; it is idempotent and safe for the local-UUID (never-signed-in-with-Apple) user. The policy/active-session deletes are wholesale precisely so teardown never depends on decoding the `CDUser` - a corrupt/unreadable user aggregate can no longer leave those records orphaned behind a "delete account" that reports done (the per-user `ActiveSessionStore.clear(for:)` remains for the Ready-Screen/active-session paths; `SessionPolicyStore` has no per-user delete - only the wholesale `deleteAll()`). The Sign in with Apple case surfaces on-device guidance only (captain's Option (a): no backend, no token revoke), hosted on `RootView` via the transient `AppState.showAppleSignOutGuidance` flag because the teardown tears down the Settings screen; the Apple-vs-local decision goes through the `AppleCredentialStateProviding` seam over `getCredentialState`. US-AD06 (HealthKit sample removal) is out of scope, a separate follow-on.

**Anonymous product telemetry (funnel instrumentation, a separate PRD):** `AnalyticsServiceProtocol.record(_:)` takes an `AnalyticsEvent` - one of exactly 13 pre-registered event names (`gtm/06-channels/event-metric-schema.md`, snake_case raw values that are the wire contract), a millisecond client timestamp, and a non-identifying `[String: AnalyticsValue]` bag. US-T02 built the seam, US-T04 the **transport** behind it, and US-T06 the **consent** in front of it: `mock()` wires the recording in-memory `MockAnalyticsService`, while `live(context:installId:)` wires `LiveAnalyticsService` (`Services/Analytics/`), one fire-and-forget `URLSession` POST per event to the deployment's `POST /logEvent`, with no Convex SDK. **The first production emission call site landed in US-T07**: `RepTodayApp.init()` emits the three app-entry events (`app_install`, `day7_return`, `day30_return`) through `AppEntryTelemetry.eventsForLaunch(...)` (`Services/Analytics/AppEntryTelemetry.swift`), a pure clock-injected decision unit, fire-and-forget through `services.analyticsService.record(_:)`. **The second call site landed in US-T08**: `OnboardingView.onAppear` calls `OnboardingViewModel.onboardingStarted()`, which emits `onboarding_started` (no properties, guarded to once per view-model lifetime by `didEmitOnboardingStarted` rather than persisted, since the funnel counts distinct installs and the backend dedups by `installId`) and records the start instant off the view model's injected clock; `finish()`'s **success branch** then emits `onboarding_completed` carrying `elapsed_seconds` (`.int`, whole seconds from that instant to now, clamped at 0), never on the failure branch. **The third call site landed in US-T09**: `ReadyView` threads `services.analyticsService` into `ReadyViewModel` (optional, defaulted `nil`, exactly like `OnboardingViewModel`'s), whose `load()` emits `ready_screen_shown` once per Ready Screen appearance - guarded by a `hasEmittedReadyScreenShown` one-shot in the style of its `hasComputedConsistency`/`hasCheckedReprogramOnOpen` guards, not persisted - carrying `generation_ms` (`.int`, whole milliseconds of the `workoutEngine.generateWorkout(...)` await specifically, measured off the injected clock and clamped at 0). It emits from `load()` rather than the shared `generate()` so a duration-chip regeneration (which reaches the engine only through `selectDuration()`) structurally cannot re-emit and inflate the count, though `generation_ms` is still measured on that path (`lastGenerationMs`). **The fourth through sixth call sites landed in US-T10**: the active-session player's three lifecycle events (`ActiveSessionViewModel`). `session_started` fires from `start()` inside its existing `guard startedAt == nil`, carrying `requested_minutes` - and, because a resumed player restores `startedAt`, it emits once per **physical** session and is never re-emitted on resume. `session_completed` fires from the choke point `recordSessionEnd()` (called by `ActiveSessionView.close()`, the sole dismiss path) **only when the session actually completed**, one-shot-guarded, carrying `requested_minutes`/`completed_minutes`/`was_return` and `perceived_difficulty` when rated (omitted when not). It fires at **dismissal, not at `finish()`** (captain decision, Option B), because `perceived_difficulty` is collected via `rate()` on the completion screen *after* `finish()`; the accepted, documented tradeoff is that a completion the user force-quits from the celebration screen before tapping Done emits no `session_completed` (its `WorkoutLog` still persists). **`session_abandoned` deliberately refines the AC's literal "emit at dismiss-with-`completed==false`" (captain-approved Option B, fixing a review finding):** a mid-session dismiss keeps the session **resumable** (US-K04 does not clear the store), so it is a *pause*, not an abandonment - emitting there would let one physical session emit *both* an abandon (on the pause) and a completed (after a resume+finish), double-counting the completion metric. So the player emits **no** terminal event on a resumable pause, and `session_abandoned` fires on **true give-up** of a resumable session, owned by `ReadyViewModel` (which still holds the snapshot after the player is gone): an explicit **Discard** (`discardResumableSession()`) or an **overwrite** by a fresh **Start** replacing a still-resumable session (`abandonResumableSessionForOverwrite()`, wired to the Start button), both routed through one `emitSessionAbandoned(for:)`. It carries `completed_minutes` and a coarse `abandon_point` (`AbandonPoint`: `warmup`/`mainWork`/`cooldown`, from the block the snapshot's current step sits in), both read off the persisted `ActiveSessionState` - `completed_minutes` from a new `exercisedMinutes` field the snapshot captures at each meaningful change (the last active moment), **not** recomputed as wall-clock at give-up (which would fold a pause's idle gap into the total). The structural guarantee is now **exactly one terminal event per physical session** (completed xor abandoned) across the whole resume path - never both, never neither-when-an-outcome-occurred. **The seventh call site landed in US-T11**: `week_active` (no properties, per schema) emits from `SessionCompletionService.recordCompletedSession` - the one seam holding both the just-completed log and the sink - at most once per distinct active calendar week, guarded by a **persisted** set of already-emitted week-starts (`UserDefaults` key `telemetry.weekActiveEmittedWeeks`) so a second session in the same week is a no-op that survives relaunch. It reuses `ProgressAnalytics`'s rollup bucketing rather than re-deriving one - keying off the same `ConsistencyScore.startOfWeek(log.completedAt, ...)` function - but hands it `AppState.cohortCalendar` (Sunday-start, Pacific), **not** the `Calendar.current` the on-device rollup uses: this is the one calendar decision US-T11 owned, adopting the same cross-user split US-T05 pinned `install_week` to, because `week_active` carries no properties and the server can only bucket it by client timestamp (the two calendars are coupled - change one only with the other). `ServiceContainer.live` resolves the sink once and hands it to both the container and the completion recorder; `mock()` leaves the recorder's sink `nil`. **The final call sites landed in US-T12**: the monetization funnel on the paywall (`PaywallViewModel`, threaded `services.analyticsService` -> `PaywallView` -> `ProgressTabView` optional/defaulted-`nil`, exactly like US-T09). `paywall_shown` (carrying `entry_point`, a new closed `EntryPoint` enum whose only case today is `progressUpsell` = `progress_upsell`) emits from `load()`, one-shot-guarded by `hasEmittedPaywallShown` (US-T09 style) so a `.task` re-run cannot double-fire. `subscribe`/`trial_started` emit from `purchase(_:)`'s `.resolved` branch **only on a real `.premium` grant**, so a user-cancel (resolves to `.free`), a `.pending`, and a thrown failure all emit nothing; `restore()` is deliberately not an emission site. Trial-ness is read off the resolved `Subscription.trialEndsAt != nil`: a trial-bearing grant emits property-less `trial_started`; a direct paid grant emits `subscribe` carrying `plan` (the StoreKit product id, `plan.id`). One documented residual (US-T12 note in the PRD): a trial *converting* to paid does not yet emit `subscribe`, because the only conversion signal is an out-of-band StoreKit `Transaction.updates` entry that `LiveStoreKitFacade.listenForTransactions()` consumes internally and never surfaces to the analytics seam - wiring it needs conversion-tracking machinery this story scopes out. So a build now fires at app entry, through onboarding, on the Ready Screen, across the session lifecycle, on the weekly rollup, and on the paywall - **every one of the 13 events**. The live StoreKit purchase legs verify only on device / the `.storekit` configuration, never the plain Simulator; the unit suite (`PaywallViewModelTests`) proves the emission *decisions* through `MockAnalyticsService`. `record(_:)` checks an injected `isEnabled` gate - production passes the `AppState` instance's own `analyticsGate`, read per emission rather than captured, so the toggle takes effect on the next event and not the next launch - then hands the event to a detached task and returns - so no caller ever waits on the network, and every failure is swallowed. Consent is therefore a property of **that sink** rather than of the protocol: an emission site calls `record(_:)` unconditionally and must never re-check the flag itself (a second gate could disagree with the first), and the trigger for making it structural is a count rather than a judgement - **count the emitting conformers of `AnalyticsServiceProtocol`**. There is exactly one today; the moment there are two while the gate still sits inside a concrete sink, it moves to a `ConsentGatedAnalyticsService` applied once in `ServiceContainer.live`. The wire body is a **second** encoding, `AnalyticsWireBody`: `{name, installId, clientTs, props}` with `props` flattened to plain scalars and `clientTs` a bare JSON number, deliberately *not* `AnalyticsEvent`'s own tagged `Codable` form, which stays tagged so `Int`/`Double` round-trip in process. The deployment origin is `Info.plist` configuration (`RepTodayAnalyticsEndpoint`), expanded from the **per-configuration** `REPTODAY_ANALYTICS_ENDPOINT` build setting in `ios/RepToday/project.yml`: Debug carries the dev deployment `courteous-dogfish-560`, Release carries **nothing**, because no production one is chosen yet. A missing, empty, or unusable value makes the build **inert, never fatal** - `LiveAnalyticsService.configured(...)` returns `nil` and the container falls back to `NoOpAnalyticsService` - so a Release build silently sends no data rather than silently sending to the wrong destination, and choosing plus configuring the production deployment is a precondition for shipping any build that emits (US-T07 onward). That split is asserted against the running app bundle in `LiveAnalyticsServiceTests`, each half compiled only into the configuration that can observe it - but only the Debug half ever runs, because `ENABLE_TESTABILITY` is Debug-only here so `@testable import` cannot resolve under `-configuration Release`; the Release half is verified at the built artifact instead (`plutil -extract RepTodayAnalyticsEndpoint` on `Release-iphonesimulator/RepToday.app/Info.plist`). `live(context:installId:analyticsGate:analyticsService:)` also takes the sink, defaulting to exactly that resolution, so a test building the production container for its CoreData services substitutes an inert one and is structurally unable to reach the wire once the emission sites land - rather than unable only because none exist yet (FR-13). That seam is **in-process only**: `RepTodayUITests` launches the real app, which builds its own container, so the only gate reachable there is `isEnabled`. **US-T06 closed that half.** The consent flag is `AppState.analyticsEnabled` (default on, with absence resolved as on by `AppState.isAnalyticsEnabled(in:)` - a plain `bool(forKey:)` would read a never-written key as opted *out*), turned off from `SettingsView` (the toggle, with the explanation as the Privacy section's footer so VoiceOver reads it once rather than once as body copy and again as the toggle's hint), disclosed on the first onboarding screen, and overridable out of process as `-AppState.analyticsEnabled NO`, because `UserDefaults` reads the argument domain first - the only mechanism an out-of-process suite has, though it is available rather than universally applied: `OnboardingImperialUITests` passes it on every launch, while `TelemetryOptOutUITests` passes it only where being opted out is the assertion and holds its opted-in legs off the wire by **interception** instead - the probe harness swaps the transport's `URLSession` for an in-process counting `URLProtocol`, which is what lets those legs run with the gate genuinely open. So every launch there carries consent-off **or** the probe, by construction rather than by discipline: a `TelemetryPosture` enum with no case meaning neither, and every launch goes through `TestApp` (`RepTodayUITests/TestApp.swift`), the bundle's sole `XCUIApplication`, whose only launch entry point takes a posture by value - so a test cannot hold a raw launchable app and a neither-guard launch is not expressible through the suite's API. Because `XCUIApplication` is a framework type any file can still construct, the guarantee is "cannot ship a bypass" rather than "cannot type one": `UITestLaunchGuardTests` (`RepTodayTests/UITestLaunchGuardTests.swift`, in the unit bundle so it runs on the routinely-run `-scheme RepToday test` gate both local runs and CI perform) scans every `RepTodayUITests` source and fails that run if `XCUIApplication(` is constructed anywhere but `TestApp.swift`. That build guard replaced the retired runtime detector (`assertNobodyLaunchedBehindOurBack`), which only *detected* a bypass after the fact and had a verified blind spot - a standalone bare `app.launch()` as a test's only launch; a source scan has no launch orderings to miss, and was sabotage-checked in exactly that ordering. The wrapper was US-T07's recorded precondition and has landed. The gate travels the same way `installId` does - `RepTodayApp.init()` hands `appState.analyticsGate` to `live(...)`, so the reader is bound to the store `AppState` writes rather than both sides independently naming `.standard`; production still resolves to `.standard`, which is what puts `NSArgumentDomain` in front of the persisted value, so moving `AppState` onto an app-group suite later is a change that has to re-verify the override first (noted on `AppState.init`'s `userDefaults` parameter). The gate is proved out of process by `RepTodayUITests/TelemetryOptOutUITests` against `Services/Analytics/TelemetryUITestHarness.swift`, a `#if DEBUG` and launch-argument-gated probe supplying the two things the out-of-process suite needs - a deterministic emission attempt on *every* launch (US-T07's real `app_install` fires only on `isFirstLaunch`, which the suite's onboarded launches are not, so it never fires there and the probe stands in) and a count another process can read - so the assertion observes the gate rather than the absence of anything to emit; it carries a positive control, and its non-vacuity was checked by sabotaging the gate and watching it fail. What it proves stops at the `URLProtocol` boundary inside the app, which is what keeps FR-13 (no bytes leave); that bytes reach Convex at all is the live, non-re-running transcript `artifacts/reports/US-T06/validation.md`. The **sink** that POST targets: US-T03 deployed `convex/`'s `events` table, `logEvent` mutation, and `POST /logEvent` -> `204` HTTP action, which rejects an unknown event name, an oversized property bag, and a missing or wrong-kind `installId`/`clientTs` (presence and kind only - those are the two columns K4 and K1 are counted from, so coercing them would land `"undefined"`/`NaN` rows that look valid), and does nothing else; a caller's fault answers `400` with the message, the sink's own failure `5xx` with no detail - which is why the action runs the SDK's `convexToJson` over the arguments itself, so a `props` field name Convex refuses lands on the caller's side of that split instead of looking like an outage. US-T04 added the two size caps US-T03 had knowingly left off, once a real client existed to need them: `installId` bounded at 64 UTF-8 bytes (length only, never format) and the request body at 64 KiB, checked before anything is parsed so an oversized payload can no longer reach argument serialization and answer `5xx`. It also added the boundary's first re-runnable test (`convex/http.test.ts`, `npm test`). The split is still not total, and `convex/README.md` records the residual in full: a caller fault that only a *write-time* Convex rule catches - `props` nested past the value-depth limit - still surfaces as `5xx`. Those checks are only worth anything because the action is the sink's one way in: `logEvent` is an `internalMutation`, so a new writer belongs behind the action rather than beside it - a public Convex function is callable directly on the deployment's `.convex.cloud` API and would skip all of them. So the wire contract has both ends and the wire, and no caller. The **identity** those events carry landed separately in US-T05, on `AppState` (`Utilities/AppState.swift`): `installId` is a random UUIDv4 minted on first launch and held only in `UserDefaults`, alongside `firstLaunchAt`, a `lastActiveAt` that only `init` writes (so it moves on a cold launch, not on a resume, and nothing reads it), and a computed `installWeek` that calls `ConsistencyScore.startOfWeek(_:_:)` rather than re-deriving a week boundary. Where it is *not* kept is the load-bearing part: never the Keychain, the IDFA, `identifierForVendor`, the Sign in with Apple identifier, or an email, because an id that outlives an uninstall resurrects an identity the user believed they deleted - though `UserDefaults` buys "dies with the app **absent a backup restore**" rather than an absolute, since `Library/Preferences` rides iCloud/iTunes backups and device-to-device transfer. `AppState.init` therefore takes injected `now`/`calendar`/`newInstallId` seams, and a reinstall is modelled in tests as a wiped `UserDefaults` suite. The id reaches the transport by being **passed down**, not re-read: `RepTodayApp.init()` builds `AppState` first and hands `installId` to `ServiceContainer.live(context:installId:)`, so a second code path can never disagree with `AppState` about which of the three launch states an install is in. Three launch states are resolved at construction and stay stable across relaunches: a genuine first launch mints the id and stamps `firstLaunchAt`; an install that already existed when this build shipped - discriminated as **onboarded with no stored `installId`** - mints an id but is *not* a first launch and, absent an origin already on disk, gets `firstLaunchAt == nil` plus a persisted `firstLaunchUnknown` marker, so it neither fabricates an origin nor re-mints on every launch (accepted residual: an earlier build whose user never finished onboarding still reads as fresh); a stored id with neither an origin nor that marker is corrupt and re-mints whole. The rule is narrower than "re-mint the pair": a missing id is re-minted, a *usable stored origin is always kept* (`isFirstLaunch` is true only when this launch stamped the origin itself), and the `firstLaunchUnknown` marker records that no usable origin exists rather than that the pair was incomplete - discarding an origin that survived would cause the very miscohort the rule exists to prevent. So `firstLaunchAt` and `installWeek` are `Date?`, and US-T07 settled the **two** data-modelling decisions this left open, both falling out of one rule - `app_install` fires **iff `isFirstLaunch`**, return events only when `firstLaunchAt != nil` (see the US-T07 criteria in the funnel PRD for the recorded consequences). A `nil` cohort (an unknown-origin pre-existing install) emits **nothing** rather than the upgrade week - no `app_install`, no returns - so K4's denominator omits it (acceptable because that population is empty until the first public build). A re-minted identifier beside a surviving origin emits **no** `app_install` (keying off the origin stamp, not the mint, so one physical install is not double-counted) but *does* window its return events off that origin, leaving the id in K4's numerator and absent from its denominator. `install_week`'s wire form was also decided here: a coarse `yyyy-MM-dd` week-start string (`AppEntryTelemetry.installWeekString`), never a precise timestamp. The re-minted state stays unreachable today (nothing clears `installId` alone), so the constraint that held US-T06 back from a "reset telemetry identity" control still binds any later story that would add one - it must not resurrect that state without re-confirming this decision. `installWeek` is bucketed in `AppState.cohortCalendar` (Gregorian, Sunday-start, `America/Los_Angeles`) and not `Calendar.current`, because it is grouped across users server-side; the on-device Consistency Score stays on `Calendar.current` on purpose, since a training week is a local week - change the constant, not the score. The two calendars are coupled, and US-T11 settled the seam: `week_active` carries no properties, so the server can only bucket it by client timestamp, and its cadence buckets in `AppState.cohortCalendar` rather than inheriting `Calendar.current` from the `ProgressAnalytics` rollup whose `ConsistencyScore.startOfWeek` bucketing it reuses - the same cross-user split, for the same reason. Change one calendar only with the other. `previousActiveAt` snapshots the `lastActiveAt` the previous cold launch left, because `init` overwrites it before anything can read it; nothing reads the snapshot today, and it is **not** what the day-7/day-30 return windows measure from (those are `firstLaunchAt`).

## The Deterministic Engine

Pure Swift, on-device, no network, no LLM, <100ms. Step 0 overrides (cold start, Return) run first and are no-ops in the steady state; the cold start also carries a **Start Seed** - a difficulty band `[startingDifficultyFloor, cappedMaxDifficulty]` over strength/primal plus a volume seed, both seeded from `profile.fitnessLevel`, eased a tier per `tooHard` rating, and retired at the handoff (which records the floor the week actually ran at so Step 5 does not treat a withheld chain as fresh).

1. **Session shape** - 5-10 single-focus; 11-20 blend light; 21-40 blend full; 41-60 blend extended.
2. **Pillar balance** - **every** session is **strength-led**, structurally (US-001/US-002, made unconditional by **US-M01**). There is no strength-vs-mobility split step anymore: `PillarBalance`/`PillarWeights` were **removed** by US-M01, and `SessionAssembly` builds the training middle as a single strength block (primal folded in) for single-focus and the shorter blends, or a strength block plus a dedicated primal block for an extended (41-60 min) blend - **never a mobility middle block at any length**. Mobility survives only as the warm-up and cooldown bookends, which since **US-M02** are *pattern-matched*: each mobility movement carries a `complements: [MovementPattern]` tag (`Exercises.json`, load-validated), and both bookends **lead** with a stretch complementing the day's lead strength pattern, then fill with the existing staleness / no-repeat ordering - a preference, never a filter (`SessionAssembly.leadingComplement`/`leadStrengthPattern`), with the general pool as the never-starved fallback. The minutes the retired **Movement Practice** mobility block used to hold were reallocated to strength (US-M01 decision 3), so a blend's training time is essentially all strength (short/full) or strength leading a small primal minority (extended, `SessionAssembly.extendedPrimalTrainingShare = 0.15`, strength >~0.80 of training). Since **US-M03** the desk-worker `sitsLong` signal *biases* bookend selection (never a direct sizing lever): it stable-promotes posture/hip openers - stretches whose `complements` name a hip-dominant pattern (`SessionAssembly.postureHipPatterns` = squat/hinge) - to the front of the warm-up/cooldown pools, *underneath* the US-M02 lead-complement promotion, a reorder-only preference that changes no block's count and no bookend stretch count so it can never re-inflate a short session or reintroduce a mobility middle block (`SessionAssembly.postureHipLean`/`isPostureHipOpener`); it does change which stretches *fill* the fixed-count bookends, and since stretches differ in `workSecondsPerSet` a desk worker's bookend duration - and, through the timing fit that feeds `trainingBudget`, a strength set count - can differ; since **US-CC08** paced rep-based stretches up while leaving hold stretches alone, that is no longer even incidental (a desk worker's 45-min warm-up costs ~32s more, which the fit pays out of a strength round), so round counts legitimately differ across profiles. What is test-guarded at the shipped catalog/lengths (`SitsLongBookendBiasTests.testSitsLongChangesNoTrainingMovementOrTarget`) is the real invariant rather than byte-identity: the training middle holds the same movements at the same per-set targets, every block stays internally even, no mobility middle block appears, and equal bookend seconds still produce identical round counts - so the bias can reach the training middle through no channel but bookend cost. `pillarWeighting` is likewise inert (see `SessionPolicy`). Primal is untouched (folded into strength short, its own block at 41-60 min).
3. **Movement-pattern focus** - stalest-first; never repeat the previous session's lead pattern.
4. **Filter pool** - drop by phase, injuries, difficulty cap, recent skips; everything is bodyweight (Zero-Equipment Floor). The difficulty cap is the *effective* cap (`ExercisePoolFilter.effectiveDifficultyCap`): the `FitnessLevel` band while `.discipline`, lifted to the full `1...5` catalog once the user has earned the `.strength` phase (US-SP01).
5. **Progression-chain selection** - the user's frontier tier, advancing only when criteria are cleared; avoid the last `varietyWindow` sessions (default 3).
6. **Adaptive Overload** - capacity-relative reps/sets/holds, never a fixed heroic number; the Asymmetric Ramp backs off fast and climbs slow, paced by `progressionRate`.
7. **Assemble + fit timing (even-round circuit model, US-CC03/US-CC04, ADR-0003)** - warm-up first, cooldown over 10 min, land within +/-1 min of the request. A training block is played as circuit **rounds**: every exercise in it carries the **same** set count = the block's round count (the strength block and the extended 41-60 min dedicated primal block are each internally uniform), so the block is genuinely even and "Round N of M" is well-defined. The planned wall-clock (`SessionAssembly.blockSeconds`, summed by `plannedSeconds`/`totalSeconds` plus one between-block transition) prices a circuit block as `rounds × Σ workPerSet + rounds × between-station transitions + (rounds - 1) × round-rest`; a bookend is one set each, linear. `SessionAssembly.workSecondsPerSet` prices a set as a fixed setup cost plus the per-unit work of the target prescribed (a hold's per-unit cost is one second per prescribed second *per side*, via `Exercise.isPerSide`/`sidesPerSet`), so a grown or seeded target is planned honestly, then scales the rep half by `workPaceGenerosityFactor` (US-CC08) so the number is a **generous runtime pace** rather than a typical-case estimate. Since US-CC01 the player's auto-advancing work window counts down this very number, so it is one source of truth for planning and runtime both; it is still never *measured* (a set logs the target it was prescribed), so systematic bias matters and per-second precision does not. **Two rest gaps** (US-CC04): a fixed ~15s **between-station transition** (`transitionSeconds`, the "Next: get ready" beat) inside a round, and a bounded **between-round rest** (`minRoundRestSeconds`...`maxRoundRestSeconds`, 30-75s) that the fit tunes as its **primary lever** - the retired per-exercise `.addSet`/`.removeSet` moves are gone. The fit is a deterministic greedy over a combined `setRoundsAndRest` move (round count + in-band rest tuned together so it can trade a round for rest in one step) plus whole-exercise promote/drop at the block's round count - never touching Step 6's capacity-relative per-set target and never making a block uneven. The strength/primal round-count rail is `maxTrainingSets`, capped at **4** by US-RC01 (`minTrainingSets` 2; was `1...8`): no exercise is ever prescribed more than four times, and once every active station sits at the cap the fit fills the rest of a long session **wider** instead - promoting a further progression chain per pattern as an accessory (`ProgressionChainSelection.selectAll`, `Builder.strengthBlock`/`primalBlock`) depth-first from the reserve via the coupled `.addReserveWithRounds` move (round count solved jointly with adding the station, see the Fourth PRD note above). The warm-up/cooldown bookends stay one set per exercise (`allowSetAdjust: false`) and grow only by promoting more distinct movement *types*; the warm-up is seeded directly at a length-scaled count (`SessionAssembly.warmupExerciseCount`: ≤10 min 1, 11-20 2, 21-40 3, 41-60 4, all active items with `minItems` pinned), a lean opening (US-003), capped by the `maxWarmupExercises` ceiling (4). (In-session swap keeps the block's round count - it never re-picks a slot's set count - so a swap can never make a circuit uneven; it also holds across all remaining rounds and the block stays even, US-CC07.)

In-session **swap** substitutes within the same pillar, pattern, difficulty band, and time budget, sized by the session's own policy (`progressionRate`, Start Seed, `varietyWindow`, Return/re-entry ease). Under the even-round model (US-CC03) it **keeps the slot's set count** - which on a training block is the block's uniform round count - so a swap can never make a circuit uneven; the per-slot set-count re-pick lever is retired (a substitute joins the circuit at the block's round count or declines), and the widened work-scaled `slotTolerance` now applies to every slot since none has a set lever. A swap therefore moves the session by at most the swapped slot's tolerance. Applying a swap across all remaining rounds landed in US-CC07 (player-side: replacing the one `Step` per station carries the substitute through every remaining round with the circuit staying even; the substitute is an honest late entrant, logging only the rounds it played); reshaping the block's shared round-rest on swap to re-absorb the in-tolerance drift was scoped out of US-CC07 (bounded already by `slotTolerance`). A deterministic on-device **AI Programmer** (`Services/Programmer/`) writes the per-user `SessionPolicy` the engine reads: it detects re-program triggers, diagnoses plateaus, learns the Default Duration, and never returns a workout.

## Consistency & Phase (no gamification)

No XP, no levels, no badges, no streak to break.

- **Consistency Score** - `weeklyAdherence = min(1, workoutsCompleted / weeklyGoal)`, a recency-weighted rolling average x 100. A 5-min session is a full show-up; a miss dents but never zeroes; a Return excuses the gap weeks it closed. `longestChain` is an all-time maximum surfaced as earned pride.
- **PhaseEvaluator** - deterministic, never user-selectable; Strength requires both sustained consistency and cleared entry tiers across push/squat/hinge/core. Earning Strength lifts the engine's effective difficulty cap to the full catalog (US-SP01, `ExercisePoolFilter.effectiveDifficultyCap`), so competence overrides the conservative onboarding fitness-level estimate.
- All copy is identity-framed ("you're someone who moves"), never loss-framed.

## Key Conventions

- `@Observable` only, never `ObservableObject`/`@Published`. All service methods are `async throws` - the one exception is `AnalyticsServiceProtocol.record(_:)`, which is `async` but not `throws` because emission is strictly fire-and-forget, so a call site reads `await analytics.record(event)` with no `try`.
- Enums are `Codable`, `CaseIterable`, and `Identifiable` where they have a stable id.
- Always use `Theme.Colors` / `Theme.Typography` / `Theme.Spacing`; never hardcode colors, fonts, or spacing.
- Button height 56pt, card radius 16pt, touch targets 44pt (60pt on active workout screens).
- Exercise library: 74 bodyweight movements in `Resources/Exercises.json`, all `equipment == []`, load-time-validated (26 of them mobility, each carrying a `complements: [MovementPattern]` bookend-matching tag since US-M02 - a missing tag on a mobility movement fails load).
- Accessibility throughout: VoiceOver, Dynamic Type, Reduce Motion, haptics with an audio alternative. Prescription copy has one source - `ActiveSessionView.targetText` (seen: "3 × 0:30 per side") and `.targetAccessibilityText` (spoken: nouns agreeing with their own counts, "1 set of a 45 second hold"); every other surface calls them instead of re-formatting.
- Pure engine/evaluator logic takes an injected `asOf` clock; never read the wall clock inside it.
- Tests live in `ios/RepToday/RepTodayTests/` (`XCTestCase` + `@testable import RepToday`); all new logic needs tests, and each story adds a row to `docs/test-coverage.md`. `ios/RepToday/RepTodayUITests/` is the XCUITest bundle under its own scheme, for the few things only a real touch can exercise.

## Project Structure (ios/RepToday/RepToday)

```
App/ DesignSystem/ Models/   Entry point, Theme tokens, domain enums + Codable structs
Persistence/                CoreData stack, conversions, CoreData-backed services
Services/Protocols/ Mock/    Service definitions and mock implementations
Services/Engine/            Deterministic pipeline (Steps 0-7 + swap)
Services/Programmer/        Trigger detection, plateau diagnosis, duration learning, policy writer
Services/Language/          Variety Language template, resolver, proxy client
Services/Consistency/       Consistency Score, PhaseEvaluator, ConsistencyTrend
Services/Progress/          Progress-tab analytics (free + premium-gated deep layer)
Services/ActiveSession/     Resume store, completion recorder, session summary
Services/Analytics/         Telemetry sinks: the live Convex POST, the inert no-op fallback, the Debug-only XCUITest probe harness
Services/Auth|Health|Subscription/   Sign in with Apple, HealthKit writes, StoreKit 2
DI/ ViewModels/ Views/      Container + injection, @Observable view models, SwiftUI screens
Utilities/ Resources/       AppState and helpers; Exercises.json, assets, .storekit (no demo animation ships yet)
```

## Out of Scope (MVP Non-Goals)

No LLM/AI calls in the loop, no custom backend behind the core loop (the telemetry sink is the one server-side thing that exists, and nothing in the loop reads it), no XP/levels/badges, no social/leaderboards/challenges, no equipment-based exercises, no full Strength-Phase catalog, no Android/widgets/Live Activities/Apple Watch (see the PRD's Non-Goals for the full list).

## Artifact Locations

PRDs and task briefs live in `.claude/agent/tasks/`; work products go under `artifacts/` - `reports/` (engineering, QA, test results), `specs/`, `learning-logs/` - created on demand.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.

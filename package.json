{
  "name": "reptoday-telemetry-sink",
  "version": "1.0.0",
  "private": true,
  "description": "Append-only Convex sink for Rep Today's anonymous product telemetry (US-T03).",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "typecheck": "tsc --noEmit -p convex",
    "test": "vitest run"
  },
  "dependencies": {
    "convex": "^1.16.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.55",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}

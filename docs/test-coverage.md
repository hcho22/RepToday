# Test Coverage Map

Per-area coverage of `ios/RepToday/RepTodayTests/` and `ios/RepToday/RepTodayUITests/`, extracted from `CLAUDE.md`.
The rows naming `convex/` are the exception and say so in place: that toolchain is not Swift, so its behavioural suite is `convex/http.test.ts` run by `npm test` (`vitest` + `convex-test`, US-T04, extended by US-T14), not XCTest.
Each row is added as the owning story lands.

| Area | Coverage |
|------|----------|
| Scaffold (`ScaffoldTests`) | Design-token values (button/card/touch sizes) |
| Models / enums | Codable round-trips, stable raw values; `User.ColdStart` round-trips with and without a recorded `bandFloorAtHandoff`/`bandAimAtHandoff`, and a record persisted before the Start Seed band decodes to "no band ran" (US-O02) on both fields rather than failing or acquiring one retroactively |
| Exercise library (`ExerciseLibraryTests`) | Bundled `Exercises.json` decodes; per-pattern/pillar counts, contiguous progression chains with resolving links whose difficulty never decreases as the chain progresses (Step 5's no-history entry rule scans by `progressionOrder` and matches on difficulty, so it is only the gentlest match while the two orderings agree), Zero-Equipment Floor, phase gating (gated skills are the library's difficulty-5 summit; every tier 1-4 beneath one is Discipline-reachable), hold/rep field contract; the `isPerSide` flag the timing model reads agrees with the human-readable `advancementCriteria` on every movement, in both directions (nothing at runtime cross-checks the two, so a new per-side movement missing the flag would be silently under-timed by half its work) - asserted *positively*, every criteria string having to match one of a small set of recognised authoring templates, so an unanticipated phrasing such as "3x8 clean reps per arm" fails loudly instead of falling through to "not per side" the way a closed per-side whitelist did, with a second backstop that no criteria may name a limb at all outside a recognised marker; every per-side hold's authored estimate covers the hold on both sides (the duration force-unwrapped and the sweep asserted non-empty, so a hold missing its default cannot pass the check vacuously against zero); the Start Seed band has room to rotate (US-O02) - both band gates walk every movement pattern the Discipline strength/primal catalog reaches, not the patterns the band leaves standing, so a pattern the band empties fails instead of dropping out of the loop; within every fitness level's `[startingDifficultyFloor, cappedMaxDifficulty]` band each banded movement pattern offers at least **two progression chains**, the number that actually matters because Step 5 picks one exercise per chain, so a single in-band chain would hand the user the identical movement all week, and at least **two difficulty tiers**, so a banded level's `[floor, cap]` is a real range in every pattern rather than a label on a single tier; every `animationName` in the shipping catalog resolves to a file the app bundle actually carries (US-O01), which is also the enforcement half of `docs/asset-attribution.md` - an uncleared asset can never be wired into the catalog |
| Exercise service (`ExerciseServiceTests`) | Real library loads/caches; by-pillar/pattern/phase/difficulty-range and next-in-chain queries; each validation rule (duplicate id, equipment floor, dangling chain link, non-contiguous chain) throws a descriptive `ExerciseLibraryError` from a broken fixture; missing-resource and decode failures |
| Engine - session shape (`SessionShapeSelectionTests`) | `SessionShapeTemplate.select` maps 5/10/15/20/30/45/60 and the 10/20/40 boundaries to single-focus / blend-light / blend-full / blend-extended across the full 5-60 range, clamps out-of-range requests to the `5...60` supported range, is deterministic, and resolves every blend to the canonical `.blend` `SessionShape` |
| Engine - pillar balance (**removed by US-M01**) | The `PillarBalance` split step and its `PillarBalanceTests` suite were **removed by US-M01** (2026-08-13): with the Movement Practice mobility block gone, there is no strength-vs-mobility split to compute (the lead is unconditionally strength) and no `PillarStaleness`/`PillarPlan`/`PillarWeights` to test. The surviving strength-lead and strength-share invariants those tests once pinned now live end-to-end in the session-assembly and strength-primary-regression rows below (see the US-M01 row). |
| Engine - movement pattern (`MovementPatternFocusTests`) | `PatternStaleness` days-since-worked per pattern (most-recent wins, skips excluded, never-worked nil); `rank` stalest-first with never-worked most stale and canonical tie-break; `select` no-repeat of the recent session's lead pattern (isolated from staleness, window-bounded, pillar-aware), explicit-request override, no-history default, single-candidate fallback, determinism |
| Engine - exercise pool filter (`ExercisePoolFilterTests`) | `InjuryContraindication` tag -> pattern mapping (case/plural/separator-insensitive normalization, unknown-tag no-op, multi-injury union); each filter rule independently (phase gate, fitness-level difficulty cap, injury, recent-skip > 3, equipment floor) and combined (US-C04 validation case); per-pattern fallback (relaxed soft filters picks the safest option, injury/phase-gated empty pattern reports `.noSafeOption`); determinism / library-order preservation |
| Engine - progression chain (`ProgressionChainSelectionTests`) | `AdvancementCriteria` parses `NxM` / standalone / hold-seconds and is-met checks (reps and holds, qualifying-set count); `selectInChain` no-history entry (at the chain entry with nothing withheld, past the tiers the cold-start Start Seed band held out of reach, at the chain's own *gentlest* tier when every eligible tier was withheld - never its hardest, which would invert the pool restriction that narrowed it - and unchanged by default), frontier = highest-order worked, advance only when cleared, no leap past an un-cleared tier, no advance onto an ineligible/gated next tier, clamp when the frontier tier is ineligible, skipped-work excluded; `select(pattern:)` no-repeat-last-3 variety, active-chain preference, no-history gentlest entry, determinism; variety never buys a regression - a chain the Start Seed band withheld loses to repeating the movement the user actually trains, because "never on offer" is not "fresh" (the post-handoff cliff guard: Step 0's band withholds whole chains during cold start, so their untouched entry tiers are the only "fresh" candidates the session the band lifts), while a fresh in-band candidate still wins over both a genuinely withheld candidate in the same running and the movement worked last session, a chain that tops out inside the band is *not* retired by a harder tier the user reached elsewhere in the pattern, and an entirely-withheld candidate set (a Return, an injury filter) leaves the band unreachable so the no-repeat window applies normally instead of silently switching itself off; an empty withheld set reproduces the plain chain-local behavior exactly; the tunable `varietyWindow` lever (US-E03) widens/narrows the no-repeat lookback and defaults to `recentSessionWindow`; PRD validation (cleared knee push-ups -> standard push-up) over the real library |
| Engine - adaptive overload (`AdaptiveOverloadTests`) | `AdaptiveOverload.target` no-history rep/hold defaults (exercise-specific, not a constant) and empty/zero-set fallback; progressive bump from capacity (unrated and `justRight`); `tooHard` ease below / `tooEasy` push above capacity, both within one cycle, with guaranteed direction on small capacities and the rep floor honored; hold logic in seconds; capacity-relative (different capacities -> different targets), most-recent-usable performance wins, determinism; the `progressionRate` lever (US-E03) advances too-easy/progressive targets faster (reps and holds) while leaving the `too_hard` ease untouched and honoring the rails, and its default parameter equals a neutral `1.0`; the Asymmetric Ramp (US-E05) - eager down-step >= patient up-step (asserted on both the constants and on live targets), a recent skip eases like a `too_hard` (reps and holds) and wins over an earlier `too_easy` while the worked session still supplies the set count, rails honored; the Re-entry Ramp (US-E06) - a `reentryScale < 1.0` holds a capacity-derived target below the neutral one and climbs back monotonically as it relaxes to `1.0`, honoring the rep/hold floors, leaving a no-history default untouched, composing with `progressionRate`, and the neutral scale a no-op; PRD validation (3x12 squats: `too_hard` eases immediately and by more than `too_easy` climbs) over the real library |
| Engine - session assembly (`SessionAssemblyTests`) | `SessionAssembly.assemble` runs Steps 1-6 end-to-end over the real library: every session opens with a `.warmup` block; **a short single-focus session leads strength** (US-001) - a fresh (no-history) desk worker at 5 and 10 min, the originally-reported all-mobility failure profile, now opens with a mobility warm-up and then a `.strength` training block with **no** `.mobility` training block and no mobility movement outside the warm-up; the **length-scaled warm-up** - it is seeded at `warmupExerciseCount` distinct one-set mobility movements (1-4 across the ≤10/11-20/21-40/41-60 bands under US-003, a lean opening that never crowds out strength), grows with session length, never exceeds the `maxWarmupExercises` ceiling (4), and even the worst-case mobility-stale 60-min blend (warm-up at ceiling + Movement Practice at cap) reuses no mobility movement across blocks, leaves pool headroom, and never starves the cooldown of its holds; a `.cooldown` closes only sessions over 10 min; `plannedSeconds` (`Σ(sets × workPerSet) + rests + transitions`, `workPerSet` re-priced as a fixed per-set setup cost plus the per-unit work of the target actually prescribed) lands within ±1 min for 5/10/15/20/30 (history and fresh-user, **and** for a long-tenured user whose per-side holds have all grown to their rail - the other two fit cases run against history sitting near the rep defaults with no holds at all, which is exactly where the setup/per-unit split and the per-side doubling are both no-ops), that case also asserting `AdaptiveOverload` clamps a per-side hold so the *set* costs at most `maxHoldSeconds` rather than that much per side; the timing fit promoting a reserve at any set count within the rails so a block already at `maxTrainingSets` still has a fine-grained lever; the **one-set Movement Practice rule** - every exercise in the mobility Movement Practice block is exactly one set at every blend length (15-60; single-focus 5-10 carries no mobility training block under US-001) and worst case (a mobility-stale history that drives the block to its cap), the turned-into-a-test form of the reported "Cat-Cow 2x10 / Pigeon 4x0:45 per side" bug; the block grows a longer session by exercise **count**, not sets (15 -> 60 min adds distinct movements up to `maxMobilityTrainingExercises`, all still one set); and the two long durations that stressed it most - 45 and 60 min mobility-leaning - still land within ±1 min once the strength/primal set lever (`maxTrainingSets`) carries the time the capped one-set mobility block no longer pads; sub-100ms latency; fully-formed, capacity-relative output (rep xor hold per `isHold`, sets >= 1, equipment floor, no cross-block repeats); structural determinism; a blend is **strength-dominant** (US-002): the strength block owns strictly more planned time than the mobility accessory whether mobility is the stalest pillar or the freshest, a no-history 20/30-min session keeps strength the dominant share of the training-block time (bounded `0.7...0.88` - the upper bound widened from `0.85` under US-003, whose leaner warm-up routes freed budget into strength sets so the realized share runs up to ~0.87 / mobility ~13%) with a genuine mobility accessory present and strength leading (the US-002 validation), and the fit still lands within ±60s across 15/20/30/45/60 for both an active and a sedentary (larger-accessory) profile; a blend cooldown reserves more than one static hold (holds-only honored); an extended blend (US-E02) carves out a dedicated `.primal` block (`pillar == .primal`, `locomotion`, equipment floor) alongside strength + mobility and never folds primal back into the strength block, while 10/20/30-min shapes carve out no primal block (no regression), all deterministically; the Session Policy levers (US-E03) thread through `assemble` - `SessionPolicy.default` reproduces the unpoliced output across 10/20/30/50 min, even a doubled `pillarWeighting[.mobility]` cannot unseat the strength lead of a blend and leaves the fixed mobility accessory unchanged (US-002 - the lever now leans only the extended blend's within-family strength/primal split, covered in pillar balance), and a higher `progressionRate` advances a selected exercise's rep target end-to-end within the rails; the Step 0 cold-start override (US-E04) threads through `assemble` too - `ColdStartOverride.cappedPool` removes every over-cap movement while active and is a no-op once cold-start retires or no contract is present, an end-to-end advanced-user session caps difficulty (an over-cap `push_diamond` that appears unpoliced is held back to `push_standard`), every cold-start single-focus day leads strength (US-004, reversing the retired First-Week Contrast rotation) - even a desk worker's mobility bias and even an injured beginner on the day the old rotation forced primal - the strength lead never draws from the `locomotion` pattern, a warmed-up user (`coldStart.active == false`) is byte-for-byte unaffected, and cold-start assembly stays deterministic on the forced strength-led day (PRD validation: a beginner's cold-start sessions lead strength and never exceed difficulty 2); the Step 0.5 Return override (US-E06) threads through `assemble` too - a 10-day gap caps difficulty below the tier the same history advances onto unpoliced (the Return is measurably easier than the pre-gap norm), a Return single-focus leads with strength over the stalest pillar (US-005, Step 2 discipline-over-optimization - kept gentle by the cap and floor, not the pillar), the Re-entry Ramp climbs a selected exercise's rep target back over decrementing `reentry.rampSessionsRemaining`, the engine stamps `wasReturn` on the assembled `Workout` (true on a Return, false for a present user and while cold-start suppresses it), the Return is suppressed while cold-start owns the first sessions, and Return assembly stays deterministic; PRD validation (intermediate user, some history, 20 min -> warm-up first, cooldown last, 19-21 min, <100ms) |
| Engine - exercise swap (`ExerciseSwapTests`) | `ExerciseSwap.swap` takes the session's `sessionPolicy` (US-O02) so a substitute is sized by the same Step 6 levers the lineup was - `progressionRate`, the cold-start Start Seed, `varietyWindow`, and the Return / Re-entry Ramp ease (US-E06) read off the session's own `wasReturn` stamp - with the default policy reproducing the pre-policy swap exactly; the general property is pinned rather than each lever separately, so a fourth cannot go missing silently: every lever visibly moves the substitute's target (a faster `progressionRate` climbs, a Return drops to `reentryFloorScale`, a mid-ramp session to the ramp's partial scale, the Start Seed opens a no-history substitute above the movement's own default); a valid swap stays within pillar/pattern/difficulty band and the slot time budget (PRD validation over the real library), the budget check pricing **both** sides through `SessionAssembly.workSecondsPerSet` at the target each movement actually carries - so a capacity-grown 24-rep `push_diamond` slot no longer accepts a never-logged `push_pike` opening at its 8-rep default just because the two flat estimates match (grown past even the raised 5-set rail's reach, so the guard holds regardless of the ceiling); the **set count** is the lever that keeps that check from disabling the feature - a slot already in budget at its own count keeps it, a capacity-grown `push_standard` slot at 12 reps (its own "3x12" advancement target) is re-picked inside `minTrainingSets...maxTrainingSets` instead of refused, and a **one-set** block may not spend the lever at all: a stretch that only fits at two sets is refused outright in the warm-up, the cooldown, **and the mobility Movement Practice block** (all `allowSetAdjust: false` - a stretch is one set at every length, so a swap never reintroduces a multi-set stretch), the fixture sized past the *widened* one-set tolerance so it asserts the lever is withheld rather than re-asserting the gate; set-preservation is pinned as outranking drift - `push_standard` grown to 10 reps must return the peer that fits at 3 sets rather than a closer 4-set restructuring, and `mobility_pigeon` at its own default must stay one set rather than becoming two sets of another stretch, both drawn from the shipped catalog and both previously restructured; the whole-library sweep runs at **four growth levels** (x1.0/x1.25/x1.5/x2.0 of each movement's own default, not only the defaults, where the target-scaled check is a no-op), asserting every substitute lands inside the tolerance, stays inside the set rails, and that coverage never collapses (without the lever it collapses at the higher growth levels), and is run **twice**, once in the adjustable 3-set/40s-rest shape and once in the real non-adjustable bookend shape (1 set, `mobilityRestSeconds`, `.warmup`), because the adjustable shape is the one configuration where the set lever always exists; bookend coverage is asserted at the numbers that actually hold over the expanded 26-movement mobility catalog (26/26, 26/26, 26/26, 25/26), the single refusal at x2.0 being a stretch grown past any comparable one-set peer; substitute is bodyweight, discipline-phase, and carries a capacity-relative per-set target; the Start Seed is scoped as the assembler scopes it, so a swapped *mobility* movement opens at its own default even for an advanced cold-start user; injury-respecting (a shoulder-injured push slot refuses rather than offering an unsafe peer; a knee injury leaves a push swap intact); no-alternative cases (lone peer, phase-gated peer, a peer no permitted set count brings inside the budget); never duplicates a movement already in the session; preserves the slot's rest; determinism |
| Engine - Return / Re-entry Ramp (`ReturnOverrideTests`) | `ReturnOverride.isReturn` fires at/above the `returnThresholdDays` gap and not below, uses the most recent session, and is `false` for a no-history user; `returnPool` caps difficulty on a Return and never empties the pool; `overridePlan` leads single-focus and blend with **strength** (US-005 - the comeback's gentleness now rides the difficulty cap and volume floor, not the pillar; a no-op off a Return); `reentryScale` serves the floor on a Return, climbs monotonically from floor to neutral as `rampScale`'s counter decrements, is neutral with no ramp/Return, and clamps an out-of-range counter |
| Consistency Score (`ConsistencyScoreTests`) | US-H01: the pure `ConsistencyScore` evaluator - empty history is zero, a fully on-goal history is 100, a single missed day dents modestly (never zero), a 5-minute session is a full show-up (duration irrelevant to adherence), recent weeks outweigh older ones in the rolling average, an unexcused empty week dents but never zeroes; Return protection (PRD validation: a Return after a multi-week gap does not reduce the score - the gap weeks it closed are excused and the comeback week counts as a full show-up - while the same comeback without the flag is penalized), `longestChain` is the best run ever achieved (a later break never lowers it, a Return week counts toward it), the `weeklyGoal` is respected and clamped to >= 1, determinism |
| PhaseEvaluator (`PhaseEvaluatorTests`) | US-H02: the pure `PhaseEvaluator` over a self-contained synthetic library - a fresh user is Discipline; consistency-only (8 on-goal weeks, no cleared entry tier) stays Discipline; competence-only (all four entries cleared but <1 window of history) stays Discipline; a hot single week that clears every foundation and scores 100 still fails the sustained-over-8-weeks requirement; both-met promotes to Strength; three-of-four foundations stays Discipline (AND across push/squat/hinge/core); a logged-but-not-cleared entry (short reps) and a skipped clearing set never count as competence; PRD validation (8 weeks >= 80% without cleared entry tiers -> Discipline); determinism |
| Consistency trend (`ConsistencyTrendTests`) | US-M01: `ConsistencyTrend.trend` - an empty history has no trajectory, three weeks of history yields three ascending oldest-first points, the span is capped at the rolling window (`recentWeeksWindow`) and starts no earlier than first activity, the newest point equals the headline `ConsistencyScore.evaluate` score (single source of truth), earlier vantages reflect only the activity accumulated by then (future-dated logs discarded), and it is deterministic |
| Session policy (`SessionPolicyTests`) | `SessionPolicy.default` documented values / determinism / equal weighting of all three pillars; Codable round-trips (default, fully-populated, LLM note, `[Pillar: Double]` intact); `updatedBy`/`note.source` raw-value pins |
| Cold-start seeding (`ColdStartSeedingTests`) | `ColdStartContract.cappedMaxDifficulty(for:)` maps beginner/intermediate/advanced to 2/3/4 and never exceeds the steady-state `ExercisePoolFilter.difficultyCap`; `seeded(for:)` forces First-Week Contrast and carries the cap; `SessionPolicy.seeded(forFitnessLevel:)` layers only the contract onto the neutral default (stripping it yields `default`); end-to-end (PRD validation) three fresh users' first sessions never exceed their cap and open *exactly* at their level's seeded `startingDifficultyFloor` - asserted against the contract rather than re-derived from the pool the assembler drew from, so the assertion can actually fail |
| Fitness-level start seeding (`StartSeedTests`) | US-O02: the Start Seed mapping per fitness level (floor 1/2/3, volume x1.0/x1.15/x1.30 over 3/3/4 sets), the beginner's seed is exactly neutral, `seeded(for:)` carries the whole seed, each active level's floor sits strictly beneath its cap so the band is a real range, the three levels resolve to distinct banded pools over the real library (intermediate strictly wider than advanced), no banded pattern is ever starved, and the neutral constants equal the engine's own defaults; backward-compatible persistence (the two-argument memberwise init, a legacy contract and a legacy whole policy decoding to the neutral seed, a seeded policy round-tripping); pool banding over the real library (strength/primal floored to each pattern's band entry, a beginner's pool byte-for-byte the capped pool, mobility identical at every level, library order preserved, a synthetic pattern with nothing as hard as the floor clamped to its own hardest rather than emptied, and a no-op once cold-start retires or no contract is present); the no-history volume seed (reps and holds scaled, rails honored, neutral reproduces the default target, a capacity-derived target untouched); the self-correction safety net - the Asymmetric Ramp still eases the volume within one cycle (and by more than a too-easy climbs, a skip too), and `ColdStartOverride.startSeed` also steps the *tier* down, but only on an explicit `too_hard` rating: that rating lowers the floor a tier, drops a set, and eases the rep multiplier, while two routine skips ease the volume (to the neutral set floor) and leave the tier band exactly where it was, never past neutral, with a beginner's already-neutral seed untouched and an end-to-end second session served a strictly easier lead movement; the seed survives the seams it crosses - a swap under the session's policy sizes the substitute at x1.30 where an unpolicied swap falls back to the movement's default, and `SessionAssembly.workSecondsPerSet` splits a movement's estimate into a fixed per-set setup cost plus a per-unit cost - a hold's per-unit cost is known a priori at one second of work per prescribed second *per side*, so its setup is the authored remainder `estimate - sides × defaultDurationSeconds` (`mobility_9090_hip` grown to 40s costs 90s; `core_side_plank` at "3x30s hold per side" grown to 40s costs 85s, not the 65s a per-side-blind split charged; a hold clamped to `maxHoldSeconds` costs its setup plus the hold on every side it is held), while a rep, which has no such anchor, takes the assumed `setupSecondsPerSet` and derives its cadence from its own authoring, so a slow per-side movement keeps it (`push_archer` at 8 reps costs 74s, not the 59s a fixed per-rep cadence gave) - with the catalog sweep bounding the model from **both** sides: every movement reproduces its authored estimate exactly at its own default, its implied setup is never negative (which is how strict proportionality over-charged a grown set) and, for a rep-based movement whose setup is assumed rather than observed, never more than `maxSetupShareOfEstimate` of the estimate (which is how a grown set gets under-charged), and every hold's set costs at least the hold itself on every side it is held, at half, one, two and `maxHoldSeconds` times its default - the assertion the per-side-blind split failed (flat fallback with no default) so every seeded 5/10/15/20/30/45/60-min session at every level still lands within the ±1 min tolerance; the movements the band withheld (`ColdStartOverride.withheldByStartSeed`) - empty for a beginner whose floor is neutral, exactly the banded movements beneath the live floor and never a mobility one, shrinking (never growing) when a `too_hard` rating eases the seed so the reopened tier stops being withheld in the same move, read once the contract is cleared from the floor `ColdStartHandoff` *recorded* at the handoff - resolved over a realistic week whose retiring session is still unrated, with the un-eased aim recorded beside it so a `too_hard` that lands on that session afterwards re-resolves the floor (idempotently, and re-rating away from `too_hard` restores it) while a rating on a later session leaves the retired band alone - rather than re-derived from the never-revised self-report (an eased floor of 2 keeps the difficulty-2 tiers on offer past the retirement, and a user with no recorded floor - a pre-US-O02 record, a neutral contract, a generation against `SessionPolicy.default` - withholds nothing at all rather than acquiring a band retroactively), and unmoved by a routine skip; the band leaves no cliff behind it - an advanced user walks the whole five-session cold-start window completing everything prescribed, the first session after `ColdStartHandoff` clears the contract prescribes nothing gentler than the tiers that week trained at, and a pattern that week never touched at all still resolves at or above the seeded floor rather than at its chains' entry tiers; easing needs no precedence rule to win - a Return caps the pool below the whole band, which leaves nothing in-band to prefer, so it still serves under `ReturnOverride.returnMaxDifficulty` despite a strong pre-gap history, and a `too_hard` rating de-escalates the tier even in a pattern the user *does* have history in (advanced squat: `lunge_split_squat` at difficulty 3 held vs. a strictly easier tier eased); PRD validation end-to-end (an advanced user's first strength lead is a harder tier at x1.30 reps over 4 sets pre-fit, the beginner's is unchanged, warm-up/mobility/cooldown identical for both) and determinism |
| Cold-start strength lead (`FirstWeekContrastTests`) | US-004 (reverses the retired US-G02 First-Week Contrast spread): the pure `ColdStartOverride.overridePlan` rule (every cold-start day leads strength - a desk worker's mobility-biased single-focus plan becomes `.single(.strength)` on every day, no rotation; `forceContrastSpread` is the on/off switch - off, a mobility-biased plan is a no-op passthrough; blend days lead strength too via `favoring(.strength)`, which preserves the share multiset so the blend still sums to 1 and starves no pillar; determinism over inputs); the real onboarding seed end-to-end through `assemble` (PRD validation: a `sitsLong` beginner's first three seeded sessions all lead strength, and every selected movement stays at or below the cold-start difficulty cap - gentleness preserved); and the control that the same desk worker on the neutral `default` (no contract, Step 0 a no-op) also leads strength (US-001 steady state) - the override and the steady state now agree |
| Variety Language (`VarietyLanguageTests`) | US-G03: `VarietyLanguage` template - the contrast line offline (PRD validation: a mobility session after a strength one -> "Today's a mobility day - yesterday was strength", `source == .template`), only-named first session (no fabricated yesterday), same-pillar drops the contrast clause, per-pillar labels, most-recent log wins, blend-log lead = most-worked non-skipped pillar, the "today" pillar matches the actually-assembled session end-to-end over the real library, a warm-up-only session yields no note, determinism; `VarietyLanguageResolver` - offline returns the template even with a provider, graceful fallback to the template on a simulated proxy failure / empty line / warmed-up user, the LLM line used (`source == .llm`) only when online + cold-start-active + provider, template-only with no provider (the MVP default) |
| Variety Language proxy client (`ProxyVarietyLanguageProviderTests`) | US-N05: `ProxyVarietyLanguageProvider` over a stub transport - a success returns the trimmed line with exactly one call at the injected endpoint/timeout; the request body carries only the contrast (today/yesterday pillars + labels, `yesterday` omitted when absent) and never any user field; an injected `sharedSecret` is sent as the `Authorization: Bearer` header (and omitted when absent); each failure throws so the resolver falls back - non-2xx (`.badStatus`), empty line (`.emptyLine`), undecodable body, and a propagated transport error; and end-to-end through `VarietyLanguageResolver` the proxy line is used (`source == .llm`) online + cold-start-active while a proxy failure falls back to the template, never blocking |
| Cold-start handoff (`ColdStartHandoffTests`) | US-G04: `ColdStartHandoff.advanced` increments `sessionsLogged` per completed session and flips `active` off exactly at `handoffThreshold` (5), staying on just below it, one-way (a retired state is frozen), across the full window; `reconciled` clears `coldStartContract` once inactive (yielding exactly `SessionPolicy.default`), retains it while active, and is a no-op with no contract; the combined `afterCompletedSession` flips and clears together on the fifth session (validation test), keeps both mid-window, and carries every other user field through; US-O02: the retiring session - and only it - records the Start Seed floor the week actually ran at onto `User.ColdStart.bandFloorAtHandoff` (mid-window sessions leave it `nil`), with `band` a required argument so a caller can never silently record "no band ran", and a handoff with no cold-start contract in play records the neutral floor, so a band is never asserted over a user who never ran one; Step 0 (`ColdStartOverride.isActive`/`cappedPool`/`overridePlan`) is a proven no-op after the handoff; determinism |
| Reprogram trigger (`ReprogramTriggerTests`) | `ReprogramTrigger.Kind` raw-value pins (the four PRD kinds) and case count; `id` is the kind raw value; Codable round-trip for every kind with `detectedAt` intact |
| Session policy service (`SessionPolicyServiceTests`) | `MockSessionPolicyService` returns `SessionPolicy.default` from `currentPolicy` and `reprogram` (every trigger kind) and an empty `dueTriggers` for a fresh user; `seedInitialPolicy` hands back the fitness-level-seeded cold-start contract (US-I01) |
| AI Programmer - re-weighting service (`DeterministicSessionPolicyServiceTests`) | `DeterministicSessionPolicyService.currentPolicy` is the default until a policy is written; `reprogram` increments `version`, stamps `updatedBy == .deterministic` and `updatedAt = trigger.detectedAt`; each trigger moves the expected levers (physical_stall raises progression/variety, disengagement eases and never raises challenge - even off a physical_stall trigger via re-diagnosis, return seeds the `reentry` ramp and leaves optimization levers neutral, weekly_boundary moves only the learned duration); the written policy persists and reads back through `currentPolicy`; Default Duration learning tracks completed-not-requested minutes and folds each session exactly once across re-programs (PRD validation: requested 20 / completed 12 -> default snaps to 10 or 15, honest note names the change without claiming a difficulty move); note is `nil` when nothing observable changed; `seedInitialPolicy` (US-I01) persists a fitness-level-capped cold-start contract that reads back through `currentPolicy` while keeping the default's `version`/`updatedBy` |
| AI Programmer - trigger detection (`ReprogramTriggerDetectionTests`) | `ReprogramTriggerDetection.dueTriggers` fires each kind independently - `weekly_boundary` on a new calendar week (not within the same week), `return` on a long gap, `physical_stall` on a frontier tier with a gated existing next tier cleared 2+ sessions without advancing (not on a single clear, skipped work, or a repeatedly-cleared single-tier mobility chain), `disengagement` on a falling requested-vs-completed completion ratio or an elevated window skip rate (not on steady sessions, on intentionally shorter sessions finished fully, or fewer than a full window); Trigger Precedence (disengagement suppresses physical_stall, PRD validation); no triggers for a fresh or healthy in-week user; precedence ordering (`return` before `weekly_boundary`); determinism and `detectedAt` stamped from `asOf` |
| AI Programmer - plateau diagnosis (`PlateauDiagnosisTests`) | `PlateauDiagnosis.diagnose` classifies a clear stall as `physicalStall`, a falling-completion history as `disengagement`, a mixed history as `disengagement` (Trigger Precedence, PRD validation), and a healthy/fresh/single-tier-cleared history as `nil`, deterministically; the explicit lever mapping `reweighted(_:for:)` raises `progressionRate`/`varietyWindow` on a stall and eases/narrows them on disengagement (never raising challenge from any starting policy), moving no other lever and no `version`, and clamps both levers to their rails on a recurring plateau |
| AI Programmer - Default Duration learning (`DefaultDurationLearningTests`) | `DefaultDurationLearning.snappedToChip` snaps to the nearest chip and resolves a boundary to the shorter chip; every learned default is a valid chip; the EWMA anchors to the onboarding seed (else the existing average) and moves toward completed durations, tracks completed not requested minutes, folds `from recentLogs` in chronological order regardless of input order, never mutates `onboardingSeedMinutes`, treats empty input as a no-op, and is deterministic; PRD validation (requested 20 / completed ~12 -> EWMA trends toward ~12, default snaps to 10 or 15, never the requested 20) |
| AI Programmer - templated policy note (`PolicyNoteTests`) | `PolicyNote.templated` names a stepped-up challenge on a stall diff and eased intensity on a disengagement diff (each with the matching variety clause), reports a progression-only or variety-only move without over-claiming, appends the new go-to duration when it changed, returns `nil` when nothing observable moved, is always `source == .template`, never references the user's `why`, and is deterministic |
| Persistence | Save/fetch round-trips for `CDUser` and `CDWorkoutLog`; `CDSessionPolicy` default/fully-populated round-trip, in-place overwrite, `userId` scoping, loud failure on a missing blob; US-K04: `CDActiveSession` round-trip (with running-rest deadline and paused-rest remainder), in-place overwrite, `userId` scoping, loud failure on a missing blob; the `ActiveSessionState` JSON codec round-trips losslessly (including the `[UUID: [CompletedSet]]` completed-set map); `CoreDataActiveSessionStore` saves, loads, and clears an in-progress session; US-O03: the only thing a hold puts on disk is the side still owed, and three tests hold that line - `testSaveAndReloadActiveSessionWithHoldBetweenSides` round-trips a per-side set parked between its legs through CoreData, so a user who left half way through a two-leg set is never asked to repeat side 1; `testActiveSessionStateDecodesAHoldWrittenWithTheRunningLeg` feeds the decoder a snapshot in the earlier US-O03 shape (`totalSeconds`/`deadline`/`remainingWhenPaused`/`isRunning`) and asserts it decodes to the side it recorded while the running leg is discarded - which is the point, since a restored leg is what banked sets nobody performed; and `testActiveSessionStateDecodesWithoutTheHoldField` feeds it a pre-US-O03 snapshot with no `hold` key at all, so neither shape costs a user their in-progress session across an app update |
| CoreData services / production container (`CoreDataServicesTests`) | US-N02: `CoreDataUserService` save/read-back, upsert-in-place by id (no duplicate), delete clears; `CoreDataWorkoutLogService` save/read-back, upsert-in-place by id (the rating re-save), half-open `[start, end)` date-range query sorted oldest-first, delete by id; the production `ServiceContainer.live(...)` composes the CoreData-backed services over one shared context (a user/log written through it reads back, the policy service returns the default with no due triggers for a fresh user, and the completion recorder writes a durable log). Both container tests pass `NoOpAnalyticsService` through the factory's telemetry seam (US-T04): they build the production container for its CoreData wiring alone, and substituting the sink is what makes them structurally unable to reach the network now that US-T07 through US-T12 have added the emission call sites, rather than incidentally quiet because none exist (FR-13 - which that seam holds for in-process tests like these; the out-of-process `RepTodayUITests` launches build their own container and rest on US-T06's opt-out flag instead, which has since landed as the `-AppState.analyticsEnabled NO` launch argument - available to every XCUITest launch, though not passed by every one of them; see the `TelemetryOptOutUITests` row for which legs use it and which are held off the wire another way) |
| Onboarding view model (`OnboardingViewModelTests`) | US-I01: `buildUser` maps the collected answers (trimmed name/why, no opening bias in the minimal flow, `primaryGoal` defaulted to `.stayActive`), the one duration answer seeds both `defaultMinutes` and `onboardingSeedMinutes`, `coldStart` starts fresh (active, nothing logged), injuries become sorted engine tags; `finish` saves the user and seeds a cold-start policy that reads back with a contract capped from the fitness level; a save failure surfaces a message, returns false, and resets the in-flight flag; step navigation walks/clamps, the basics step gates advance on a non-blank name, and the duration step is always ready so finish is never gated; US-N01: identity resolution keys the saved user by an in-flow Sign in with Apple identifier - recorded either through the official `SignInWithAppleButton`'s `completeSignIn(identifier:)` path or the programmatic `signInWithApple()` path - or a previously-persisted one, and falls back to the local identifier when no auth service is wired or a sign-in fails (non-gating - the failure is swallowed and the first session is never blocked), and `canSignInWithApple` gates the affordance to an unsigned user; US-O04: the imperial answers convert to metric at the boundary - 5 ft 7 in / 165 lb persist as ~170.18 cm / ~74.84 kg, the derived `heightCm`/`weightKg` track the imperial state with no second representation to drift, `heightTotalInches` normalizes the feet/inches split in both directions, the US defaults are asserted to *differ* from the values the validation test types (so a mapping that ignored its input could not pass by coincidence), and end-to-end past the boundary the HealthKit energy estimate prices a 60-min 5-MET session off kilograms (~374 kcal, pinned below `5 x 165` outright) - reading pounds as kilograms would inflate every calorie written to Health by 2.2x unflagged; US-T08: the two onboarding-funnel emissions, asserted through the recording `MockAnalyticsService` with an advanceable clock - `onboardingStarted()` emits exactly one `onboarding_started` with no properties and is idempotent across a re-`onAppear` (the emit-once guard), a successful `finish()` after it emits exactly one `onboarding_completed` carrying `elapsed_seconds` as `.int` equal to the whole seconds the clock advanced (42.9s truncates to 42, off the injected clock, no raw `Date()`), a failed `finish()` (the user service throws) emits **no** `onboarding_completed` so the event is bound to the success branch that also fires `onComplete`, and the sink is optional so a view model with none wired still completes onboarding - consent is enforced inside the live sink, so no call-site opt-out check is present or tested here (that gate is `LiveAnalyticsServiceTests`/`TelemetryOptOutUITests`) |
| Unit conversion (`UnitConversionTests`) | US-O04: the pure imperial <-> metric conversions onboarding runs at its input boundary - the PRD's known values (5 ft 7 in == 170.18 cm; 165 lb == 74.8427 kg), the exact international factors pinned as constants, and round-trips over the *whole* range the UI offers rather than a sample (every inch of 47...87, every pound of 70...445 - the imperial cover of the 120...220 cm / 35...200 kg sliders they replace, rounded outward at every end, so nobody the app used to fit is silently clamped on the way in); the `totalInches` label overloads agree with the split ones across that whole range and clamp a negative total, keeping the feet/inches split in the one type that owns it rather than in a `/ 12` per caller; `feetAndInches(fromCentimeters:)` rounds total inches and re-splits, so a height rounding up to a whole foot reads `6 ft 0 in` and never `5 ft 12 in` (the one arithmetic bug this conversion has), rounds to the nearest inch in both directions, converts an un-normalized `5 ft 19 in` to the height it names, and clamps negative/zero input; the spoken forms agree with their own counts ("1 foot 1 inch", "1 pound") |
| Onboarding basics, rendered end-to-end (`OnboardingBasicsEvidenceTests`) | US-O04: hosts the production `OnboardingView` driven to its basics step in a real key window and reads the live accessibility tree, so the assertions are on what the screen says and VoiceOver speaks rather than on a reimplementation - each measurement row is named once and speaks its value as a sentence ("Height" / "5 feet 8 inches"), no metric unit survives anywhere on the step, every row (Age included) clears the 44pt touch-target floor on its *laid-out* frame - which is how the 20pt slider and the 46.5 x 32pt platform stepper were both caught - and every row is an `adjustable` element whose VoiceOver increment/decrement moves it by that row's own step (1 in, 5 lb). Hosting and settling poll the runloop until the element being read is actually there, failing at the same deadline and naming what was awaited, rather than spending a fixed wait on every call. Renders the step at the default and largest accessibility type sizes to `artifacts/reports/us-o04/` (a per-run temporary directory unless `REPTODAY_WRITE_EVIDENCE=1`), the accessibility render being what caught the row overflowing and the glyph drawing through its own button |
| Step-button press and hold (`PressRepeaterTests`) | US-O04: the touch path behind the hand-drawn stepper, which the accessibility assertions never reach (`accessibilityIncrement()` routes through the adjustable action and presses nothing). Nothing commits while the finger is down: the step lands on **release**, so a finger that touches `+` on its way to scrolling the step moves nothing, and a press that ends anywhere but on the button - stolen by the scroll view, carried clear of the control, the row disappearing - commits nothing at all, however often that end is reported. A tap is exactly one step whichever order the button reports the release and the end of tracking in (the platform decides, and neither ordering may double-count or swallow it), and a held press ends exactly where it stood when the finger lifted, its release adding nothing on top - including at the end of the range - which is the double-fire a tap gesture stacked on a long press produced. Deliberately absent is *where* the finger was: the button is a real `Button`, so a release reaches the repeater only as a platform touch-up **inside the button's own bounds**, touch slop and all, and the hand-rolled bounds rules that preceded it are gone rather than tested - a `TapGesture`'s unconfigurable ~10pt against a 22pt tracker silently dropped any press drifting between them, and the drift radius that replaced it was drawn around where the finger *landed*, which is a circle of the view's own invention rather than a button's bounds. Both ways a hold can finish clear the flag saying the repeat already owns the press: a reported release consumes it, and a repeat ended by a refusal clears it on its way out - the case no release ever reports, since a hold that reaches the bound disables its own button under the finger. Either sequence is driven to a later release that must still step, so a stale `true` cannot swallow one. A hold waits out the hold delay once and then runs on the repeat interval, and a hold that reaches the end of the range stops on the refusal instead of writing the same clamped value back through the binding ~11 times a second until the finger lifts (Observation notifies on every write regardless of equality). The clock is injected, so a whole hold is driven without waiting on one |
| Onboarding basics, pressed in the shipping app (`OnboardingImperialUITests`, `RepTodayUITests`) | US-O04: the same step driven through the installed app by a real finger, which is the one thing the hosted-window tests cannot do - a `Button`'s press state comes from touch delivery, so `PressRepeater`'s state machine is exercised in isolation and the accessibility path presses nothing. The run launches through the shared `TestApp` wrapper (`RepTodayUITests/TestApp.swift`), naming the `.optedOutWithNoProbe` posture, which contributes `-AppState.analyticsEnabled NO` alongside `-AppState.isOnboarded NO` (both read from the argument domain, so neither depends on what the last run left in the container); consent-off is carried on **every** launch of this suite, so it is off the wire before US-T07's app-entry emission makes that matter rather than after, and the wrapper - not a raw `app.launchArguments =` here - is now the only way this suite can launch. It walks welcome -> basics, and dismisses the keyboard, which covers the measurement rows entirely. The step buttons are `accessibilityHidden`, so they are pressed by coordinate off the row's own frame, the way a thumb finds them: the step opens on the imperial defaults with no metric unit anywhere in the live element tree, a tap on `+` moves the row by its own unit (1 in, 5 lb) and `-` walks it back, and a 1.5s hold repeats past the 400ms hold delay (measured 175 -> 225 lb) rather than stepping once. Every press test taps first as a positive control, so a coordinate that missed the button is reported as a miss instead of passing as a no-op. The story's acceptance path is then walked end to end: the PRD's validation measurements (5 ft 7 in, 165 lb) are dialled in with the step buttons, the remaining steps are answered on their defaults (bounded by the flow's own four remaining steps, so a step that stops advancing fails rather than spins), and the run must land on a generated session - the per-control tests prove the rows read and move in imperial, this one proves those answers are *accepted* by the flow they feed, a boundary conversion failure surfacing as onboarding that never reaches a session. That walk runs to the end of the real first launch, so it also answers the Health share prompt the main tabs raise on arrival - declined, then its follow-up confirmation dismissed - which is a remote view inside the app's own process rather than a springboard alert, so an ordinary query reaches it and no interruption monitor is involved; the step is bounded and optional, because a container that already answered has no sheet to dismiss and must still pass. US-T06 lifted that handling out of this suite into `RepTodayUITests/HealthAccessPrompt.swift`, unchanged, because `TelemetryOptOutUITests` meets the same sheet on arrival and a second copy would drift. The ready-session render is gated on the tab bar being hittable again rather than on the sheet leaving the element tree, which it does while still animating off. Two abandoned presses commit nothing by two different routes - dragged down the step, where the `ScrollView` takes the press over, and lifted 200pt clear along the row. The adjacent-button case is deliberately *not* asserted in either direction: landing on `+` and lifting over `-` is a short move, still inside `+`'s touch slop, so it steps `+` the way two adjacent buttons do anywhere else in iOS - a platform boundary rather than one this app draws, and pinning it would turn a change in Apple's slop into a failure of ours. Runs under its own `RepTodayUITests` scheme (`xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepTodayUITests -destination '<simulator>' test`), because it installs and launches the app out of process and the documented `-scheme RepToday test` should stay the unit run it has always been. Each test screenshots the running app where it asserts, files the capture with the `.xcresult`, and writes a browsable copy under the same convention the unit-bundle evidence tests follow: a per-run temporary directory by default, so a plain run never dirties the worktree, `artifacts/reports/us-o04/` under `REPTODAY_WRITE_EVIDENCE=1`, or wherever `REPTODAY_EVIDENCE_DIR` points. That scheme forwards both variables from build settings, since the XCUITest runner is a Simulator process and inherits no shell environment |
| Auth - Sign in with Apple (`AppleAuthServiceTests`) | US-N01: `AppleAuthService` over a stubbed authorizer + in-memory store - a successful sign-in returns and persists the stable identifier, `completeSignIn(identifier:)` persists an externally-obtained (button-completion) identifier into the same store and reads it back locally, `currentUserIdentifier` reads it back (a local, offline read), `signOut` clears it, and a failed authorization propagates while leaving the store untouched; the `InMemoryAuthCredentialStore` round-trips (save overwrites, clear empties, seeded initial value) |
| Onboarding injury vocabulary (`OnboardingInjuryVocabularyTests`) | US-I01: every offered `InjuryOption` tag normalizes onto an `InjuryContraindication` key (non-empty contraindicated patterns), the union of all options resolves, and the tags are pinned as a stable persisted contract - closing the pool filter's fail-open gap where an unrecognized injury tag silently disables protection |
| Ready view model (`ReadyViewModelTests`) | US-I01: `load` generates a session at the user's Default Duration and threads the in-force policy (cold-start contract) into the engine, surfaces an empty-state message and never calls the engine when there is no user, and end-to-end (real engine + library) produces a warm-up-first session at the requested length; US-J01: `durationChips` is the canonical 5/10/15/20/30/45/60 vocabulary, `selectDuration` regenerates at the new duration and updates `selectedMinutes`/`requestedMinutes` while keeping the session present, reuses cached inputs (the user is fetched once, not per tap), is a no-op on the already-selected chip or before a user loads, a re-appear (`load` again) preserves the user's tapped chip rather than snapping back to the default, a regeneration failure rolls the selection back and keeps the still-displayed session, and end-to-end (real engine) regenerates a 15-min session to a 30-min one on a chip tap; US-J02: `load` surfaces the session's Variety Language line (template-sourced, naming today's and yesterday's contrasting lead pillars), the forgiving Consistency Score, and the policy `note` (when present, `nil` otherwise); a due trigger kicks off a background `reprogram` against the highest-precedence one while the session renders immediately (Start never waits), no trigger writes nothing, and the on-open check fires once per open (a tab re-appear does not re-fire it); US-K04: `load` surfaces a `resumableSession` saved under the user's id (and `nil` when none is saved), `discardResumableSession` clears both the store and the surfaced state, and `refreshResumableSession` picks up a session the player just saved without a full reload; US-L01: `handlePlayerDismiss(completed: true)` re-reads the full log history and refreshes the displayed Consistency Score so a just-completed session is reflected on return; US-T09: the `ready_screen_shown` emission, asserted through the recording `MockAnalyticsService` with a shared advanceable clock the stub engine advances *inside* `generateWorkout` (0.25s -> exactly 250 whole ms) - the first `load` emits exactly one `ready_screen_shown` carrying `generation_ms` as `.int(250)` measured around the engine call specifically (and `lastGenerationMs` matches), a duration-chip regeneration does **not** re-emit (still one event after `selectDuration`) while `generation_ms` is still measured on that path, a tab re-appear (`load` again) does not re-fire it (the first-load one-shot guard survives a refresh), and the sink is optional so a view model with none wired still loads the screen - consent is enforced inside the live sink, so no call-site opt-out check is present or tested here; US-T10: the give-up abandonment side of session-lifecycle telemetry - `discardResumableSession` and `abandonResumableSessionForOverwrite` each emit exactly one `session_abandoned` off the persisted snapshot (`completed_minutes` from `exercisedMinutes`, `abandon_point` bucketed `warmup`/`mainWork`/`cooldown` by the block the current step sits in), an ordinary fresh start with nothing paused emits nothing, and a pause -> Discard lands exactly one abandonment across the physical session (the completed side and the full cross-view-model invariant live in the `ActiveSessionTelemetryTests` row) |
| Progress view model (`ProgressViewModelTests`) | US-M01: `load` populates the headline `consistency`, the `trend`, and the `completedDays` from the full log history; `longestChain` surfaces as an earned achievement; `completedDays` are start-of-day normalized; the headline score matches the newest trend point; an empty history is a valid non-error populated-vs-empty signal (only a missing user sets `errorMessage`); `load` refreshes on a second call so a just-completed session shows on return; US-M02: `load` computes the `analytics` legibility layer from real history (pillar balance, chain position, personal bests), a free user's `isPremium` is `false` while still getting the basic analytics, and a premium user's `isPremium` unlocks the `deep` layer. `ProgressTabSnapshotTests` in the same file renders the free, premium, and empty-state surfaces to PNGs, and the committed copies under `artifacts/reports/us-m02/` (`progress-m02-free.png`, `progress-m02-premium.png`, `progress-m02-empty.png`) are what it produces, so a layout or copy change on the Progress tab shows up as an image diff rather than as a stale render cited as evidence. Like `PerSideSwapEvidenceTests` it resolves its destination through the shared `EvidenceOutput` helper - a per-run temporary directory unless `REPTODAY_WRITE_EVIDENCE=1` (which regenerates those committed baselines in place) or `REPTODAY_EVIDENCE_DIR=<root>` redirects it - so a plain `test` leaves the worktree clean and the renders do not depend on the machine they were first captured on. `EvidenceOutput` is also what pins one meaning for that override: it names a *root*, and the story folder is appended to it exactly as it is under `artifacts/reports/`, so one redirected run cannot scatter two suites' renders into two different shapes. Each render is cropped back to the height the content actually laid out to, so a committed baseline is content rather than a blank tail, and a surface that outgrew the height it was hosted in throws before any PNG is encoded, so that run fails loudly without replacing a committed baseline with a truncated image; a surface that hosted but never drew is caught the same way, by `EvidenceOutput` sampling the captured pixels *before* it writes them - a single flat colour is a surface that never drew, at any canvas size, which an encoded-byte floor is not, since PNG size scales with area - so neither known-bad capture can reach disk. The same class also reads the hosted view's live accessibility tree - through the shared `HostedSurface` / `AccessibilityTree` seam, which is what lets the surface settle before it is read - to gate that the calendar resolves "today" and its completed-day dots against the *injected* clock and calendar rather than the wall clock (the expected spoken date is derived from an identically configured formatter, so a non-English simulator does not fail it), and that the weekday-initial header is hidden from VoiceOver instead of being read as seven loose letters - the day cells naming their own weekday ("Wednesday, Jul 8, 2026, today, session completed") so hiding the header costs a listener nothing |
| Progress analytics (`ProgressAnalyticsTests`) | US-M02: `ProgressAnalytics.from` over a synthetic library - pillar balance splits by worked-exercise count and sums to 1, skipped/set-less exercises are excluded (no over-claiming); chain position reports the frontier tier and `hasNextTier` (single-tier tops out at no next tier, the frontier is the highest-order worked tier even when a lower tier was worked more recently, an untrained foundation is "not started", and it is phase-aware - a Discipline user sitting at the top discipline tier of a chain that continues into a locked Strength tier counts only reachable tiers and reports no next tier "in reach", while a Strength user on the same history sees the extra tier counted and reachable); personal bests (sessions, longest session, best week, total minutes, best single-set reps/hold naming the movement); the deep layer omits untrained patterns, sums weekly volume oldest-first, and counts the difficulty mix ignoring unrated sessions; empty history yields empty analytics; determinism |
| Active session player (`ActiveSessionViewModelTests`) | US-K01: `ActiveSessionViewModel` flattens the workout's blocks into one ordered step list (block context + 1-based overall position), an empty session is immediately complete; `completeSet` advances within an exercise then to the next then to completion, recording each set's prescribed target (reps for rep-based, seconds for holds) and a no-op once complete; `skipExercise` advances without recording sets, flags the exercise skipped (finishing the session on the last one), and discards partial sets recorded before a mid-exercise skip so a skipped exercise never carries completed sets into the eventual log; `loggedExercises` builds the eventual-log rows with completed sets, pillar/pattern, and skip flags; elapsed time is measured from `start()` against the injected clock (zero before start), `start` is idempotent (never resets), and elapsed freezes at completion; progress reflects completed sets over total; US-K02: `completeSet` opens a rest for the prescription's `restSeconds` (none after the final set, none when `restSeconds == 0`, dropped on skip), `restRemaining(asOf:)` counts down against the injected clock, `completeRestIfElapsed` fires the injected `SessionCuePlayer` spy exactly once at zero (never before, never per-tick, never while paused - `skipRest` ends it with no cue), `extendRest` adds time to remaining and total, and `pauseRest`/`resumeRest` freeze and reschedule the remainder across backgrounding; US-K03: `swapCurrentExercise` replaces the current slot with the engine's `.substituted` result in place (rest preserved, at whatever set count the engine sized it with, the current round kept rather than reset to 1 so a mid-circuit swap never re-offers or double-counts already-completed peer stations - US-CC02 OPT1, `testSwapMidCircuitDoesNotReplayCompletedPeerStations` - with a substitute carrying *fewer* sets clamped down to its last round rather than stranded past its end), discards partial sets recorded for the replaced movement, hands the swap step a snapshot of the *current* lineup (a swapped-in movement is in it, the replaced one gone) that keeps the session's block structure, so the swap step can tell a set-adjustable training block from a warm-up or cooldown bookend, flips `noSwapAlternative` and keeps the slot on `.noAlternative` (cleared on advance), is a no-op with no engine wired (`canSwap == false`), hands the engine the policy the session was generated against (US-O02) so the substitute keeps the session's Start Seed, and end-to-end over the real engine + bundled library yields a same-pillar/same-pattern peer with a valid target; the player's target copy states a per-side prescription as per side in both the visual and the VoiceOver string ("3 × 0:30 per side", "3 sets of 30 second holds per side"), driven off `isPerSide` so it cannot drift from what the engine charges, and omits it for a bilateral movement; the spoken target agrees with every count it names - a single-set slot (the shape of every warm-up and cooldown, so the first and last thing a VoiceOver user hears in any session) reads "1 set of a 45 second hold" / "1 set of 12 reps" rather than "1 sets of 45 second holds", a one-rep set reads "1 rep", and pluralisation composes with the per-side suffix ("1 set of a 30 second hold per side"); the Ready-screen preview shares the player's two formatters instead of carrying a second copy, so its VoiceOver label speaks the nouns rather than reading the "×" glyph aloud; US-K04: `snapshot` captures the exact play state; every meaningful change persists to the injected `ActiveSessionStore` (awaited via `persistenceTask`); `init(state:)` restores the exact position, completed work, and lineup, preserves the elapsed-time origin across a relaunch (idempotent `start` never resets it), resumes a running rest from its absolute deadline and a paused rest from its frozen remainder, and clamps an out-of-range index; a swap persists the new lineup; completion clears the store; no store wired is a no-op; US-L01: completing the session writes a `WorkoutLog` through the injected completion service (fire-and-forget via `completionTask`) with the requested-vs-completed minutes (20 requested / 14 completed) and the shape/focus/`wasReturn` facts copied off the played `Workout`, the rating left `nil`; the logged completed duration (and the summary's) is capped at `requestedMinutes` so a long backgrounded stretch on the wall-clock session clock never inflates it; no completion service wired is a no-op; the `summary` is `nil` before completion and reflects the finished session's duration, set count, and covered pillars/patterns after, excluding skipped movements; US-L02: `rate` records the perceived-difficulty rating and persists it (via the completion service) onto the *same* log id the initial completion write used, re-tapping overwrites (newest last), rating before completion or without a service is a no-op / memory-only, and an un-rated session persists nothing extra - the rating write does not re-run the full completion recording; US-O03: the Hold Timer is offered only on a timed movement (`holdSecondsPerSide` is `nil` for a rep-based one, so its manual flow is untouched) and never on top of a rest, `startHold` counts the prescribed seconds down against the injected clock, `completeHoldIfElapsed` fires the `SessionCuePlayer` spy exactly once at zero (never early, never per-tick) and records the set with the prescribed duration before handing off to the rest; a per-side hold runs **one leg per side** - the first leg cues the switch, parks on side 2 and records nothing (half a set is not a set), and only the last leg records the single set, resetting the side for the next; `cancelHold` records nothing, fires no cue, and leaves the same side ready to re-start; a hold's set can also be banked **by hand without ever starting the timer** - recorded at its prescribed target, with no cue, resting like any other completed set - including from the second side of a per-side set, where it still records exactly one set, and **while a leg is actually running**, where it takes the countdown down with it, fires no cue, records exactly one set, advances the set counter, resets the side and opens the rest exactly as the timer reaching zero does (the abandoned deadline banks nothing on any later tick) - the non-destructive way out of a hold, beside a `Stop hold` that records nothing and a `Skip` that discards the exercise's banked sets; a hold started while a swap is in flight never survives onto the substitute (the leg is refused, and the ticker's later check records no phantom set and fires no cue); a paused hold freezes its remainder, never auto-completes however long the app is away, and reschedules from that remainder on resume *within* the session; skip and swap both drop a running hold and its side without a cue; a hold leg **never survives the player being torn down** - a snapshot taken mid-leg carries no countdown at all, so a relaunch eight seconds later comes back idle with `Start hold` offered, and neither the on-appear resume nor the ticker banks a set or fires a cue; the same holds for a leg frozen on the way out and resumed a day later, the door a deadline check could not close, while the *side* of a half-done per-side set is preserved so the user is never sent back to side 1; and a rest that already ran out while the app was gone is dropped on restore rather than firing its completion cue on the first tick for a rest nobody is in - the same class of defect, fixed once now that both timers share one `Countdown`; the hidden clock is still measured, so the completion summary still reports the total; US-CC01: a rep-based **training** set (strength / dedicated-primal block) now counts down on an auto-advancing **work window** rather than a manual "Complete set" - `currentStepAutoAdvances` is true only for a rep-based set in a `.strength`/`.primal` block (a hold keeps the US-O03 Hold Timer, a warm-up/cooldown bookend keeps its manual path even when rep-based, so the gate is the block category), `workWindowSecondsPerSet` equals `SessionAssembly.workSecondsPerSet(of:)` for the slot on screen - the single source of truth US-CC08 calibrates, including per-side rep pricing read from planning's own function - the window auto-starts hands-free on `start()` (no Start-hold-style tap), `completeWorkWindowIfElapsed` records the set completed (prescribed reps as performed, **never** skipped) and flows into the existing US-K02 rest at zero while firing the `SessionCuePlayer` cue exactly once (never before zero, idempotent per-tick after), **Done** (`finishWorkWindowEarly`) advances early recording completed with **no** cue and no skip, `pauseWorkWindow`/`resumeWorkWindow` freeze and reschedule the remainder so backgrounding never auto-advances a window the user is away from, the rest ending (`completeRestIfElapsed`/`skipRest`) and a no-rest set advance both auto-start the next set's window so a whole strength block completes hands-free with every exercise logged its full set count and none skipped, and a swap drops the running window without a cue then re-arms one sized to the substitute; the live production player is driven end-to-end in `PerSideSwapEvidenceTests` (a real short work window auto-advances into the rest hands-free, and the real **Done** control activated the way VoiceOver's double-tap does advances a still-running long window early). Accepted consequence recorded per US-CC09: the `>=80%` `session_completed` telemetry reads a touch more generously than the old tap-gated model, since an auto-advanced set logs completed without a tap; the terminal-event contract is otherwise unchanged; US-CC02: a training block (`.strength` / dedicated `.primal`, gated by `SessionAssembly.isCircuit`) is played as a **circuit of rounds** - `nextPosition(fromIndex:set:)` rotates one set of each station per round (A, B, C, A, B, C, ...) instead of grinding all sets of one exercise first, `currentRound`/`circuitRoundCount` surface "Round N of M" read off the block (a station's r-th set *is* round r, so `currentSet` doubles as the round; linear warm-up/cooldown bookends carry neither and flow linearly), the two engine gaps pace the rotation through the US-K02 rest overlay (a short `SessionAssembly.transitionSeconds` between stations, the uniform per-block round rest between rounds), and aggregate logging is unchanged (`completedSets` keyed by prescription id - each station still logs its full set count by the end whatever the rotation order); a **skip** inside a circuit removes the exercise from every remaining round (the rotation passes over a skipped station) so it logs not-done with no completed sets while the other stations still finish their rounds - the only aggregate-correct reading of "skip this exercise" once the walk rotates (swap-across-rounds reconciliation stays US-CC07) - and resume restores round + station from the existing `currentStepIndex`/`currentSet` with no snapshot-schema change: `testStrengthBlockRotatesABCAcrossRoundsWithRoundLabels` (the PRD validation - A,B,C×3 in order with "Round 1/2/3 of 3" and three sets logged per exercise), `testCompleteSetRotatesToTheNextStationInTheRound`, `testCompletingARoundWrapsToTheNextRound`, `testBookendsAreNotCircuitsOnlyTrainingBlocks`, `testCircuitCrossesTransitionBetweenStationsAndRoundRestBetweenRounds`, `testSkipInsideCircuitRemovesExerciseFromRemainingRounds` (plus the rewritten walk/rest/swap/resume tests that now assert the rotated order); the running player is driven hands-free through the rotation and the round label out of process in `ContinuousCircuitUITests.testStrengthBlockRotatesThroughRoundsHandsFree`; US-CC05: a **warm-up/cooldown bookend** timed hold auto-starts hands-free (never a Start-hold tap) while a strength/primal **training** hold keeps the US-O03 manual path - `currentStepIsBookendHold` is the block-category gate (the complement of US-CC01's work-window gate), `autoStartHoldIfNeeded` is driven from every reveal transition (`start`, no-rest set/exercise advance, rest end, skip) and re-armed after a swap, and a per-side bookend runs **side 1 -> a brief "Switch sides" beat -> side 2** with no tap, the beat a shared-`Countdown` rest carrying an in-memory `RestContext == .switchSides` that names itself and fires **no** completion cue (so exactly one cue per hold leg - two legs, two cues, none for the beat - `testPerSideBookendRunsSide1ThenSwitchSidesThenSide2HandsFree`), a training hold parks on side 2 for a tap with no beat (`testTrainingHoldStaysManualNotAutoStarted`), two bookend stretches flow station-to-station hands-free (`testBookendHoldsFlowStationToStationHandsFree`), a resumed bookend re-opens idle and auto-starts the owed side fresh with no phantom set or cue on the restored/expired leg (`testResumedBookendHoldReopensIdleAndAutoStartsFresh`), and **Stop hold** on a bookend is a real escape that is never silently re-armed (`testStopHoldOnABookendDoesNotImmediatelyReArm`), plus the gate/auto-start basics (`testBookendHoldGateDistinguishesBookendFromTrainingHold`, `testBookendHoldAutoStartsHandsFreeOnStart`); the running warm-up is driven hands-free out of process in `ContinuousCircuitUITests.testWarmupBookendHoldsRunHandsFreeIncludingSwitchSides` (auto-start and no Start-hold control hard-asserted, the per-side Switch-sides legs exercised when the generated warm-up leads with a per-side stretch); US-CC06: an explicit **user Pause** escape hatch freezes whichever countdown is live - the work window, a hold leg, or a rest - and, with it, the audio/haptic completion-cue timing (a paused `Countdown` never reports `hasElapsed`, so `completeWorkWindowIfElapsed`/`completeRestIfElapsed`/`completeHoldIfElapsed` are all no-ops while paused), resuming from the *exact* remainder: `testUserPauseFreezesTheWorkWindowAndResumesFromTheExactRemainder`, `testUserPausedWorkWindowNeverAutoAdvancesOrFiresTheCue` (the cue-freeze AC - no set banked, no cue, then one cue at the true remainder after resume), `testUserPauseFreezesARestAndItsCue`, `testUserPauseFreezesAHoldLeg`; the user pause is a **foreground** state distinct from the scene-phase background pause, so `resumeFromForeground` (the new foregrounding path) never un-freezes a held user pause while it still resumes a plain background pause (`testForegroundingDoesNotUnfreezeAUserPause`, `testForegroundingStillResumesAPlainBackgroundPause`); Pause is offered only when a countdown is live (`canUserPause`/`isCountingDown` - not on an idle Start-hold training step, not on the completion screen: `testPauseIsUnavailableWithNoLiveCountdown`); and any advancing escape hatch taken while paused (Done, Skip, Skip rest, Swap, Stop hold) clears the stale pause so the flow never lands running-but-showing-Resume (`testAdvancingWhilePausedClearsTheUserPause`); the shipped Pause control is driven end-to-end through the running player out of process in `ContinuousCircuitUITests.testPauseFreezesTheWorkWindowWithoutBackgroundingAndResumeContinues` (a finger reaches the work-window Pause, the announced "N seconds remaining" holds across real wall-clock time with the app foregrounded, the control flips to Resume in place with no separate mode, and Resume returns to a pausable window); US-CC07: a mid-circuit **skip** or **swap** holds for **all remaining rounds** and keeps the block uniform (Option A, honest late-entrant - keeps the US-K03 discard-on-swap contract). Skip is already aggregate-correct from US-CC02 (one Step per station, `hasSet(_:inRound:)` passes over a skipped station in every round); US-CC07 locks it with `testSkipInsideCircuitRemovesExerciseFromAllRemainingRounds` (skip B in round 1 -> rotation cycles only A,C through rounds 2 and 3, A and C log 3 completed sets each, B logs zero and `skipped == true`). Swap likewise holds across every remaining round because replacing the one slot carries the substitute through all later rounds while keeping the block's uniform *prescribed* round count M (`ExerciseSwap` already keeps the slot's set count, so the engine needs no change): `testSwapAppliesToAllRemainingRoundsAndKeepsBlockStructurallyUniform` (swap B in round 2 -> every strength station still prescribed M=3, the substitute plays rounds 2 and 3 and logs exactly 2 completed sets as an honest late entrant that never fabricates its pre-swap round, peers A and C each still complete 3 and no station over-logs), and `testSwapNoAlternativeMidCircuitKeepsExerciseInAllRemainingRounds` (a `.noAlternative` swap leaves the exercise in place and it still completes all its rounds). The documented residual: a mid-circuit swap's substitute completes only the rounds it was in the session for (`currentRound...M`), so it can end one or more completed rounds short of its peers - that is the honest log (the user did fewer sets of the substitute), and the block stays structurally even (US-CC03). The running player is driven through a mid-circuit skip and on through the later rounds out of process in `ContinuousCircuitUITests.testMidCircuitSkipRemovesExerciseFromLaterRoundsHandsFree` (a finger presses Skip, then the skipped movement is asserted absent from every later round it rotates through) |
| No manual-mode guard (`NoManualModeGuardTests`) | US-CC06: a build-time guard in the routinely-run **unit** bundle (so it runs on the `-scheme RepToday test` gate, not only the on-demand XCUITest scheme) that scans the active-session player sources (`ActiveSessionViewModel.swift`, `ActiveSessionView.swift`) and fails the run if a `manualMode`/`ManualMode` identifier appears - turning "the continuous-circuit follow-along flow is the only player, with no separate manual mode" (US-CC06 AC, PRD Non-Goals) from a review-time promise into a build fact. It keys on the camel/Pascal-cased identifier a real toggle would introduce, not the English word "manual" (which appears legitimately in comments for the *manual* Start-hold / Complete-set paths), and asserts the scanned files exist so it cannot pass vacuously if the layout moves. Same enforcement pattern as `UITestLaunchGuardTests` |
| Session lifecycle telemetry (`ActiveSessionTelemetryTests`) | US-T10: the three session-lifecycle emissions on `ActiveSessionViewModel`, asserted through the recording `MockAnalyticsService` with an injected mutable clock. `session_started` fires once from `start()` carrying `requested_minutes`, and is idempotent under a second `start()` (its `startedAt == nil` guard). `session_completed` fires from the single dismiss choke point `recordSessionEnd()` (which `ActiveSessionView.close()` calls) **only when the session actually completed**, carrying `requested_minutes`, `completed_minutes` (whole minutes off the injected clock - 5 min asserted from a 300 s span), `was_return`, and `perceived_difficulty` as the enum raw value (`too_hard`) - present because the rating is given (via `rate()`) *before* the emission, which is exactly why it fires at dismissal and not at `finish()`; an unrated completion omits the `perceived_difficulty` key rather than sending null. **US-T10 refinement (captain-approved fix of a review finding):** a mid-session dismiss that leaves the session **resumable** is a *pause*, not an abandonment, so the player emits **no** terminal event then (asserted: a paused dismiss records only `session_started`) - because otherwise a later resume + completion would double-count the same physical session as both abandoned and completed against the >=80% metric. `session_abandoned` therefore fires on **true give-up** of a resumable session, owned by `ReadyViewModel` (`ReadyViewModelTests`): an explicit **Discard** (`discardResumableSession()`) or an **overwrite** by a fresh Start (`abandonResumableSessionForOverwrite()`), each emitting off the persisted `ActiveSessionState` snapshot - `completed_minutes` read from the snapshot's `exercisedMinutes` (captured at the last active moment, not recomputed as wall-clock at give-up, so a paused-then-discarded session never folds in idle time) and the coarse `abandon_point` derived from the block the snapshot's current step sits in - `warmup`, `mainWork`, `cooldown` - never free text. The net invariant is proved across the full resume path in both directions: start -> pause(dismiss) -> resume -> finish emits exactly one `session_started` and one `session_completed` and never an abandon (two players sharing one sink); start -> pause -> Discard emits one `session_started` and one `session_abandoned` and never a completed (player + Ready Screen sharing one sink). Also covered: a resumed player's `start()` never re-emits `session_started` (its restored `startedAt` holds the guard), a repeated-dismiss idempotency check on completion, the snapshot capturing `exercisedMinutes`, an ordinary fresh Start with nothing paused emitting no abandonment, and the sink being optional (a player with none wired runs the whole lifecycle without trapping). Consent is enforced inside the live sink, so no call-site opt-out check is present or tested here. **Accepted, recorded blind spot (US-T10 decision):** because `session_completed` fires at dismissal to capture the rating, a session the user finishes but force-quits from the celebration screen *before* tapping Done emits no `session_completed` (its `WorkoutLog` still persists) - a rare, known gap in the >=80% session-completion-rate metric |
| Session summary (`SessionSummaryTests`) | US-L01: `SessionSummary.from` counts coverage from non-skipped exercises with a recorded set, de-duplicated in canonical pillar/pattern order; a skipped or set-less movement contributes nothing (never over-claims); the coverage phrase reads as natural language across one/two/three pillars, the focus line lists movement areas; deterministic |
| Session completion recorder (`SessionCompletionServiceTests`) | US-L01: `recordCompletedSession` writes the log verbatim (completed - not requested - duration), refreshes the forgiving Consistency Score onto the user over the full persisted history (counting the new log exactly once), advances cold-start, retires it and clears the policy contract at the handoff threshold (US-G04), keeps the contract while cold-start is active, is frozen once already retired, and reads the freshest persisted user for the read-modify-write so a learned Default Duration the on-open reprogram wrote after the Ready-Screen snapshot is preserved, never clobbered; US-L02: `recordPerceivedDifficulty` updates the log in place by its stable id (an upsert, not a second record) and does not re-advance the cold-start counter the completion write already moved; US-O02: driven through the real completion-then-rating ordering, a `too_hard` on the retiring session - which can only be collected after the handoff already recorded the band - still eases `bandFloorAtHandoff` by a tier without moving `sessionsLogged`, while rating a session outside the cold-start week leaves the retired band untouched; US-N03: a wired HealthKit spy proves the completed session is mirrored to Health once against the freshest persisted user, a throwing HealthKit service never disrupts the log write / Consistency refresh (completion never throws), and no-service-wired records nothing to Health |
| Weekly-active telemetry (`WeekActiveEventTests`) | US-T11: the `week_active` emission on `SessionCompletionService.recordCompletedSession`, asserted through the recording `MockAnalyticsService` with an injected clock and the pinned `AppState.cohortCalendar` (Gregorian, Sunday-start, Pacific) - the one calendar decision this story owns, deliberately not `Calendar.current`. The Validation Test: the first completed session of week W emits exactly one `week_active`, a second session in the same W emits none (per week, not per session), and the first session of week W+1 emits one more. Also covered: the event carries **no** properties (per schema); the emit-once guarantee is keyed off a **persisted** set of already-emitted week-starts (`UserDefaults` key `telemetry.weekActiveEmittedWeeks`) so it survives a fresh service instance (a relaunch) rather than a per-instance guard; and the emission count over a multi-week history equals the number of distinct active week-starts `ProgressAnalytics`'s rollup buckets over that same history in the same calendar (the AC-2 reuse, guarding the named failure indicator of a divergent week definition). Week bucketing keys off `ConsistencyScore.startOfWeek(log.completedAt, ...)` - the same function and vantage the rollup uses, not a fresh calendar calculation. Consent is enforced inside the live sink, so no call-site opt-out check is present or tested here; the persisted key is written to `.standard`, so this suite and `CoreDataServicesTests.testLiveContainerCompletionRecorderWritesDurableLog` restore it via `restoreAfterTest` rather than leaking it into a later test |
| HealthKit workout mapping (`HealthKitWorkoutSampleTests`) | US-N03: the pure `HealthKitWorkoutSample.from` - start derived as `completedAt - durationMinutes` (zero-duration floored to one minute), the external UUID is the log id (idempotency key), energy is `MET x weightKg x hours` with MET the mean of worked movements (skipped / set-less movements excluded so a passed-on movement never inflates it), unresolved MET falls back to the calisthenics default and a missing weight to the default weight, energy is always positive, a mobility focus maps to `.flexibility` and blend/strength to `.functionalStrengthTraining`, and it is deterministic |
| StoreKit subscription service (`StoreKitSubscriptionServiceTests`) | US-N04: `StoreKitSubscriptionService` over a stub `StoreKitFacade` - the entitlement mapping (no entitlement is free, an active one is premium carrying its `expiresAt`, the trial window sets `trialEndsAt`, the latest-expiring of several wins, `refreshEntitlements` matches `currentSubscription`); `premiumPlans` maps + orders products (monthly first, price/trial/priceLine) and throws `productsUnavailable` on none; a purchase unlocks premium (PRD validation) while a cancel keeps the current state (free stays free, premium stays premium), a pending purchase reports `.pending` without granting, and a purchase error propagates; `purchasePremium` buys the monthly plan; restore re-grants an owned entitlement (PRD validation), is free with nothing owned, and a sync error propagates |
| Paywall view model (`PaywallViewModelTests`) | US-N04: `PaywallViewModel` over a configurable stub service - `load` populates plans and surfaces a gentle non-blocking message when none are available or the load fails; a successful purchase/restore flips `didUnlockPremium` and clears the in-flight marker; a silent user-cancel and a nothing-owned restore do not unlock (the cancel shows no message, the empty restore a gentle one); a pending Ask-to-Buy purchase surfaces a gentle waiting message without unlocking; a purchase/restore failure surfaces a gentle message and never unlocks. US-T12: the monetization funnel emissions, asserted through the recording `MockAnalyticsService` in process (no network, FR-13). `paywall_shown` fires exactly once across two `load()` calls (the one-shot `hasEmittedPaywallShown` guard survives a `.task` re-run) carrying `entry_point = progress_upsell` (the `EntryPoint` enum's raw value). At `purchase(_:)`'s resolved branch, keyed off a real `.premium` grant: a direct paid grant (`trialEndsAt == nil`) emits exactly one `subscribe` carrying `plan == plan.id` and no `trial_started`; a trial-bearing grant (`trialEndsAt != nil`) emits one property-less `trial_started` and no `subscribe`. A cancel (resolves to `.free`), a `.pending` (Ask-to-Buy), a thrown failure, and a `restore()` each emit **neither** `subscribe` nor `trial_started`. What these prove is the view model's emission decisions given a resolved purchase outcome, not that a live App Store purchase resolves - the live StoreKit legs verify only on device / the `.storekit` configuration. Consent is enforced inside the live sink, so no call-site opt-out check is present or tested here. Not covered here (documented residual, US-T12 note): a trial *converting* to paid does not emit `subscribe`, because the StoreKit transaction listener that observes conversions is not wired to the analytics seam |
| App shell (`AppStateTests`, `ServiceContainerTests`) | `AppState` defaults, UserDefaults persistence, invalid-tab fallback; US-T05: the anonymous per-install identity, run against an injected clock and a per-test `UserDefaults` suite (never `.standard`), covering the PRD's validation test end to end - a first launch mints a parseable UUIDv4 `installId`, stamps `firstLaunchAt`/`lastActiveAt`, and persists all of it; a relaunch preserves `installId` and `firstLaunchAt` and moves only `lastActiveAt`; the reinstall leg is a **wiped suite**, which is exactly what an uninstall does to `UserDefaults`, and must yield a *new* id and a *new* `firstLaunchAt` - so an id surviving it fails here as the Keychain-persistence symptom the story's failure indicator names, though what a wiped suite cannot model, and what no test here claims, is a backup restore: `Library/Preferences` rides iCloud/iTunes backups and device-to-device transfer, so the honest guarantee is "dies with the app absent a restore" - and the device run described next does not change that, because it exercised the delete-and-reinstall path and no restore path at all. Those same three stages were **also** run outside XCTest entirely, against a real Debug install on an iPhone 16 Simulator with every value read out of the app's own `Library/Preferences` plist rather than from a suite, together with three probes no unit test can make: a binary-safe search of the whole simulated device after the uninstall (keychain database and its WAL included) that found no trace of the deleted install's id **and** found the live install's id in exactly one file, its own plist, so the absence is a search that would have caught a survivor; a background-and-resume that left `lastActiveAt` untouched where only a cold launch moves it; and the upgrade path driven on a real install rather than a wiped suite. Transcript, observed values, and limits: `artifacts/reports/US-T05/validation.md` - which is a **point-in-time record that nothing re-runs** - a hand-driven device transcript, not a CI-able test - so the `AppStateTests` cases in this row are the only part of any of it a later change can fail against, exactly as with the US-T03 transcript below. Back in the suite: a half-written identity (an `installId` with neither a `firstLaunchAt` nor the unknown marker) re-mints whole rather than cohorting an install against a week it did not install in, and the mirror case is gated separately because the rule is narrower than "re-mint the pair": a missing or empty `installId` sitting beside a stored `firstLaunchAt` re-mints only the id, keeps that origin, derives `installWeek` from it, reports `isFirstLaunch == false`, and writes no unknown marker - the marker records that no usable origin exists, and one does; `installWeek` equals `ConsistencyScore.startOfWeek(firstLaunchAt, calendar)` - the reused helper, not a second derivation - and sits strictly *behind* a mid-week launch, so no precise install time is recoverable from the cohort; and two installs on one device differ, plus a direct inequality against `identifierForVendor`, which is the closest a unit test gets to proving the id is not device-derived. The **upgrade** path is gated too, because it is the one the K4 denominator actually depends on at launch: an install that is already onboarded but carries no `installId` mints an id, reports `isFirstLaunch == false`, and leaves `firstLaunchAt`/`installWeek` `nil` rather than fabricating the upgrade date as an origin - and a relaunch of that state keeps the same id and the same `nil`, which only holds because the unknown is persisted as its own `AppState.firstLaunchUnknown` marker (without it the pair rule would re-mint the id every launch, since an upgraded install and a corrupt half-write have the same shape on disk). One residual is asserted nowhere because it is accepted rather than fixed: someone who installed an earlier build and never finished onboarding still reads as a fresh install, since `isOnboarded` is the discriminator. `previousActiveAt` is covered as `nil` on a first launch and as the previous launch's stamp on a relaunch - the value `init` would otherwise destroy when it overwrites `lastActiveAt`; nothing reads it in production, and it is **not** what the day-7/day-30 return windows are computed from (those are `firstLaunchAt`). The cohort calendar is covered twice: a Saturday-evening Pacific install - already Sunday in UTC - buckets through the production default to the Sunday its Pacific week began and differs from what a device-shaped (UTC) calendar produces, and `AppState.cohortCalendar`'s identifier, `firstWeekday`, and time zone are asserted directly, so a drift off the pin fails rather than passing quietly on whatever locale the runner happens to have. Not covered, stated rather than implied: nothing in production yet reads `isFirstLaunch`, `installWeek`, or `previousActiveAt` (US-T07 emits `app_install` from the first two), and no test puts `installId` on a wire, because there is no transport until US-T04. US-T06's consent flag is covered here too: it persists and survives a relaunch in both directions; a launch-argument **string** (`NO`/`0`/`false`, and their opposites) closes and opens the gate, which is the shape `object(forKey:) as? Bool` would miss; turning it off leaves `installId` untouched; the gate re-reads rather than captures; and - the only configuration that can actually fail - a gate built from an `AppState` on a non-default suite follows **that** store while `.standard` is pinned to the opposite answer, so a writer and a reader bound to different stores fails here rather than silently emitting for a user who opted out. Those last two are the exception to this row's "never `.standard`": they have to write the test host's own defaults, because `.standard` is the store production's gate reads, and a suite of their own would only prove the two halves agree about a container nothing ships with. Putting the key back is therefore part of the test rather than politeness - a leftover leaks into every case that runs after it in the same process - and it goes through one shared helper, `DefaultsSnapshot.swift`'s `restoreAfterTest(_:in:)`, registered as a teardown block so it survives an early return and cannot drift away from the write it undoes. `LiveAnalyticsServiceTests` uses the same helper for the same reason. Mock `ServiceContainer` resolves every service - all thirteen, the US-T02 `analyticsService` included, with the property count asserted against `Mirror` first so adding a service without wiring it here fails rather than passing unnoticed (among them the session-policy service, whose `currentPolicy` is the default and `dueTriggers` empty, and the US-L01 session-completion recorder) |
| Per-side copy + swap, rendered end-to-end (`PerSideSwapEvidenceTests`) | Runs the real pipeline over the bundled library and asserts on the surfaces the user actually reads: every slot of a generated 30-min session that the timing model charges for both sides is labelled "per side" in *both* the visual target and the VoiceOver string, and no bilateral slot ever is (both directions, so the suffix cannot become unconditional); the session still lands within +/-1 minute of the request; the in-session swap driven through `ActiveSessionViewModel` at a per-side slot returns a same-pillar/same-pattern peer carrying its own honest per-side label with the set counter kept at its first set, and a sweep swaps **every** slot of the session in turn - warm-up, Movement Practice, Strength and cooldown - asserting each substitution keeps the session inside +/-1 minute, respects the Zero-Equipment Floor, and never moves the set count of a 1-set structural bookend. It also renders the production `ActiveSessionView` and `ReadyView` to PNGs (player at a per-side hold and at a per-side rep movement, the same hold at accessibility Dynamic Type where the target line's `fixedSize` is what keeps "per side" from being the part that truncates, the player before and after a real swap taken from the view model's own snapshot, and the Ready Screen's whole lineup) so the copy can be reviewed where the user meets it rather than only as a string assertion. The same pipeline gates the *spoken* target end-to-end: every slot of the generated session is checked that its set count, rep noun and hold noun each agree with their own number and that the per-side suffix survives the pluralisation, with the single-set warm-up and cooldown bookends called out explicitly because they are the first and last thing a VoiceOver user hears in any session; and the Ready Screen's row labels are read back off the live SwiftUI accessibility tree of the hosted production view (rather than re-derived from the formatter) - hosting and reading both through the shared `HostedSurface` / `AccessibilityTree` seam, so this suite gets the same settling policy as every other one rather than its own copy of it - asserting each row speaks exactly the sentence the player speaks and that nothing on the screen reads the "×" glyph aloud - the regression that a second copy of the formatter caused. Both bookends are also rendered to PNGs alongside the transcript. US-O03 is gated on the same surfaces, now read against a **training** hold (a `.strength`/`.primal` timed hold via `firstTrainingHoldIndex`, since US-CC05 makes warm-up/cooldown bookend holds auto-start and so drop the Start-hold control the manual path still owns): the hosted player at a timed movement leads with "Start hold" while keeping the manual completion beside Skip as a quiet secondary and, once running, speaks its remaining seconds, offers "Stop hold", **still offers that manual completion** - without it the running state's only visible exits are one that records nothing and one that discards the sets already banked - and keeps the rest of the row's slots in place; a per-side hold names the side on the button, on the set tracker and in both spoken forms, on side 1 and on the between-legs side 2 a relaunch restores to; the rest overlay keeps its own countdown; and **no** surface carries an `"Elapsed time …"` label any more - the regression this story exists to prevent, asserted off the live accessibility tree rather than assumed from the source. `testHoldTimerRunsDownAndHandsOffToRestInTheLivePlayer` closes the loop in real time: it activates the production "Start hold" control the way VoiceOver's double-tap does, lets the wall clock run out on a short-authored hold, and asserts the live player has recorded the set and moved to the rest overlay - exercising the `Timer` publisher, the deadline arithmetic and the auto-advance as one system rather than as pieces, pumping the runloop until that hand-off is observable rather than for a fixed budget, so it fails on behaviour rather than on timing under load. `testClosingThePlayerMidHoldLeavesNoLegOnDisk` drives the other live path the same way - it activates the real "Start hold" control and then the real close control, and asserts what actually reached the `ActiveSessionStore`: **no countdown at all**, only the side of a half-done per-side set, so the resume that snapshot feeds comes back offering `Start hold` with nothing banked. A running leg is only reachable by driving the real control, since nothing restores one - which is exactly the property under test. `testBankingASetByHandMidLegEndsTheLegAndOpensTheRestInTheLivePlayer` drives the escape the running state has to offer, live and through the real controls: it activates "Start hold", then activates the manual completion mid-countdown, and asserts the countdown is gone, the rest overlay is up, and the set counter has advanced exactly one set. `testTheSessionTotalIsHiddenDuringPlayAndRevealedOnTheCompletionSummary` gates the story's other half on one hosted surface: the player is read for an elapsed clock (and for the minutes worked so far in any form) before the real "Finish session" control is activated, and the summary that replaces it is read for the same session's total - so a player with no clock and a summary with no total, which is a session whose minutes simply vanished, fails here rather than passing as two half-truths. `testManualHoldCompletionGrowsRatherThanClippingAtAccessibilityType` measures the narrowest column in the three-slot secondary row off the live accessibility tree at both default and `accessibility3` type, so the longest title in the row ("Finish exercise") is gated as growing the row rather than clipping inside a fixed 60pt height. The player at a hold (idle, running, idle at accessibility Dynamic Type, per-side side 1 and side 2), the rest overlay, and the completion summary that finally reports the total are rendered to PNGs **by these tests themselves**, and the committed copies under `artifacts/reports/us-o03/` are what they produce. The renders and their assertions run on every test run; where the bytes land is opt-in. A plain `xcodebuild ... test` writes them to a per-run temporary directory, so running the suite never leaves the worktree dirty; `xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepToday -destination 'platform=iOS Simulator,name=iPhone 16' test -only-testing:RepTodayTests/PerSideSwapEvidenceTests REPTODAY_WRITE_EVIDENCE=1` regenerates the committed images in place, which is how a drifted screen is meant to become a diff. It is a build setting rather than a shell variable because the test bundle runs inside the Simulator and does not inherit the invoking shell's environment; the scheme forwards `$(REPTODAY_WRITE_EVIDENCE)` and `$(REPTODAY_EVIDENCE_DIR)` (empty by default) into the test process. This suite resolves none of that itself: it goes through the shared `EvidenceOutput` helper, which pins `REPTODAY_EVIDENCE_DIR` as a *root* with the story folder appended - never the final directory - so a redirected run keeps the same `<root>/<story>/<file>` shape as the committed baselines. `EvidenceOutput` is also the only path to disk for a render, and it checks that the surface actually drew *before* writing, so a capture that came back near-blank fails the run loudly instead of quietly replacing a committed baseline with itself. The running-hold render pins the countdown to a specific remaining second and lets the arc finish animating before capturing, so its bytes stop depending on where the runloop pump happened to land. These stay live surfaces rather than golden images, though: any render still showing the exercise demo catches that demo's continuous pulse at whatever phase it is in, so the renders are review material and the assertions beside them are the gate. The per-side and swap renders belonging to the earlier story write to `artifacts/reports/per-side-swap/`, kept apart from the files the US-O03 acceptance notes point a reviewer at. The hosted Ready Screen is generated on a `MockWorkoutEngine` pinned to the same fixed `asOf` as the fixed-clock `generate()` baseline (US-T02 made that clock injectable), so the two paths agree deterministically instead of drifting a staleness tier apart as the wall clock moves off the fixture date. |
| Analytics seam (`AnalyticsServiceTests`) | US-T02: the funnel-instrumentation seam (`AnalyticsEvent`, `AnalyticsServiceProtocol`, `MockAnalyticsService`) - three events of different property-bag shapes recorded through the mock come back in order with names, timestamps, and bags intact and a fresh mock is empty; every `AnalyticsValue` scalar case (`Int`/`Double`/`String`/`Bool`, including a whole-numbered double that must not decode back as an int) round-trips through `Codable` via its `type` discriminator, as does a whole `AnalyticsEvent`; and `AnalyticsEventName` has exactly the 13 in-app cases with their snake_case wire raw values (the two web-side events absent), each raw value round-tripping - so a missing or extra event name fails loudly. The seam itself is then exercised end-to-end rather than only the model: one anonymous install's whole funnel (all 13 events in emission order, carrying the schema's own property names, on timestamps pinned to a fixed install moment so the run is byte-reproducible) is recorded through `ServiceContainer.mock()`'s `analyticsService`, each emission written exactly as an emission call site must write it - `await services.analyticsService.record(event)`, no `try` - so the deliberately non-throwing signature is used as a call site rather than asserted about; the mock hands every event back in order, and the recorded array is then encoded to the model's own JSON and decoded back losslessly, asserting the snake_case event names are what serialise and that neither web-side event appears. **Only the mock records, and that is deliberate**: until US-T04 the production container wired the discarding no-op, so driving the whole funnel through it cost nothing; it now wires `LiveAnalyticsService` against the configured deployment, and recording 13 events through it from a unit test would put 13 POSTs on the wire, which FR-13 forbids - so the production wiring is asserted **by type**, without emitting, and the request body is `LiveAnalyticsServiceTests`' coverage. That type assertion follows the build configuration, since the default sink does: Debug must resolve `LiveAnalyticsService` (an endpoint is configured), Release must resolve `NoOpAnalyticsService` (none is). This is the one test that deliberately does **not** pass `live(...)`'s `analyticsService` override, because the wiring the default resolves to is what it exists to assert - every other production-container test substitutes an inert sink instead. The funnel fixture's timestamps are fixed offsets from a pinned install moment rather than read from the clock, so the container test is deterministic run to run; it writes no files and touches no network |
| Telemetry sink (`convex/`, live-deployment validation - no XCTest) | US-T03: the Convex backend carries **no Swift tests**, because it is not Swift and the story changes no app code - its gate is the Convex CLI's own `tsc` typecheck, which runs on every `npx convex dev`/`convex deploy` and blocks the push (it caught the real defect here: the HTTP action handing an untrusted `String` to the mutation's 13-literal `v.union`), plus `npm run typecheck` standalone against the committed `convex/_generated/`, which is committed precisely so a fresh clone can run it with no deployment configured. Behaviour is gated by the PRD's Validation Test run live against a dev deployment (`hcho22/reptoday-telemetry`, `courteous-dogfish-560`) rather than by unit tests, and the transcript is recorded in `artifacts/reports/US-T03/validation.md`: a valid `session_completed` POST - the widest real property bag in the pre-registered schema, carrying `generation_ms` inside `props` so the pass-through is exercised too - returns `204`, and `npx convex data events` reads back exactly one row with `props` intact and `serverTs` populated *and distinct from* `clientTs`, which is what proves the stamp is server-side rather than echoed. The three rejections (an unknown name - `landing_page_view`, a real schema event that is deliberately outside the app's 13, so it probes the union rather than a typo; >4096 UTF-8 bytes - probed with 2100 `é`, which is only 2111 UTF-16 code units and so would have slipped a `String.length` check; >32 keys) each return `400`, and the table is then **read back** holding only the valid events, so the non-insert is proven by the table's contents rather than inferred from the error. The read is an authenticated query round-trip against persisted rows, so a false local success is ruled out the same way the US-T01 spike ruled it out. Three further rejections and one status split landed **after** that run, over two review rounds, and were then re-validated in a second full live pass against the same deployment at commit `42c1310` - 14 rejected POSTs, each answering `400` with the action's own sentence rather than Convex internals, and `npx convex data events` afterwards holding only the valid rows, so the non-insert is again proven by the table's contents rather than inferred: a missing or non-string `installId` and a `clientTs` that is not an actual finite JSON number are now refused at the HTTP boundary with `400` instead of being coerced into the literal string `"undefined"`, `NaN`, or a silently reinterpreted `"0x1f"` - the two columns K4 and K1 are computed from, where a row that looks valid but is junk is worse than no row; a deployment or database failure now answers `500` with no detail echoed rather than sharing the rejections' `400`, so a sink outage and a caller's fault stop looking alike to the only audience that reads the status (US-T04's client swallows it); and a `props` field name the Convex SDK's own serializer refuses (a leading `$`, a non-ASCII or control character, or over 1024 characters) now answers `400` rather than `500` - a re-classification, not a new rule, because `ctx.runMutation` already refused that input inside the action's own isolate and merely threw a plain `Error` instead of a `ConvexError`, landing it on the sink's side of the split where a hostile client could manufacture what looked like an outage on the only signal the PMF test has. The action runs the SDK's `convexToJson` up front rather than restating its rule, so that check cannot drift from what Convex enforces and still inspects field names only, leaving the forbidden per-key and per-type checks of `props` absent. The transcript is marked in place wherever its recorded response *bodies* no longer match the current code - the unknown-name rejection fires at the boundary now, so it no longer carries Convex's `ArgumentValidationError` text, and the two bag rejections are `ConvexError`s now, so they no longer carry the `Uncaught Error:` prefix and stack frame - while every status code, row count, and shape assertion it records still holds; nothing in it was rewritten to predict output that was never observed. A third review round then made `logEvent` an `internalMutation` so the HTTP action is the sink's only entry point rather than merely the client's - it shipped as a public `mutation`, which Convex exposes on the deployment's own `.convex.cloud/api/mutation` endpoint under the same slug a shipped binary's `.convex.site` URL carries, and a direct call there **was observed live** inserting a row with an empty `installId` and a `clientTs` of `0`, skipping every boundary check above - the empty `installId` being the half those checks refuse, while a `clientTs` of `0` is a finite JSON number that lands through the route too, since the check is presence and kind by design and range is left to a query written later. The typecheck is a real gate for that change rather than a formality, since it refuses `api.events.logEvent` and accepts only `internal.events.logEvent`, and `convex/_generated/` needs no regeneration because it derives both surfaces from the module's own types - and it is now backed by a live re-probe at commit `99a11d0`, where the identical direct call answers `Could not find public function for 'events:logEvent'` and inserts nothing. The transport status is `200` both before and after the fix, because Convex reports a missing public function in the response payload rather than as an HTTP status, so the evidence is that payload plus the absent row rather than a status code. The junk row the pre-fix probe inserted was deleted afterwards, by a scratch mutation written outside the repository and removed again by redeploying the branch's own code, so the repo still contains exactly one mutation and the table read back at `99a11d0` holds only valid rows - each with a non-empty `installId`, a non-zero `clientTs`, and a `serverTs` distinct from its `clientTs`. What no live run covers, and the transcript states rather than glosses: two later rounds touched the action's error classifier only, both landing after the run above and gated by `npm run typecheck` alone. The fourth deleted an unreachable branch - an `Error` merely *named* `ConvexError`, which neither of the SDK's two cross-isolate reconstruction shapes can produce and which would have echoed a raw internal message into a `400` body if it ever had fired. The fifth then hardened the single branch that deletion left load-bearing: it recognised a `ConvexError` by `instanceof`, which the SDK itself avoids for this purpose - the class stamps `Symbol.for("ConvexError")` on every instance and `registration_impl` tests for that symbol rather than for class identity, because identity does not survive the two copies the package ships under `dist/cjs` and `dist/esm`, and the syscall layer reconstructs from a relative import while `http.ts` imports the `convex/values` specifier. Under a duplicated resolution every property-bag `400` would have quietly become a `500`, the false outage the split exists to prevent. The action tests for the symbol now and still echoes only the error's structured `data`; that is a strict superset of what `instanceof` matched, so the `ConvexError` rejections and the generic `500` fall-through are both unchanged. As US-T03 shipped, none of that re-ran: there was no automated behavioural test over `convex/` and this repo has no CI, so the transcript was a point-in-time record and `tsc` cannot catch a regression in a validation rule. That was recorded as an unchecked US-T04 acceptance criterion rather than closed here - a JS test runner and its dependency being new scope for a story whose deliverable is one table, one mutation, one route - and **US-T04 closed it**; see the `convex/http.test.ts` row below, which also carries the two size caps US-T03 left off and the two places where probing them live corrected this row's own earlier predictions. Not covered here, by design: nothing in this row touches the iOS build, and the request body the client POSTs is `LiveAnalyticsServiceTests`' coverage |
| Live telemetry transport (`LiveAnalyticsServiceTests`) | US-T04: `LiveAnalyticsService` driven through a `URLProtocol` stub that intercepts every request **in process**, so nothing in this suite performs a network call (FR-13) - the stub reads `httpBodyStream` rather than `httpBody`, because `URLSession` moves the body into the stream before a `URLProtocol` sees the request and reading the wrong one would make every body assertion silently vacuous. Asserted: the configured URL, `POST`, the JSON content type, and a body of exactly `{name, installId, clientTs, props}` with the bag **flattened to plain scalars**; an event with no properties still sending `props: {}`; `clientTs` crossing as a **bare JSON number**, checked against the raw bytes rather than a parsed value, since a re-tagging encoder would still produce valid JSON and this is the assertion most likely to regress silently - and, beside it, that the model's own `Codable` form is *not* the wire form (it stays tagged, per US-T02, names `timestampMs`, and carries no identifier), so a later "simplification" that POSTs `JSONEncoder().encode(event)` fails here rather than at the sink; every one of the 13 names reaching the wire as its snake_case raw value. Fire-and-forget: `record(_:)` returns while the stub holds the request open for 1.5 s, a transport error and a non-2xx answer are both swallowed without poisoning the service (the next event still sends). The opt-out gate: a closed gate produces **no request at all**, and it is re-read per emission rather than captured at construction. US-T06 then connected it to the persisted flag and added two tests here for that - the real `AppState.isAnalyticsEnabled(in:)` reader over a real `UserDefaults` suite stopping and resuming emission on the same service instance (so "honoured without a restart" is exercised rather than argued), and, in Debug, `ServiceContainer.live(...)`'s own built service reporting its gate follows `UserDefaults.standard` (the parameter's default, and where production's `AppState` lives), so a future edit that drops `isEnabled:` from the `configured(...)` call fails here. A third asserts the container uses the gate it is **handed** rather than resolving one of its own, driven from a non-default suite so a container that quietly fell back to `.standard` fails rather than passes. Configuration: a missing, blank, non-string, schemeless, `http`, or unparseable endpoint resolves to `nil` (inert, never fatal), a usable origin resolves to `<origin>/logEvent`, a bundle without the key configures nothing, and the app bundle's own `Info.plist` value produces a service that POSTs to that deployment's `/logEvent` - with the expected host read back out of the plist rather than hard-coded, so moving the deployment does not break the test. The per-configuration endpoint split is read out of the **running app bundle** rather than asserted in prose: Debug must carry a usable HTTPS origin that `configured(...)` builds a service from, and Release must carry none and resolve to `nil` through the *same* `endpoint(fromOrigin:)` check, so an unconfigured Release build is inert by the one existing route rather than a second one. Each half is compiled only into the configuration that can observe it. **Only the Debug half runs**, and the reason is recorded rather than glossed: `ENABLE_TESTABILITY` is set on the Debug configuration alone, so `@testable import RepToday` does not resolve under `-configuration Release` and there is no Release test run to execute the other branch (a pre-existing project property; de-optimising the shipping binary to change it would be a worse trade than the gap). The Release half is verified instead at the artifact - `plutil -extract RepTodayAnalyticsEndpoint` on the built `Release-iphonesimulator/RepToday.app/Info.plist` reads the empty string, against the Debug bundle's `https://courteous-dogfish-560.convex.site` - and the empty string's rejection is already unit-covered above; the transcript is in `artifacts/reports/US-T04/validation.md`. Not covered: nothing asserts what a *production* endpoint would be, because none has been chosen; and nothing here reaches the **out-of-process** half of FR-13 - `RepTodayUITests` launches the real app, which resolves the live transport from the Debug endpoint and no parameter in this process can reach. US-T06 closed that half with the launch-argument-overridable `analyticsEnabled` flag; it is `TelemetryOptOutUITests`' coverage, in the row below. **One more thing nothing here covers, and it is structural rather than a missing case:** consent is enforced inside `LiveAnalyticsService.record(_:)`, so the privacy invariant is a property of *that implementation* rather than of `AnalyticsServiceProtocol` - a second emitting sink would have to re-implement the gate from memory, and neither the type system nor any test in this suite would catch its absence. The call-site rule that puts it there is deliberate and stays: a call site never checks consent itself, because two gates that can disagree is worse than one gate in a known place, so this is not a hint to start checking consent at call sites. The follow-up is written as a condition to check rather than a caveat, answerable by looking instead of by judgement - **count the emitting conformers of `AnalyticsServiceProtocol`**: today there is exactly one, since `NoOpAnalyticsService` discards, `MockAnalyticsService` records in memory, and the Debug-only probe harness intercepts in process, so none of them can reach the network. If that count is ever greater than one while the gate still lives inside a concrete sink - a batching queue, a retry wrapper, a secondary transport - the decorator is already overdue: the gate moves to a `ConsentGatedAnalyticsService` applied once in `ServiceContainer.live`, so it holds for any sink rather than for one. A trigger that depends on somebody noticing a defect is the weaker kind - the lesson of the launch-guard trigger in the row below, whose first version required a bypass to be noticed by the same process that created it - which is why this one is a count. The item is filed in firstmate's work queue too, the same treatment the launch-guard wrapper got |
| Telemetry consent surfaces (`TelemetryConsentSurfaceTests`) | US-T06: the Settings toggle and the onboarding disclosure read off the **production views**, hosted in a real key window through the shared `HostedSurface` / `AccessibilityTree` seam, so what is asserted is what is drawn and what VoiceOver speaks rather than a re-description of the layout. Settings: the toggle is present and labelled "Share anonymous usage data", the explanatory sentence is on screen, the privacy-policy link is there, and the copy is checked *as copy* - it must name what the data is for and what it is **not** attached to, and must not contain loss framing, which is the difference between an opt-out and a dark pattern. That sentence is also asserted to be announced **exactly once**, counted over labels *and* hints together rather than pinned to whichever view renders it (it is the section's footer today): it was once both a row of its own and the toggle's `accessibilityHint`, so VoiceOver read the whole paragraph twice in succession and these tests asserted each half rather than catching the pair. The toggle's hint is asserted to be short and *not* the body copy, which is what a hint is for. The switch's state is read off the live tree in **both** directions, so a hard-coded on cannot pass. The toggle is then **activated the way VoiceOver's double-tap activates it**, and the persisted flag is read back: that is what proves the control is wired to the gate rather than to a local copy of it - a switch bound to nothing would look identical in a screenshot. The same activation is used for the one thing this story must **not** do: after opting out, the stored `installId` is asserted byte-identical and still there on the next construction, so US-T07's re-minted-identity state stays unreachable by construction rather than by intent. Onboarding: the disclosure is asserted on the **first** step (where it has to be, since US-T07's `app_install` fires at app entry), that it says where the opt-out lives, and that the privacy-policy link is beside it. Plus a guard on `LegalLinks.privacyPolicy` - the one constant the paywall's Review 3.1.2 link and both consent surfaces now share - that it is the hosted Rep Today policy at `reptoday.app/privacy` (the earlier "still a placeholder" guard was retired deliberately, per its own instruction, when the real hosted policy at `gtm/03-site/privacy.html` replaced the `example.com` placeholder); the same case pins `LegalLinks.termsOfUse` to Apple's standard EULA host, which is a real destination rather than a placeholder and so is asserted as itself. **Not covered here:** whether anything is emitted; that is `LiveAnalyticsServiceTests` in process and `TelemetryOptOutUITests` out of it |
| Opt-out gate, out of process (`TelemetryOptOutUITests`, `RepTodayUITests`) | US-T06: the gate proved in the **real app**, launched out of process, which is the only place `LiveAnalyticsService`'s `isEnabled` closure is the sole gate - the test process never builds the container, so US-T04's `analyticsService` parameter cannot reach there. **The honest problem this row exists to record:** no emission call site existed when this suite was written (US-T07 has since landed the first, at app entry; US-T08 through US-T12 added the rest, so all 13 now emit), so "zero network calls with telemetry off" was trivially true for reasons unrelated to the flag, and a test asserting it would pass with the flag deleted. Two things were therefore added to the app target, both inside `#if DEBUG` **and** inert without a launch argument (`Services/Analytics/TelemetryUITestHarness.swift`): an `app_install` probe emitted from `RepTodayApp.init()` - the exact name and place US-T07's first real site now uses - and a counting `URLProtocol` swapped into the transport's session, whose running count is rendered in a HUD this suite reads. A Release build compiles none of it; an ordinary Debug run without the argument is unchanged. Asserted: with `-AppState.analyticsEnabled NO` the app dispatches **nothing**; with the flag at its default the identical launch dispatches at least once (the positive control, without which the zero proves nothing); the launch argument and the user-facing toggle are the **same** flag, checked in both directions - launched opted out the switch reads off, launched at the default it reads on, so neither answer can be a hard-coded one; and toggling off and back on through the real Settings screen changes what the next emission does, in one launch with no relaunch. A fifth case, `testRendersTheConsentSurfacesAndTheGateChangingTheCount`, is a test as well as a photographer: it drives the same off-and-on through Settings and asserts the emit button adds one, the opted-out emit adds none, and re-enabling adds one again, filing six renders with the result bundle as it goes. **US-T09 changed the three consent-on legs from absolute counts to deltas.** Those launches run gate-on, onboarded, and reach the Ready Screen, and US-T09's `ready_screen_shown` is the first *real* emission site to fire there - dispatching alongside the probe's stand-in `app_install` and moving the launch baseline off the hard-coded `1` these tests pinned (`waitForAttemptCount(1)` began seeing `2`). Whether it lands is state-dependent across runs (only when a persisted user exists to generate against), so the three legs now sample a **settled baseline** and assert deltas around each explicit emit-button tap (`ready_screen_shown` fires once per open, so post-settle the count is stable except those taps) - captain-approved, preserving every proof while dropping the now-false "no real emission fires here" assumption US-T10 through US-T12 broke again. Its non-vacuity was checked by sabotage **three times**, once per thing the suite protects, and the transcript records each (`artifacts/reports/US-T06/validation.md`). The gate: replacing the `isEnabled:` argument in `ServiceContainer.live(...)` with `isEnabled: { true }` makes `testTelemetryOffMeansTheAppDispatchesNothing`, `testTogglingTelemetryOffAndOnIsHonouredWithoutARestart` and `testRendersTheConsentSurfacesAndTheGateChangingTheCount` - three of the suite's five - fail with the messages they were written to print, and restoring it makes all three pass. Re-running that check reads the **argument**, not a fixed spelling of it: it was `isEnabled: AppState.analyticsGate` when the sabotage was first run, and the review round that bound the gate to `AppState`'s own store made it `isEnabled: analyticsGate` (the factory parameter), which is the spelling the transcript now carries. The launch guard was then sabotage-checked twice on its own account, and **the first of those failed**: re-injecting the original unguarded launch *passed* a teardown-only version of the check, because the render leg launches twice and the second, guarded launch overwrote the first's arguments - the net could not see the bug shape it was written for, which is why the check now runs on entry to every launch as well. Re-injecting the same defect after that fails loudly. The third check found the reverse: adding the empty-arguments cell made **all five** tests fail before any launch, because `XCUIApplication` is rebuilt per test case while the app process is not, so a leftover from the previous case made `app.state` report running before this one had launched anything - the condition was wrong, not the tests, and `setUp` now terminates that leftover. **What it does not prove, stated rather than implied:** the count is taken at the `URLProtocol` boundary - the same boundary `LiveAnalyticsServiceTests` treats as authoritative - so an attempt means the transport built and dispatched a request, not that bytes reached Convex; a run that let real bytes out would break FR-13. The bytes-reach-Convex half is the live validation transcript, which does not re-run. It also says nothing about a Release build. `OnboardingImperialUITests` now passes `-AppState.analyticsEnabled NO` on every launch too, so the pre-existing suite is off the wire before US-T07 makes that matter rather than after. **Which launches carry which guard, stated rather than assumed, because "every suite passes the flag" would be an over-claim:** there are **two** guards, not one, and every launch in this suite carries at least one. Consent pinned off with `-AppState.analyticsEnabled NO` is the first, passed on the legs where being opted out is the assertion and on the render leg's *first* launch, the onboarding disclosure, which also wants no HUD standing in the picture; that leg then terminates and relaunches under the probe for the live-toggle renders, so it carries one guard and then the other rather than one throughout. **Interception** is the second and the stronger: on the opted-in legs the probe harness has replaced the transport's `URLSession` with the in-process counting `URLProtocol`, which is exactly what lets those launches run with the gate genuinely open - the positive control and the runtime-toggle test need it open, so "consent is always off" would be the wrong invariant to reach for. "One of the two, never neither" holds **by construction**, not by every future author remembering: the postures are a `TelemetryPosture` enum with no case meaning neither, and every launch goes through `TestApp` (`RepTodayUITests/TestApp.swift`), the bundle's sole `XCUIApplication`, whose only launch entry point takes a posture by value - a test cannot hold a raw launchable app, so a launch carrying no guard is not expressible through the API the suite sees. `XCUIApplication` is a framework type any file can still construct, so this is "**cannot ship a bypass**" rather than "cannot type one": the enforcement is `UITestLaunchGuardTests` in the **unit** bundle (`RepTodayTests/UITestLaunchGuardTests.swift`), which scans every `RepTodayUITests` source and fails the default `-scheme RepToday test` run if `XCUIApplication(` is constructed anywhere but `TestApp.swift`, with a positive control that the wrapper itself still constructs one so the scan cannot pass vacuously. It lives in the unit bundle because the routinely-run gate is the unit scheme rather than the on-demand `RepTodayUITests` one (and CI runs the unit scheme, build-checking the XCUITests without running them), so the guard runs on the pass everyone actually performs. That build guard **replaced** the earlier runtime detector (`assertNobodyLaunchedBehindOurBack`), now retired: it *detected* a bypass rather than *preventing* one, and had an empirically-verified blind spot - a standalone bare `app.launch()` used as a test's only launch went uncaught while the same launch followed by a helper launch was caught, root cause never diagnosed. A source scan has no launch orderings to miss: whether or when a stray application launches is irrelevant, only that its construction is present in the source, which the scan reads directly. Sabotage-checked in the ordering the detector missed: injecting a standalone bare `let a = XCUIApplication(); a.launch()` as a UI-test's only launch fails the guard by name, and removing it makes the guard pass again - which is why this is reported closed rather than a repeat of the detector, once reported closed on the strength of testing only one of the two orderings. The wrapper was the recorded **precondition on US-T07** (where `app_install` hangs off app entry so every launch reaches it and a bypass would cost live rows rather than nothing); it has now landed, so that queue dependency is cleared |
| Telemetry sink boundary (`convex/http.test.ts`, `npm test` - not XCTest) | US-T04: 51 `convex-test` assertions driving the real `POST /logEvent` route in process, in the edge runtime Convex uses, against an in-memory database - no deployment, no network, no `.env.local`. This is what US-T03 explicitly did not have. **Every rejection asserts the table is empty afterwards**, because a `400` that inserted anyway would be worse than no check, so the proof is the row count rather than the status. Covered: the happy path (`204`, one row, `serverTs` stamped server-side, `props` stored as-is, an omitted bag defaulting to `{}`, two events being two rows); all 13 names accepted and unknown/near-miss/empty/non-string/missing names refused; `installId` present, a string, non-empty, and inside the new 64-byte bound - at the bound, one byte over, and multi-byte UTF-8 that would pass a UTF-16 length check - with the bound asserted to be length-only (`"not-a-uuid"` still lands); `clientTs` an actual finite JSON number, including `1e400` arriving off the wire as `Infinity`, and a `clientTs` of `0` deliberately landing, since the check is presence and kind by design; the new 64 KiB request-size cap, both that it does **not** pre-empt the `props` cap's more specific message for a 5 KiB bag and that a bag past 64 KiB is answered by the body cap instead - still the caller's `400`, just the less specific of the two, so the ordering is asserted rather than claimed absolute; both `props` caps at their exact boundaries, the byte one counted in UTF-8; a `props` that is a non-null non-object refused, beside a `props` of `null` deliberately answering `204` with an empty bag, which is the one coercion the action makes on purpose (nothing is counted from `props`, unlike the `installId`/`clientTs` columns K4 and K1 are counted from); the `convexToJson` field-name classification answering `400` rather than `500`; the `4xx`/`5xx` split, including that the `5xx` body is exactly `{"error":"internal error"}` and echoes no internal detail; and `logEvent` still being an `internalMutation`. Two of those cannot be provoked by input and say so in place: the `500` branch is reached by handing `convexTest` a module map with `logEvent` swapped for one that throws a plain `Error`, and `logEvent` being internal is asserted on the registration object's own `isInternal` flag rather than on `api.events.logEvent`, which can say nothing because the generated `api` is `anyApi` - a proxy that answers every property access whether the function exists or not. **Not covered, and stated rather than implied:** the residual where a caller fault only a *write-time* Convex rule catches (`props` nested past the value-depth limit) still answers `500` - `convex-test`'s in-memory database does not enforce that limit, so it is unreachable here and is documented in `convex/README.md` instead. `.github/workflows/ci.yml` runs `npm test` on every PR into `main`, and it runs locally on demand too |
| Telemetry sink abuse guard (`convex/http.test.ts` + `LiveAnalyticsServiceTests`) | US-T14: the shared-secret gate and per-caller rate limiting, extending the `npm test` boundary suite to 88 `convex-test` assertions (same in-process, no-deployment, no-network setup) and adding iOS transport coverage. `post(...)` carries the valid secret by default and a `beforeEach` sets `ANALYTICS_SHARED_SECRET` on `process.env`, so every pre-US-T14 assertion keeps its meaning. **Secret:** a correct secret under the ceiling inserts one row; a missing header and a wrong secret are both `401` with the table empty afterwards; a right-length-but-wrong secret still fails (exercising the constant-time compare); the deployment having **no** secret configured fails closed with `500` and no insert (deliberately the sink's side of the `4xx`/`5xx` split, since an unguarded sink taking writes is the failure the story prevents); and a wrong-secret flood past the ceiling consumes **no** rate-limit budget, proving the secret is checked first. **Rate limiting:** the per-install ceiling inserts exactly `MAX_EVENTS_PER_INSTALL_PER_WINDOW` rows and `429`s the rest with no further insert; the source-IP backstop trips on many *distinct* installs from one `x-forwarded-for`, and a proxy chain sharing a first hop lands in one bucket rather than multiplying budget; the shared secret is **not** a rate-limit key (a second install with the identical secret is not throttled - proving keying on it would not collapse the user base into one bucket); and the window is anchored to server time. **Counter store:** driving `internal.rateLimit.checkAndBump` directly with a controlled `nowMs` proves a counter resets when its window rolls while the stale row **survives** (the hot path no longer reaps - cleanup moved off the request path to the `internal.rateLimit.reclaimExpired` cron in `convex/crons.ts` to end the cross-install OCC contention a per-request sweep caused), that `checkAndBump` touches only the caller's own current-window bucket and never another install's or a prior window's row, and that `reclaimExpired` deletes only expired windows in a bounded per-tick batch (a seeded overflow beyond `RECLAIM_BATCH` drains over several ticks, and a future-window row is never touched) - so the `rateLimits` helper table stays ephemeral and bounded, never a second evidence surface. **iOS transport (`LiveAnalyticsServiceTests`):** `record(_:)` attaches the secret on `X-RepToday-Analytics-Secret` (read off the intercepted request, in-process, FR-13); the secret follows the same inert-when-empty rule as the endpoint (`secret(fromValue:)` rejects missing/empty/whitespace/non-string; the Debug app bundle carries a resolvable secret and `configured(...)` builds a live service, the Release half asserted `nil` and verified against the built artifact like the endpoint's); and the Debug configured-from-bundle service sends the app bundle's own secret. Live three-step probe against dev deployment `courteous-dogfish-560` in `artifacts/reports/US-T14/validation.md` (point-in-time, not re-running); the in-process suite is the gate that re-runs. `.github/workflows/ci.yml` runs `npm test` and the `-scheme RepToday` unit suite on every PR into `main`, and both run locally on demand too |
| US-T13 reconciliation tabulator (`convex/reconcile/tabulate.test.ts`, `npm test` - not XCTest) | US-T13 **harness** (not the story): 22 `vitest` assertions over the pure funnel tabulator in `tools/reconcile/tabulate.js`, which turns recorded `events` rows into the per-install funnel table a reconciliation diffs against the coder's log. Pure - no network, no Convex, no wall clock (every `Date` gets an explicit ms) - so it is exercised against fixture arrays alone, the way `AppEntryTelemetry` is. The test lives under `convex/` so the existing `npm test` include and `npm run typecheck` reach it (and, via the import, typecheck the `// @ts-check` JS source), while the source stays under `tools/` so it is never bundled into the deployed sink. Covered: the funnel's event set/order asserted **equal to `EVENT_NAMES` in `convex/events.ts`** (itself pinned to `gtm/06-channels/event-metric-schema.md`), so the order is the wire contract not a guess; a clean full funnel with zero anomalies; a missing prerequisite (`onboarding_completed` without `onboarding_started`); a duplicate of a once-per-install event (`app_install` x2) while a recurring event (`ready_screen_shown` x2) is **not** flagged; the terminal-balance guarantee - started+completed only and a legit two-session completed/abandoned mix produce no flag, while both terminals for one physical session flag `EXCESS_TERMINAL` and a started-with-no-terminal flags the informational `UNTERMINATED_SESSION`; an opt-out install with zero rows shown all-absent with no defect flag; out-of-order timestamps (a later milestone before an earlier one, and any event before `app_install`); `week_active` twice in the same Pacific cohort week vs. in different weeks; client-clock skew; an unknown event name captured aside; and the CSV/markdown/anomaly formatters. The read path (`convex/reconcile.ts`'s internal-only `eventsForInstalls`) and the runner (`tools/reconcile/run.mjs`) are not unit-tested - the query is a trivial scan-and-filter and the runner is thin I/O around the pure core; both are exercised by the offline `--rows` smoke path. See `tools/reconcile/README.md` for the standing caveat that this is the harness, with US-T13's report still pending the moderated cohort, named coder, and frozen rubric |
| App-entry emission decision (`AppInstallEventTests`) | US-T07: `AppEntryTelemetry.eventsForLaunch(...)`, the pure clock-injected unit `RepTodayApp.init()` calls to decide the three app-entry events, driven through the **real** `AppState` -> planner composition against a throwaway `UserDefaults` suite (never a sink, so FR-13 is not even in reach), so a regression in how `AppState` resolves the three launch states surfaces here as well as in `AppStateTests`. The PRD's Validation Test end to end: a first launch emits exactly one `app_install` carrying the coarse `install_week`, then launches at days 3/8/12/20/33/35/40 show `day7_return` firing once on the first open inside days 7-13 and never again, `day30_return` once inside days 30-36, and no return event outside either window. Window boundaries are asserted inclusive at both ends (day 6 out / day 7 in, day 13 in but deduped, day 14 out; day 30 in, day 36 in but deduped) and a single late open still emitting once (the window is "any open during", not "the open on day 7"). `app_install` never fires on a relaunch (`isFirstLaunch == false`). `install_week` is asserted a coarse `yyyy-MM-dd` week-start ("2026-08-02" for a Tuesday install, no time-of-day, no ISO `T`) and to round-trip through `AnalyticsWireBody`'s scalar flattening as a plain JSON string, not the tagged in-process form - the US-T05 wire-encoding question closed. The two US-T05 decisions have explicit cases: an **unknown-origin** install (onboarded, no stored id, `firstLaunchAt == nil`) emits **nothing** across a first open and both would-be return windows; a **re-minted identity** (onboarded, surviving origin, empty stored id) emits **no** `app_install` but still windows a `day7_return` off the surviving origin. **Not covered here:** that the emission site never re-checks consent (it calls `record(_:)` unconditionally - the gate is `LiveAnalyticsServiceTests`/`TelemetryOptOutUITests`' subject); and that the real emission does not disturb the out-of-process probe count - it fires only on `isFirstLaunch`, and every `TelemetryOptOutUITests`/`OnboardingImperialUITests` launch is `onboarded: YES` (so `isFirstLaunch == false`), which both suites re-run green to confirm |
| Account deletion teardown (`AccountDeletionServiceTests`, `CoreDataServicesTests`) | US-AD02/US-AD03: the App Store 5.1.1(v) deletion path. `CoreDataServicesTests` covers the two new wholesale bulk deletes against the in-memory CoreData stack - `deleteAllLogs()` clears the whole (single-user) history and is a no-op on an empty store, `SessionPolicyStore.deleteAll()` clears every stored policy and is a no-op when none is stored. `AccountDeletionServiceTests` drives `AccountDeletionService.deleteAccount(appState:)` over the in-memory doubles plus a real `AppState` on an isolated `UserDefaults` suite: the signed-in case clears user, logs, policy, active session, and the Keychain identifier and resets routing (`isOnboarded` -> false, tab -> home); the local-UUID case (no Apple credential) clears and routes without throwing; a second run is an idempotent no-op; a no-current-user install still clears logs and the Keychain and routes; and an unreadable/corrupt user still has its policy and active session cleared (the wholesale, user-id-free deletes). The mock/test-double bulk deletes (`deleteAllLogs()`, `deleteAll()`, `clearAll()`) are asserted here too |
| Account deletion presentation (`AccountDeletionViewModelTests`) | US-AD04/US-AD05: `AccountDeletionViewModel`, teardown stubbed so only the presentation logic is under test. A stored, authorized Apple credential reads as used and presents the confirmation; confirming an Apple account arms the post-deletion Apple-ID guidance (`AppState.showAppleSignOutGuidance`); the local-UUID account neither reads as Apple nor arms guidance; a stored identifier Apple reports as `.notFound` shows no guidance (the `getCredentialState` seam is stubbed via `AppleCredentialStateProviding`); a thrown teardown surfaces the failure alert without routing to onboarding and does not arm the Apple-ID guidance; and a blocked-teardown double-tap runs the destructive teardown exactly once (the re-entrancy guard) |
| Account deletion flow, out of process (`AccountDeletionUITests`, `RepTodayUITests`) | US-AD01/US-AD03/US-AD04: the real app, driven by a finger through the shared `TestApp` wrapper (`.optedOutWithNoProbe`). Profile -> Settings -> the destructive **Delete Account** row -> the confirmation alert (destructive confirm distinct from Cancel) -> confirm routes back to onboarding's first screen with the main tabs gone; and cancelling leaves the user on Settings with onboarding not shown. Run on demand (compile-checked in CI, run locally when the touch path is in question) |
| Session-generation latency benchmark (`SessionGenerationBenchmarkTests`) | Re-runnable performance harness for the "<100ms on-device generation" claim (FR-12), the substantiation behind `gtm/01-research/session-generation-benchmark-2026-08-10.md`. Times the exact call the claim measures - `MockWorkoutEngine.generateWorkout(...)` -> `SessionAssembly.assemble` (the `generation_ms` await in `ReadyViewModel.generate()`) - **cold** (fresh engine per sample, first generation) and **warm** (repeated on a warmed engine) across 5/10/15/20/30/45/60 min, computing p50/p95/max (nearest-rank) with deterministic input. Pure measurement, no tight timing gate (the device gate lives in the record, not here); asserts only that every lane ran and produced finite non-negative p95s. Prints a `BENCH_BEGIN..BENCH_END` markdown table to the test log. Re-targetable to a real device unchanged via `-destination` - the Simulator run is an optimistic proxy, not the authoritative iPhone XS / iOS 17 number. **Opt-in: skipped on default and CI runs; run by passing `RUN_SESSION_BENCHMARK=1` to xcodebuild** (forwarded into the Simulator process via the `RepToday` scheme env, like `REPTODAY_WRITE_EVIDENCE`) |
| Vocabulary & decision record (US-006) | Documentation-only story - the strength-primary vocabulary closer - so it adds **no** test code: the only `.swift` change is the `Pillar` enum doc note in `Models/Enums.swift` (plus a corrected `ExerciseLibraryTests.testPillarCoverage` comment; no assertion changed), alongside a new root `CONTEXT.md` glossary and `docs/adr/0001-strength-primary-sessions.md`. Its verification is the PRD Validation Test rather than XCTest/vitest: grepping the code and docs the branch touches for `co-primary`/`mobility lean` finds no surviving **current-tense** assertion of the old co-primary/mobility-lean behavior (`gtm/**` marketing copy is out of scope and intentionally untouched). The strength-primary engine behavior this story documents is already covered by the US-001..US-005 rows above (pillar balance, session assembly, Return / Re-entry Ramp), so the unit suite stays green unchanged |
| Strength-primary regression guard (`StrengthPrimaryRegressionTests`) | US-007 (the cross-cutting guard for US-001..US-005, **no new engine behavior**): one consolidated suite that sweeps every requested length `5/10/15/20/30/45/60` across all three generation regimes - **steady-state** (fresh no-history desk worker, the originally-reported all-stretches profile), **cold-start** (US-004 First-Week window, difficulty-capped), and **Return** (US-005 post-gap comeback) - through the real `SessionAssembly.assemble` pipeline, and pins that every generated session (1) contains a real `.strength` **training** block (not just the mobility warm-up) and is never **mobility-led** - the training block owning the most planned time is strength, computed with the engine's own `workSecondsPerSet` work model - with single-focus 5-10 min carrying a strength focus and **no** `.mobility` training block at all; (2) holds the strength-primary share bands - a single-focus session's training time is ~100% strength (>= 0.8 floor), a blend's **strength family** (strength + any dedicated primal block, US-E02) is strength-dominant inside the US-002/US-003-validated `0.7...0.88` band with a genuine mobility accessory present and strength leading primal, and a dedicated check that the representative blends (20, 30 min) sit near the ~0.75-0.80 target; and (3) stays deterministic (every swept regime/length is byte-identical run to run at a fixed injected `asOf`) and a pure function of the injected `asOf` (re-anchoring "now" plus a same-relative-gap history to a date a year later produces the identical session - no wall-clock read) - reaffirming, not replacing or loosening, `DeterministicSessionPolicyServiceTests` and `SessionAssemblyTests`. A precondition test asserts the three regimes are genuinely distinct engine paths (cold-start override active; Return detected end-to-end and stamped `wasReturn`, not suppressed) so the sweep cannot silently collapse to the steady state |
| Movement Practice removal (US-M01, `SessionAssemblyTests` + `StrengthPrimaryRegressionTests`) | US-M01 (Movement Practice removal; freed minutes -> strength): removes the mobility accessory training block and its split machinery (`PillarBalance`/`PillarWeights`) and reallocates its minutes to strength - so several pre-US-M01 rows above are **superseded** where they mention the Movement Practice block, a mobility accessory, the `0.7...0.88` strength band, `PillarBalance`/`PillarWeights`/`overridePlan`, or `pillarWeighting` sizing (those tests were rewritten or removed, not merely extended). New/rewritten coverage: `SessionAssemblyTests` pins that **no** `.mobility` training block is emitted at any blend length (15-60) even on a strongly mobility-stale history (`testNoMobilityMiddleBlockAtAnyBlendLength`), that the freed minutes go to **strength** and not to bookend stretching (strength training time strictly exceeds the bookend mobility time at every length, and the session still lands within ±60s - `testFreedMinutesGoToStrengthNotStretching`), that a blend's training middle is a strength-only block regardless of staleness, and the **US-M01 validation** at 20/30/45 min - no mobility middle block, strength share above the archived ~0.75-0.80 accessory model, strength leading any dedicated primal block; the extended-blend row now asserts **no** mobility block alongside the dedicated primal block, and `StrengthPrimaryRegressionTests` now sweeps every length/regime asserting **zero** mobility training seconds and strength share `> 0.80`. Engine constants recalibrated: `maxTrainingSets` `5 -> 8` (the strength/primal set lever is now the only training lever, so it carries the whole of a long session) and a fixed `extendedPrimalTrainingShare` (0.15) sizes the extended primal minority block in place of the deleted staleness-weighted split. Primal is untouched (folded into strength short, its own block at 41-60); cold-start/Return still lead strength (now structural) and stay gentle via the cap + volume floor. Broken deleted-symbol tests were removed (`PillarBalanceTests` whole suite; the `overridePlan`/`PillarWeights` unit tests in `ReturnOverrideTests`/`FirstWeekContrastTests`/`ColdStartHandoffTests`) |
| Pattern-matched bookends (US-M02, `BookendComplementSelectionTests`) | US-M02 (prefer-then-fill bookends): the new per-stretch `complements: [MovementPattern]` metadata on the 26 mobility movements (`Exercises.json`, load-validated so a mobility movement missing the tag fails loudly - `ExerciseLibraryError.mobilityMissingComplements`) plus the warm-up/cooldown bias that **leads** with a stretch complementing the day's lead strength pattern, then fills with the existing staleness / no-repeat ordering. `BookendComplementSelectionTests` runs `SessionAssembly.assemble` end-to-end over the real library and pins: (a) for lead patterns squat / push / core (the PRD validation set, forced via a history that leaves the target strength pattern the stalest), the first warm-up **and** first cooldown stretch complements the actual strength-block lead, at 20 min so the lean warm-up always leaves a complementary hold for the cooldown; (b) a general-pool fallback - never an empty or starved bookend - when nothing complements the lead (a library whose stretches have that pattern stripped from their `complements`): both bookends stay present at their length-scaled count and simply lead from the general pool; (c) the coverage invariant - every strength pattern (squat/hinge/push/pull/core) resolves to >= 6 complementary stretches, all 26 mobility movements carry the tag, and two multi-pattern entries (World's Greatest Stretch -> squat/hinge/push, Thoracic Rotations -> push/pull/core) are pinned so a mapping typo is caught; (d) determinism (identical inputs -> byte-identical bookends) and `asOf`-purity (shifting every input date plus `asOf` by the same offset preserves staleness and yields identical bookends, so the prefer-then-fill reorder reads no wall clock). The bias is a pure, stable reorder of the already-deterministic mobility ordering (`SessionAssembly.leadingComplement`); it never drops a stretch, so bookend counts and the US-M01 ±60s timing fit are unaffected (the existing `SessionAssemblyTests`/`StrengthPrimaryRegressionTests` timing rows stay green). |
| `sitsLong` biases bookends, never a direct sizing lever (US-M03, `SitsLongBookendBiasTests`) | US-M03 (`sitsLong` repurposed to *bias*, not a direct sizing lever): the desk-worker flag now stable-promotes posture/hip openers - stretches whose US-M02 `complements` name a hip-dominant pattern (`SessionAssembly.postureHipPatterns` = squat/hinge) - to the front of the warm-up/cooldown pools (`SessionAssembly.postureHipLean`/`isPostureHipOpener`), *underneath* the US-M02 lead-complement promotion, which keeps final authority over the lead slot. The implementation took the simplest of the PRD's "and/or" options - **reorder only, no extra stretch** - so it changes no block's count. `SitsLongBookendBiasTests` runs `SessionAssembly.assemble` end-to-end over the real library and pins: (a) the posture/hip-opener predicate is exactly the squat/hinge-complementing subset of the 26 mobility movements and is a genuine non-total slice (>= 6 openers, not all), with canonical members spot-checked (Pigeon in, Arm Circles out); (b) the PRD validation test proper - two otherwise-identical no-history profiles (`sitsLong` true / false) at 5 and 20 min: the desk profile's bookends carry strictly more hip openers (and never fewer - the invariant the stable partition guarantees) while the block count/structure is identical, no mobility middle block appears, and the 5-min session is not re-inflated (its warm-up stays the length-scaled single movement, no cooldown); (c) `sitsLong` is not a direct sizing lever - the whole training middle (strength, and the extended primal block) is byte-identical between the two profiles across 5/20/45/60 min at the shipped catalog, and the bookend counts match block-for-block (the reorder-only choice adds no stretch); this byte-identity is test-guarded here rather than structural, since the reorder changes bookend *composition* and stretches differ in `workSecondsPerSet`, so bookend duration - and, through the timing fit, a strength set count - could in principle differ under a future catalog change; (d) determinism (identical inputs -> byte-identical `sitsLong` bookends) and `asOf`-purity (a shifted reference clock over no history leaves them identical). The regression guards stay green: `BookendComplementSelectionTests` (US-M02 slot-0 authority preserved), `StrengthPrimaryRegressionTests`/`SessionAssemblyTests` (US-001/002/M01 strength-share and ±60s timing unaffected, since the bias touches only mobility bookend ordering). |
| Uniform three-part session shape (US-M04, `UniformSessionShapeTests`) | US-M04 (uniform Warm-Up -> Strength -> Cooldown vocabulary; validation-only, **no new engine behavior** - US-M01..US-M03 already built the structure): sweeps `5/10/15/20/30/45/60` min through the real `SessionAssembly.assemble` (fresh no-history intermediate user, `.default` policy) and pins, off the blocks the assembly actually produced, that every length maps cleanly onto the uniform vocabulary. (a) **Shape**: the ordered block categories are `.warmup` first, exactly one `.strength` training block, a `.primal` block only at the extended 41-60 min lengths (leading order strength-before-primal), and a `.cooldown` only past `cooldownThresholdMinutes` - with **no** `.mobility` block anywhere (no mobility middle block at any length), no off-vocabulary category, and no empty block reaching the player. (b) **Length-scaled, non-forced bookends**: the warm-up is seeded at `warmupExerciseCount` (1/2/3/4 across the bands), one set per movement; the cooldown appears **iff** `minutes > cooldownThresholdMinutes` (so 5/10 min are lean, warm-up-only, never force-fitted with a cooldown) and stays within `maxCooldownExercises`, one set each - asserting the shipped contract, not a new one. (c) **Strength leads the clock**: strength holds the majority of training time (bookends excluded) at every length - mobility contributes zero training seconds, strength leads any dedicated primal block, and strength stays > 0.80 of the training clock consistent with `extendedPrimalTrainingShare = 0.15`. (d) Determinism: the block-category signature at each length is byte-identical run to run at a fixed injected `asOf` (no wall-clock read). Complements `StrengthPrimaryRegressionTests` (which sweeps the same lengths for strength-lead / no-mobility-middle across three generation regimes) by pinning that the surviving shape reads as the uniform three-/four-part vocabulary and the bookends stay length-scaled rather than forced. |
| No mobility middle block, ever (US-M05, `StrengthPrimaryRegressionTests`) | US-M05 (Movement-Practice-removal regression guard; **test-only**, no engine change - US-M01..US-M04 built the behavior): extends the US-007 sweep (same `5/10/15/20/30/45/60` min × steady-state / cold-start / Return regimes through the real `SessionAssembly.assemble`) with three structural pins. (1) `testNoSessionEmitsAMobilityMiddleBlockAcrossAllLengthsAndRegimes` - for every (regime, length) the session emits **no** `.mobility`-category block at all, no training block resolving to the mobility pillar, and a training middle built only from `.strength` (+ a dedicated `.primal` block that appears only at 41-60 min, strength always present) - so a later tuning change cannot silently reintroduce Movement Practice. (2) `testBookendsLeadWithAStretchComplementingTheStrengthLeadAcrossRegimes` - at the blend lengths (15/20/30/45/60) that carry both bookends, the first warm-up **and** first cooldown item is a mobility stretch whose `complements` contains the strength block's actual lead pattern, re-checking US-M02 prefer-then-fill at the regression altitude (the fresh mobility pool + >= 6 complements/pattern guarantee the lead resolves to a complementary stretch, not the fallback). (3) `testSitsLongNeverChangesStructureOrAddsAMobilityMiddleBlockAcrossAllLengths` - sweeping the full `5...60` matrix, toggling `sitsLong` on an otherwise-identical no-history profile leaves the block-category structure identical and introduces no mobility middle block, complementing (not duplicating) `SitsLongBookendBiasTests`' 5/20/45/60 sizing pins. The existing US-007 strength-lead / share-band / determinism / `asOf`-purity guards in the same suite stay green. |
| Even-round circuit timing (US-CC03/US-CC04, `SessionAssemblyTests` + `ExerciseSwapTests` + `PerSideSwapEvidenceTests`) | US-CC03/US-CC04 (engine timing-model change per [ADR-0003](adr/0003-even-round-circuit-timing.md)): the per-exercise `.addSet`/`.removeSet` timing lever is **retired** - a training block now carries one **uniform round count** (every station's `sets` equal), and the fit lands the ±60s target by tuning a **bounded between-round rest** (`minRoundRestSeconds`...`maxRoundRestSeconds`, 30-75s) as the primary lever, with a fixed ~15s **between-station transition** as the second (distinct) gap. This **supersedes** the `plannedSeconds` formula (`Σ(sets × workPerSet) + rests + transitions`) and the "promote a reserve at any set count / `maxTrainingSets` is the fine lever" prose in the rows above: the wall-clock is now `Σ block` where a circuit block is `rounds × Σ workPerSet + rounds × between-station transitions + (rounds - 1) × round-rest` (`SessionAssembly.blockSeconds`, over both `PlannedBlock` and materialized `WorkoutBlock`) plus one between-block transition, and the fit's only levers are a combined `setRoundsAndRest` (round count + in-band rest tuned together so the greedy can trade a round for rest in one step, escaping band-edge traps) and whole-exercise promote/drop at the block's round count. New `SessionAssemblyTests`: every training block (strength, and the extended 45/60 dedicated primal) is internally uniform across 5/10/15/20/30/45/60 (`testTrainingBlocksCarryOneUniformRoundCountEachEvenRound`, `testExtendedSessionPrimalAndStrengthBlocksAreEachInternallyUniform`); the between-round rest is uniform per block and stays in band at every length (`testBetweenRoundRestStaysWithinItsBandForEveryLength`); every length lands within ±60s under the even-round formula for steady and fresh users (`testEveryLengthLandsWithinToleranceUnderEvenRoundModel`); the two gaps are distinct (transition < round-rest band, `testTwoRestGapsAreDistinctTransitionShorterThanRoundRestBand`); a long session runs >1 round (`testLongSessionsRunMultipleRounds`). Determinism/`asOf`-purity and the Step 6 reps/hold target being untouched are pinned by the existing determinism and capacity-relative rows (still green). **Swap under even rounds**: `ExerciseSwap` no longer re-picks a slot's set count (which would leave a circuit uneven) - a substitute joins at the block's uniform round count or declines; the widened, soft-estimate-scaled `slotTolerance` now applies to every slot (no slot has a set lever), so honestly-comparable peers still swap; `testSwapKeepsTheUniformRoundCountRatherThanRepicking` replaces the retired re-pick test, and the whole-library growth sweep + no-alternative + one-set-block guards stay green. The `PerSideSwapEvidenceTests` sweep now asserts a swap moves the session by at most the swapped slot's own `slotTolerance` of the pre-swap plan (the swap's real even-round contract) and that no swap ever changes a slot's set count. (Reshaping the block's shared round-rest to re-absorb that in-tolerance drift on swap was scoped out of US-CC07: the drift is already bounded by `slotTolerance` and the US-CC07 AC governs rounds/uniformity, not a tighter post-swap landing.) |
| Generous runtime pace (US-CC08, `GenerousRuntimePaceTests` + `StartSeedTests` + `SitsLongBookendBiasTests`) | US-CC08 (engine work-model calibration; the player needed no new value): `SessionAssembly.workSecondsPerSet` stops being a planning-only proxy and becomes a **generous runtime pace**, governed by one documented constant - `workPaceGenerosityFactor = 1.25`, applied inside that function so the same inflated number feeds both the planning wall-clock and the auto-advancing on-screen work window, which is what forbids a screen window roomier than the plan **by construction** rather than by discipline. It scales the *estimated* (rep) half only: the fundamentals' authored cadence is 2.0-2.5 s/rep (a brisk metronome tempo, under the ~2.5-3.0 s/rep a controlled bodyweight rep takes), x1.25 lands them at 2.5-2.9 s/rep and lifts the assumed per-set setup 10s -> 12.5s (~the 85th percentile of completion time if the authored estimate is a median and cadence has ~20% CV); a **hold is unscaled**, because its per-second cost is *definitional* rather than estimated (`secondsPerHoldSecond`, doubled per side - prescribed seconds are elapsed seconds, so there is nothing to be generous about) - the same assumed-vs-observed split `maxSetupShareOfEstimate` already draws. The split is estimated-vs-definitional, not windowed-vs-unwindowed: every *rep-based* movement is paced wherever it sits, warm-up/cooldown stretches included, because those reps are estimated too and the plan owes the slower user their real time whether or not that set runs under an on-screen countdown. Step 6's capacity-relative per-set target is untouched. New `GenerousRuntimePaceTests`: the PRD validation walked over a **real assembled 20-minute session** rather than a fixture - at every auto-advancing set the player's window seconds equal `SessionAssembly.workSecondsPerSet(of:)` for that very prescription, and while the window is up its total and remaining are that same number, so a second window value introduced anywhere along the rotation fails here where a single-step assertion would miss it (`testEveryAutoAdvancingSetsWindowIsTheEnginesPlannedSecondsForThatSet`); per-side and hold pricing agree runtime and planning because it is one function, asserted rather than assumed - planning charges `sidesPerSet x` the prescribed hold plus only the movement's own authored setup, while the player's `holdSecondsPerSide` is the prescribed seconds once per leg (`testPerSideHoldPricesBothSidesInPlanningAndRunsPerSideInThePlayer`); the calibration is swept over the whole catalog so it cannot hold for a lucky movement (`testGenerousPaceScalesEveryRepBasedMovementAndLeavesHoldsAlone`); `asOf`-purity is a live assertion, not a comment - identical pricing across calls and across two sessions assembled a month apart (`testWorkSecondsPerSetIsAPureFunctionOfTheExerciseAndTarget`); and the trade-off is **bounded**, not merely accepted - every length still lands within +/-60s for every fitness level and no training block is squeezed below one round (`testGenerousPaceStillLandsEveryLengthAndNeverStarvesABlockOfItsLastRound`). **Reconciled tests (the numbers legitimately moved, none weakened):** `StartSeedTests`'s hard-coded per-set seconds are re-expressed as `paced(setup + cadence x reps)` so they still pin the model's *shape* - setup once, cadence per rep - instead of becoming opaque literals, and the whole-catalog sweep now measures each movement against its own *paced* estimate while **adding** two assertions the old numbers could not make: a rep-based set must price strictly above its typical-case authored estimate, and a hold must price exactly at it. **Accepted behavioral consequences (both documented, neither hidden):** (1) the same inflated number drives the fit, so a session lands fewer rounds - and at 45 min, one fewer **station** - measured across 5/10/15/20/30/45/60 x beginner/intermediate/advanced: 15 min 6 -> 5 rounds, 20 min 6 -> 5, 30 min 7 -> 6, 60 min strength unchanged at 8 x 5 (primal 8 -> 7), 5 min unchanged at 3; at **45 min** the strength block goes 7 rounds x 5 stations -> **8 x 4**, so the longest strength block now trains **4 distinct movements instead of 5**, narrowing its movement-pattern coverage, and with the block pinned at the `maxTrainingSets` (8) rail dropping a station is the fit's only remaining lever there - accepted as-is (a station floor or a higher rail is a separate follow-on, out of US-CC08); with worst |fit error| 1s against the +/-60s tolerance and the round outcome flat across 1.20-1.30 (so the value inside that band is a pure pacing choice); a fast user finishes early and taps **Done**. (2) The US-M03 desk-worker bias now **binds** on the shipped catalog: a rep-based stretch paces up while a hold stretch does not, so a `sitsLong` warm-up costs ~32s more at 45 min (was ~10s) and the fit pays it out of a whole strength round. That byte-identity was always incidental rather than structural (the bias adds no stretch and changes no count), so `SitsLongBookendBiasTests.testSitsLongDoesNotSizeAnyTrainingBlock` is **rescoped**, not relaxed, into `testSitsLongChangesNoTrainingMovementOrTarget`: the training middle must hold the same movements at the same per-set targets, every block must stay internally even, no mobility middle block may appear, and - the assertion that replaces the coincidence with its mechanism - **equal bookend seconds must still produce identical round counts**, so the bias can never reach the training middle through any channel but bookend cost. Captain-confirmed 2026-08-15. Generation latency is unaffected (one multiply): the opt-in `SessionGenerationBenchmarkTests` re-run gives worst-case p95 2.65 ms against the 100 ms budget |
| Generous runtime pace, rendered evidence (US-CC08, `USCC08PaceEvidenceTests`) | US-CC08 reviewer evidence, rendered on every run like the other evidence suites and written through `EvidenceOutput` (temporary directory by default, `artifacts/reports/us-cc08/` under `REPTODAY_WRITE_EVIDENCE=1`): hosts the production `ActiveSessionView` on the first auto-advancing set of a **real assembled 20-minute session** and captures the work window actually counting down, and writes a `pace-report.txt` transcript that walks the whole session putting `ActiveSessionViewModel.workWindowSecondsPerSet` beside `SessionAssembly.workSecondsPerSet` step by step (asserting no step disagrees), prices every catalog movement at its default target with its measured per-rep cadence and paced ratio, records the fit and the round x station shape at every length against `toleranceSeconds`, and contrasts the `sitsLong` and general 45-minute sessions so the US-M03 bookend consequence is readable as bookend seconds against an unchanged training middle. Deliberately references no US-CC08-only symbol, so the same suite runs against the pre-change engine to produce the before half of the comparison |
| Hands-free completion logging invariant (US-CC09, `ActiveSessionViewModelTests` + `NoPerSetCompletionTrackingGuardTests`) | US-CC09 (hardening/lock-in - the invariant was already structural from US-CC01/US-CC02/US-CC07, so no production change): an auto-advanced set records prescribed = performed identical to a tapped completion, only an explicit **Skip** logs not-done, and no per-set "did they really do it" field is added - the end-of-session perceived-difficulty `rate` (US-L02) stays the sole adaptation signal into the Ramp. `testWholeSessionHandsFreeWithOneSkipLogsCompletedExceptTheSkip` (the PRD validation test) drives a **whole session** - a warm-up bookend hold, a three-station strength circuit of rep windows across three rounds, and a cooldown bookend hold - entirely hands-free (every hold, window, and rest driven to zero by the injected clock, not one manual completion) with **exactly one training station explicitly skipped**, then reads the durable `WorkoutLog`: each auto-advanced training station logs its full uniform round count (`push_up`/`hinge` -> 3 completed sets each, `CompletedSet(reps:durationSeconds:)` = prescribed), each bookend hold logs its single set, the skipped `squat` logs zero completed sets and `skipped == true` across all rounds, and `perceivedDifficulty` is `nil` until `rate` is called (then it, and only it, carries the signal). `NoPerSetCompletionTrackingGuardTests.testCompletedSetHasNoPerSetCompletionTrackingField` pins `CompletedSet` by reflection to exactly `reps`/`durationSeconds` (compile-coupled build-time fact, the same enforcement pattern as `NoManualModeGuardTests`/`UITestLaunchGuardTests`), so any per-set difficulty/verification field trips the default `-scheme RepToday test` gate. **Accepted telemetry consequence (documented in the PRD's US-CC09 note and here):** because a work window auto-records its set at countdown zero without an affirmative tap, the US-T10 `>=80%` `session_completed` metric reads **a touch more generously** than the retired tap-gated model - accepted, not a defect (the follow-along design trades per-set proof for a hands-free flow; the rating remains the real adaptation signal; interpret the number as "reached the end" not "verifiably performed every rep"). No emission site moved and no new event was added |
| Non-verbal state cues, ducking, VoiceOver coordination (US-CC10, `ActiveSessionViewModelTests`) | US-CC10 (player-only): the single US-K02 cue seam becomes a per-state vocabulary - `SessionCuePlayer.play(_:suppressAudio:)` over a `SessionCue` enum (`.go`/`.halfway`/`.transition`/`.roundRest`/`.done`), replacing `RestTimerFeedback.restDidComplete()`. `.go` fires once when a work window auto-starts and not on an idempotent re-entry (`testWorkWindowStartFiresGoOnce`); `.halfway` fires once at the running window's midpoint - never before it, never per-tick, and frozen while paused then measured from the real remainder on resume (`testWorkWindowHalfwayFiresOnceAtMidpoint`, `testHalfwayCueIsFrozenWhileTheWorkWindowIsPaused`); a between-station advance fires `.transition` and a round-boundary advance fires the distinct `.roundRest` (`testTransitionAndRoundRestAreDistinctCues`); a rest ending fires no cue of its own - the boundary tone is the `.go` the next window fires, so the two never double (`testRestEndFiresGoOnceAsTheNextWindowStarts`); the US-CC05 switch-sides beat fires **no cue of any kind** (not a done, transition, or go) so a per-side bookend hold still lands exactly one `.done` per leg (`testSwitchSidesBeatFiresNoCueOfAnyKind`); and when VoiceOver is running the tone is withheld (`suppressAudio == true`) while the cue still fires - the haptic still lands, so a state change is never lost and never talks over speech (`testCueWithholdsAudioWhileVoiceOverIsRunning`). The spy records `(cue, suppressAudio)` pairs and its `completions` still counts only `.done`, so every pre-US-CC10 completion/skip/early-Done/switch-sides cue-count assertion reads unchanged. **Device-only, captain-verifiable manual QA the unit suite cannot cover:** the real `AVAudioSession` `.playback`+`.duckOthers` ducking/restore (music lowers, not stops, then returns) and the live VoiceOver-collision timing are only real on a device - Simulator audio routing and VoiceOver are a proxy - so the units prove the decision logic (which tone for which state, switch-sides silent, audio withheld under a VoiceOver-running flag) through the injected spy; tone source is distinct system sounds (no bundled asset, no `docs/asset-attribution.md` row owed) |
| Visual-primary work window + transition-beat "Next" cue (US-CC11, `ActiveSessionViewModelTests` + `VisualWorkWindowEvidenceTests`) | US-CC11 (player-only): the rep-based work window becomes **visual-primary** - `WorkWindowCountdownView` pairs the movement illustration (the shared `ExerciseIllustration`, extracted from `ExerciseDemoView` so the US-O01 Lottie seam is preserved by construction: clip-if-present, else the SF-Symbol glyph, Reduce Motion stills either) with a compact `CountdownRing` (parameterized `diameter`/`lineWidth`/`font`) in the fixed `exerciseSlotCard`, so a text-and-ring-only window is rejected. The between-station transition beat gets its dedicated prominent "Next: <exercise>" styling (the shared rest header stays "Rest"), driven off the existing cue-kind seam: `ActiveSessionViewModel.startRest` now retains the started cue (`restCue`), exposing `isTransitionBeat` (`.transition` cue) so the overlay tells a transition beat from a between-round rest and a switch-sides beat. `testTransitionBeatIsDistinctFromRoundRest` pins the view-model distinction under an injected clock (a between-station gap reads `isTransitionBeat`, a between-round rest does not, no rest does not); `VisualWorkWindowEvidenceTests` drives the production `ActiveSessionView` in a real key window and asserts on the live accessibility tree that the work window hosts the illustration (a "<name> demonstration" label) **together with** the ring (a "Work window," label) - not ring-only - and that the transition beat surfaces a prominent "Next: <exercise>" label, capturing both frames to `artifacts/reports/us-cc11/` under `REPTODAY_WRITE_EVIDENCE=1` (temporary dir otherwise). No engine/model/persistence change; advance logic, timing model, and cue vocabulary untouched. Looping per-movement clips stay a fast-follow (a data change behind the preserved seam); the on-device Reduce-Motion stilling is captain-verifiable manual QA the unit suite covers only through the still-illustration assertion |
| First-run explainer for the self-driving session (US-CC13, `AppStateTests` + `ContinuousCircuitExplainerTests`) | US-CC13 (presentation + one-shot flag; no engine/model/advance/timing/cue change): a single dismissible explainer shown at most once on first arrival at the continuous-circuit player, stating the four points - the session auto-advances, **+ More time** never rushes you, **Done** jumps ahead, and tones mark state changes - in identity-framed, non-loss-framed copy (`ContinuousCircuitExplainerView`). The one-shot flag lives on `AppState` (`hasSeenContinuousCircuitExplainer`, persisted; read via `shouldShowContinuousCircuitExplainer`, flipped via `markContinuousCircuitExplainerSeen()`): `AppStateTests.testExplainerShowsOnAFreshInstall` (unseen -> shows), `testMarkingTheExplainerSeenGatesItOff` (seen -> never again, persisted), and `testExplainerSeenFlagSurvivesRelaunch` (a second `AppState` off the same store stays gated) pin the gating with an injected `UserDefaults`. `ActiveSessionView` presents it as an overlay layer (not a sheet) driven by `showExplainer`, flipping the flag the moment it decides to show so a force-quit mid-explainer can never re-fire it, and stilling the entrance/exit transition under Reduce Motion (`@Environment(\.accessibilityReduceMotion)`); it reads `AppState` from the environment as an **optional** so the evidence surfaces that host `ActiveSessionView` without one are unaffected, and never blocks the session (the player's own `onAppear` has already run). `ContinuousCircuitExplainerTests` hosts the production view in a real key window: `testAllFourPointsAreVoiceOverReadable` asserts all four points are announced on the live accessibility tree (including "+ More time"/"Done" as plain spoken words, markdown asterisks stripped), `testDismissControlIsPresentAndLabeled` finds the labeled "Got it" element, `testCopyIsIdentityFramedNotLossFramed` bans loss-framed phrases, and `testDismissInvokesTheProductionCallback` proves the callback dismisses. On-device VoiceOver focus-trapping (`.isModal`) and the Reduce-Motion stilling are captain-verifiable manual QA the unit suite covers only through the hosted a11y tree and the decision logic |
| Migration: resume an old paused session in the new player (US-CC12, `ActiveSessionViewModelTests` + `PersistenceTests`) | US-CC12 (hardening/lock-in - the migration was already structural once US-CC02 kept `currentStepIndex`/`currentSet` with no snapshot-schema change, so a station's r-th set *is* round r and the saved position maps straight into round/station terms). `testResumesAnOldUnevenPausedSnapshotPreservingWorkSkipsAndHoldSide` is the PRD validation shape: an old-shape snapshot with **uneven** per-exercise set counts (pre-US-CC03: push_up 3, the per-side side_plank hold 3, squat 2), a mid-block position (side_plank, round 2), some completed sets (push_up 2, side_plank 1), one skipped exercise (squat), and a per-side `hold.side == 2` constructs the new player, which resumes at the exact saved position, maps the uneven block deterministically into round/station terms (`currentRound == 2`, `circuitRoundCount == 3` off the block's **max** station set count, a shorter station simply dropping out of later rounds via `hasSet`), preserves every banked set with none lost or double-counted (`completedSetCount == 3`), keeps the skip (squat stays skipped, logs no sets), and restores the hold **idle on side 2** (`holdSide == 2`, `isHolding == false`, `canStartHold` - never a running leg, US-O03). `testResumedUnevenPreCircuitPlaysToCompletionWithoutLosingWork` then drives a resumed uneven pre-CC circuit through to completion to prove the walk never traps or drops the pre-pause work (the position-driven rotation is total-preserving from the saved spot). Forward safety (no schema change): `PersistenceTests.testActiveSessionStateDecodesWithoutTheHoldOrExercisedMinutesFields` feeds the decoder the oldest resumable shape - neither the US-O03 `hold` key nor the US-T10 `exercisedMinutes` key present - and asserts it round-trips unchanged, so an app update never costs a user their in-progress workout |

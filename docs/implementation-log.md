# Implementation Log

Story-by-story record of what has been built, extracted from `CLAUDE.md` to keep that file lean.
The authoritative progress tracker is the PRD in `.claude/agent/tasks/`; this is the narrative of what each landed story added and why.

## Built modules (story-by-story)

As of the current clean rebuild, the US-A01 scaffold (App, DesignSystem, RootView, Assets), the US-A02 canonical domain enums (`Models/Enums.swift`), the US-A03 domain model structs (`Models/Exercise.swift`, `User.swift`, `Workout.swift`, `WorkoutLog.swift`), the US-A04 CoreData stack (`Persistence/`: `RepToday.xcdatamodeld`, `PersistenceController`, `CDUser`/`CDWorkoutLog` + conversions, `MockPersistence`), the US-A05 app shell (`Services/Protocols/ServiceProtocols.swift`, `Services/Mock/MockServices.swift`, `DI/ServiceContainer.swift`, `Utilities/AppState.swift`, and onboarding-vs-main-tabs routing in `Views/RootView.swift`), the US-B01 bundled exercise library (`Resources/Exercises.json`, 42 zero-equipment movements with valid progression chains at the time, grown to 57 by US-O02 - see below), the US-B02 exercise service (`Services/Mock/MockExerciseService.swift`: loads/caches/validates the library, throws `ExerciseLibraryError`, and answers the by-pillar/pattern/phase/difficulty-range and next-in-chain queries), the US-C01 engine Step 1 (`Services/Engine/SessionShapeSelection.swift`: the pure `SessionShapeTemplate.select(requestedMinutes:)` that maps requested minutes to a session-shape template and resolves back to the canonical `SessionShape` - extended to the full 5-60 min range in US-E01), the US-C02 engine Step 2 (`Services/Engine/PillarBalance.swift`: `PillarStaleness` computes days-since-worked per pillar from the logs, and `PillarPlan.select` picks the stalest pillar for single-focus - with a desk-worker mobility lean - or staleness-weighted time shares for a blend), and the US-C03 engine Step 3 (`Services/Engine/MovementPatternFocus.swift`: `PatternStaleness` computes days-since-worked per movement pattern, and `PatternFocus.rank`/`select` rank the chosen pillar's candidate patterns stalest-first and pick the lead pattern - holding back the most recent session's lead pattern so it is never repeated back-to-back, while honoring an explicit request), the US-C04 engine Step 4 (`Services/Engine/ExercisePoolFilter.swift`: `InjuryContraindication` maps onboarding injury tags to contraindicated movement patterns, and `ExercisePoolFilter.eligiblePool` removes phase-gated / injury-unsafe / over-cap / repeatedly-skipped movements while enforcing the Zero-Equipment Floor, with `pool(forPattern:)` falling back to the safest bodyweight option - or reporting `.noSafeOption` - when filtering empties a needed pattern), the US-C05 engine Step 5 (`Services/Engine/ProgressionChainSelection.swift`: `AdvancementCriteria` parses an exercise's free-text `advancementCriteria` and checks it against logged performance, `ProgressionChainSelection.selectInChain` finds the user's frontier tier - the highest-order tier worked - and offers the next tier only when its criteria are cleared and that tier is still eligible, never leaping past an un-cleared or gated tier and defaulting a no-history user to the chain entry, and `select(pattern:)` integrates every chain for a pattern with the no-repeat-last-3 variety rule and an active-chain preference), and the US-C06 engine Step 6 (`Services/Engine/AdaptiveOverload.swift`: `AdaptiveOverload.target` reads the user's demonstrated capacity for the selected exercise from the most recent usable log - the worked set count and the rounded average per-set reps/hold-seconds - and prescribes a capacity-relative `OverloadTarget`, applying that session's `perceivedDifficulty` within one cycle (`tooHard` eases below capacity, `tooEasy` pushes above, `justRight`/unrated nudges progressively up, with the per-cycle bump curve as tunable constants), clamping the per-set target and set count to safety rails so a target is never a fixed heroic number, and falling back to the exercise's own `defaultReps`/`defaultDurationSeconds` when there is no usable history), and the US-C07 engine Step 7 (`Services/Engine/SessionAssembly.swift`: `SessionAssembly.assemble` chains Steps 1-6 over the eligible pool and assembles a fully-formed, playable `Workout` - always opening with a `.warmup` block, closing sessions over 10 min with a `.cooldown`, and structuring training blocks per the Step 1 shape - then a deterministic best-fit `fit` pass adds/drops whole exercises or individual sets, never the capacity-relative per-set target, until the planned `Σ(sets × est) + rests + transitions` wall-clock lands within ±1 min of the request; `MockWorkoutEngine` now drives this assembler with the validated library from the exercise service), and the US-C08 deterministic swap (`Services/Engine/ExerciseSwap.swift`: `ExerciseSwap.swap` returns a `SwapOutcome` - either an equivalent substitute drawn from the eligible pool (same pillar and pattern, within a ±1 `difficultyBandWidth`, inside the `slotToleranceSeconds` time budget, never duplicating a movement already in the session, carrying a fresh capacity-relative per-set target, the original slot's rest, and the set count that keeps the slot inside that budget) or `.noAlternative` when no safe in-budget peer exists - exposed through `WorkoutEngineProtocol.swapExercise` and wired in `MockWorkoutEngine`) exist; the Epic C engine pipeline (Steps 1-7 + swap) is complete, and `ViewModels/` and the onboarding/ready/active-session `Views/` are populated as of US-I01 and US-K01 (see the v6 Epic I and Epic K notes below).
The v6 Epic D data foundation has begun: US-D01 (`Models/User.swift`) adds the nested `User.Why` (`statement` + optional `openingBias` pillar), `User.Duration` (`defaultMinutes`/`onboardingSeedMinutes`/`completedDurationEWMA`, with a `seeded(minutes:)` factory so `defaultMinutes == onboardingSeedMinutes`), and `User.ColdStart` (`sessionsLogged`/`active`, with a `.fresh` default) value types as three new top-level `User` fields - `primaryGoal` and the `UserProfile` shape are unchanged - each persisted as its own JSON-encoded `Data` column on `CDUser` (`whyData`/`durationData`/`coldStartData`) that decodes to documented defaults (empty `why`, duration seeded from `profile.typicalAvailableMinutes`, a fresh cold-start) when absent, so a pre-v6 record still loads.
US-D02 (`Models/WorkoutLog.swift`) adds `requestedMinutes: Int` (what the Ready Screen offered or the user set) and `wasReturn: Bool` (defaulting to `false`) as two new top-level `WorkoutLog` fields, and documents the existing `durationMinutes` as the actually-completed duration that feeds `duration.completedDurationEWMA` - the requested-vs-completed gap is the input Default Duration learning and the Disengagement signal read, and `wasReturn` marks a post-gap Return for the Re-entry Ramp; both persist as additive optional `NSNumber?` columns on `CDWorkoutLog` (`requestedMinutes`/`wasReturn`) that decode to documented defaults (`requestedMinutes == durationMinutes`, `wasReturn == false`) when absent, so a pre-v6 log still loads.
US-D03 (`Models/SessionPolicy.swift`) defines the always-valid `SessionPolicy` - `version`/`updatedAt`/`updatedBy` (`default`/`deterministic`/`llm`)/`progressionRate`/`pillarWeighting: [Pillar: Double]`/`varietyWindow` plus optional `coldStartContract` (`forceContrastSpread`, `cappedMaxDifficulty` in 1...5), `reentry` (`rampSessionsRemaining`), and `note` (`text` + `template`/`llm` `source`) - the single seam the AI Programmer (Epic F) writes and the engine (Epic E) reads; `SessionPolicy.default` is a deterministic, neutral starting policy (version 1, a fixed `unprogrammedEpoch` timestamp, progression 1.0, equal `1.0` weighting across all three pillars via `neutralPillarWeighting`, variety window 3, no situational overrides) that reproduces pre-policy engine behavior exactly, and it persists whole (JSON-encoded) as `CDSessionPolicy`, keyed by `userId` and overwritten in place so the last-written policy survives relaunch and offline use.
US-D04 (`Models/ReprogramTrigger.swift`, `Services/Protocols/ServiceProtocols.swift`, `Services/Mock/MockServices.swift`, `DI/ServiceContainer.swift`) opens the AI Programmer <-> engine seam: the new `ReprogramTrigger` value type carries a `kind` (`weekly_boundary`/`return`/`physical_stall`/`disengagement`, stable snake-case raw values) and an injected `detectedAt`, and the new `SessionPolicyServiceProtocol` (`currentPolicy(for:)`, `reprogram(user:recentLogs:trigger:)`, `dueTriggers(user:recentLogs:asOf:)`, all `async throws`) is the one seam the Programmer (Epic F) writes and the engine (Epic E) reads; `MockSessionPolicyService` hands back `SessionPolicy.default` for both policy calls and never reports a due trigger (real detection/re-weighting land in US-F01/US-F03), and it is wired into `ServiceContainer`/`.mock()`; `WorkoutEngineProtocol.generateWorkout(...)` gains a `sessionPolicy:` parameter that `MockWorkoutEngine` accepts at the seam and (as of US-E03) threads into `SessionAssembly`, where `SessionPolicy.default`'s neutral levers keep output identical to pre-policy behavior.
US-E01 (`Services/Engine/SessionShapeSelection.swift`, `Services/Engine/PillarBalance.swift`) begins the v6 Epic E engine extensions: `SessionShapeTemplate.select` now shapes the full 5-60 min request range with four buckets - `5-10 -> singleFocus`, `11-20 -> blendLight`, `21-40 -> blendFull`, `41-60 -> blendExtended` (the new extended-blend case) - clamping out-of-range requests into a `5...60` `supportedRange` so the mapping stays total, pure, and deterministic; every blend (including `blendExtended`) still resolves to the canonical `.blend` `SessionShape` and `PillarPlan.select` treats `blendExtended` like the other blends, so the reclassified 20-min session (now `blendLight` rather than `blendFull`) is behavior-neutral downstream; the dedicated extended primal block lands in US-E02.
US-E02 (`Services/Engine/PillarBalance.swift`, `Services/Engine/SessionAssembly.swift`) promotes primal to a first-class pillar: `PillarWeights` gains a `primal` share and `PillarPlan.select` splits an extended blend across all three pillars (each kept above `minExtendedBlendShare`, the rest apportioned by weighted staleness so the shares always sum to 1) while short/full blends keep `primal == 0` (folded into strength as before, no regression); `select` also takes a `pillarWeighting: [Pillar: Double]` staleness multiplier (defaulting to `SessionPolicy.neutralPillarWeighting`, a no-op) so primal selection respects `sessionPolicy.pillarWeighting[.primal]` - the live policy is threaded through `assemble` in US-E03. In assembly, a `blendExtended` session now builds a dedicated `.primal` block from the `locomotion` pattern / `pillar == .primal` (via the same Steps 5-6 chain selection + Adaptive Overload as the strength block, Zero-Equipment Floor and difficulty gating intact), sheds primal from the strength block so no locomotion movement is double-booked, and orders the up-to-three training blocks staler-pillar-first; the block gracefully degrades to strength + mobility when the pool has no eligible primal movement.
US-E03 (`Services/Engine/SessionAssembly.swift`, `Services/Engine/AdaptiveOverload.swift`, `Services/Engine/ProgressionChainSelection.swift`, `Services/Mock/MockServices.swift`) makes the engine consume the live Session Policy: `SessionAssembly.assemble`/`planBlocks` take a `sessionPolicy:` parameter (defaulting to `SessionPolicy.default`, a no-op that reproduces pre-policy output exactly) and thread its three levers into the pipeline - `pillarWeighting` scales Step 2's staleness split (a heavier weight measurably grows that pillar's block time), `varietyWindow` replaces Step 5's hardcoded no-repeat window (`ProgressionChainSelection.select` now takes a `varietyWindow:`, still defaulting to `recentSessionWindow`, and the assembler's mobility variety ordering mirrors it), and `progressionRate` paces Step 6's Adaptive Overload bump (`AdaptiveOverload.target` now takes a `progressionRate:`, scaling only the advancing `progressiveStep`/`easyStep` around `1.0` via `paced(_:rate:)` - never the `tooHard` ease, which is US-E05's Asymmetric Ramp - still clamped to the rep/hold safety rails); `MockWorkoutEngine` now passes its `sessionPolicy` straight through to `assemble`, finally wiring the US-D04 seam end-to-end.
US-E04 (`Services/Engine/ColdStartOverride.swift`, `Services/Engine/SessionAssembly.swift`) adds the engine's Step 0 cold-start override: `ColdStartOverride` runs before Steps 1-6 but only while `user.coldStart.active` and the live `sessionPolicy.coldStartContract != nil` (both no-ops once US-G04 retires cold-start, so a warmed-up user runs exactly the US-E03 pipeline). It touches two pipeline inputs and leaves everything else to Steps 1-6: `cappedPool` restricts the eligible pool to movements at or below the contract's `cappedMaxDifficulty` (the "served at the gentle end" promise then falls out of Step 5's existing no-history entry-tier selection, and the cap never empties the pool - it returns the uncapped pool rather than break generation), and `overridePlan` (gated additionally on `forceContrastSpread`) forces First-Week Contrast by rotating the day's lead pillar deterministically by `coldStart.sessionsLogged` from an onboarding-derived start (`why.openingBias`, else mobility for a desk worker, else strength), so consecutive cold-start days never repeat a pillar and the first week spans strength/mobility/primal instead of a desk worker's all-mobility bias; single-focus days take the rotated pillar directly (a new `.single(.primal)` path in `SessionAssembly.buildBlocks` builds a dedicated locomotion block, degrading to strength then mobility if the capped pool has no eligible primal), and blend days re-point the Step 2 shares so the rotated pillar leads (preserving the shares' sum-to-1 and floors). Step 0 stays a pure function of its inputs (the "day" is read from `sessionsLogged`, no wall clock), so determinism holds.
US-E05 (`Services/Engine/AdaptiveOverload.swift`) tunes Step 6's per-cycle bump curve into the **Asymmetric Ramp** - back off fast, climb slow: a single recent `too_hard` or a **skip** of the exercise pulls the next target down eagerly (`hardStep` steepened to `0.80`, a 20% back-off), while `too_easy` climbs only patiently (`easyStep` gentled to `1.10`, a 10% rise) and `justRight`/unrated nudges up by the gentlest `progressiveStep` (`1.05`), so the down-step magnitude is always >= the up-step (`(1 - hardStep) >= (easyStep - 1)`, asserted on the constants) and every signal still moves by at least one so direction is never lost to rounding. The `too_hard`/`too_easy` mapping plus the new skip signal are resolved into a private `RampSignal` (`eased`/`intensify`/`progress`): `demonstratedCapacity` now scans past a skipped most-recent entry to read the set count and per-set value from the earlier worked session but remembers the skip and forces `.eased`, so a recent bail eases the target (below capacity) even over an earlier `too_easy`, without the skip becoming the capacity *source*. The ramp composes with US-E03's `progressionRate` unchanged - the rate still paces only the two advancing steps, never the eager down-step - and stays within the existing rep/hold/set rails.
US-E06 (`Services/Engine/ReturnOverride.swift`, `Services/Engine/SessionAssembly.swift`, `Services/Engine/AdaptiveOverload.swift`, `Services/Engine/PillarBalance.swift`, `Models/Workout.swift`) completes the Epic E engine extensions with the **Return override** and the **Re-entry Ramp** - discipline overriding optimization after a gap. `ReturnOverride.isReturn` flags the next session a Return once the calendar-day gap since the most recent logged session reaches `returnThresholdDays` (7, pure over `asOf`, `false` for a fresh no-history user), and while a Return is active - and cold-start is not; the two are mutually exclusive, the Return gated off by `ColdStartOverride.isActive` since cold-start already serves gentle capped sessions - `SessionAssembly` serves an easy, winnable session *regardless of staleness or the policy's optimization levers*: `overridePlan` leads with mobility (a single-focus Return trains it directly, a blend re-points its shares to favor mobility via the now-shared `PillarWeights.favoring`, extracted from `ColdStartOverride`), `returnPool` caps the eligible pool at `returnMaxDifficulty` (2, never emptying it), and `reentryScale` eases Step 6's volume to the gentle `reentryFloorScale` (0.70). The Return itself carries no readjustment; the readjustment lives in the Re-entry Ramp: `sessionPolicy.reentry.rampSessionsRemaining` (seeded to `rampSessions` = 3 by the Programmer after a Return, US-F03) drives `ReturnOverride.rampScale`, which walks the volume scale from that floor back to neutral `1.0` as the counter decrements, so `AdaptiveOverload.target` (now taking a `reentryScale:` - neutral `1.0` by default, applied only to capacity-derived per-set targets, and always landing at least one below the un-held target down to the rep/hold rails) holds difficulty below normal on the sessions after a Return and climbs it back. The Return decision is made once (the shared `SessionAssembly.isReturnSession`, gating both the overrides and the flag) and stamped on the assembled `Workout.wasReturn`, so the post-session log-writer (US-L01) records it onto `WorkoutLog.wasReturn` (US-D02) rather than re-deriving detection at a different `asOf`. Every override is a pure function of its inputs and a no-op in the steady state, so a present, warmed-up user runs exactly the US-E03 pipeline; with US-E06 done the Epic E engine extensions are complete, and Epic F (the AI Programmer that writes the policy, including seeding the `reentry` ramp on a `return` trigger) is next.
The v6 Epic F AI Programmer has begun: US-F01 (`Services/Programmer/ReprogramTriggerDetection.swift`) opens the on-device, deterministic Programmer with **re-program trigger detection and Trigger Precedence** - the detection half of the seam whose re-weighting lands in US-F02/US-F03. `ReprogramTriggerDetection.dueTriggers(user:recentLogs:library:asOf:calendar:)` is a pure function returning the due `ReprogramTrigger`s (US-D04) in precedence order, so the client can re-program on open against the highest-precedence one without any server clock and the new policy applies one open later. It detects the four PRD kinds: `weekly_boundary` (the injected calendar's `weekOfYear` for `asOf` is strictly later than the most recent log's week, so a just-closed Consistency-Score week exists to re-tune against - never for a no-history user), `return` (reuses `ReturnOverride.isReturn`, the single Return-detection seam so the Programmer and engine can never disagree on what a Return is), `physical_stall` (for each chain the user has worked, the frontier tier - the highest-order worked tier, the same notion Step 5 uses - has an existing next tier (`progressionId != nil`) yet its parsed `AdvancementCriteria` are cleared in `stallClearedSessions` = 2+ distinct sessions while the frontier never rises, meaning that real next tier is gated and the user is genuinely stuck; a single-tier or top-of-chain movement has nothing to advance to and never stalls, so the library's easily-met single-tier mobility chains never misread a healthy user as a plateau), and `disengagement` (over the most recent `disengagementWindow` = 3 completed sessions, the per-session completion ratio `durationMinutes / requestedMinutes` - clamped to 1 - forms a declining non-increasing run whose newest value falls to <= `disengagementCompletionRatio` = 0.6, or a window skip rate >= `disengagementSkipRate` = 0.5; reading the requested-vs-completed gap rather than absolute completed minutes is deliberate per US-D02, so a user who intentionally requests shorter sessions and finishes them - Default Duration learning - is never misread as pulling away, and lengthening gaps are already the Return trigger). **Trigger Precedence** is enforced at the source - when both apply, `disengagement` suppresses `physical_stall` so a user pulling away is never handed more challenge - and the returned order (`return` < `disengagement` < `physical_stall` < `weekly_boundary`) mirrors it. Detection is pure and deterministic over `(user, recentLogs, library, asOf)` - the clock is injected as `asOf` and stamped onto each trigger's `detectedAt`, never read from the wall clock - and needs the validated `library` only to resolve advancement criteria and chain position for the stall signal; the concrete `SessionPolicyServiceProtocol` (US-F03) supplies the library and adapts this to the protocol's library-free `dueTriggers(user:recentLogs:asOf:)` (the shell `MockSessionPolicyService` still reports no triggers until then).
US-F02 (`Services/Programmer/PlateauDiagnosis.swift`) adds the Programmer's **plateau diagnosis** - telling a *physical stall* (capacity earned but gated) apart from *disengagement* (pulling away) and mapping each to the Session Policy levers US-F03 writes.
The two plateau predicates that US-F01 detected inline (`isPhysicallyStalled(recentLogs:library:)` and `isDisengaging(recentLogs:)`, with their tuning constants `stallClearedSessions`/`disengagementWindow`/`disengagementCompletionRatio`/`disengagementSkipRate`) now live here as the single seam, and `ReprogramTriggerDetection.dueTriggers` composes them rather than re-deriving them - so detection and diagnosis can never disagree about what a stall or a disengagement is (the same pattern by which US-F01 reuses `ReturnOverride.isReturn`), a behavior-preserving move that leaves US-F01's trigger output unchanged.
`PlateauDiagnosis.diagnose(recentLogs:library:) -> Plateau?` returns the single plateau in force (`.physicalStall`/`.disengagement`) or `nil`, applying the same Trigger Precedence at the source (disengagement wins, so a diagnosis can never hand more challenge to a user pulling away); it is pure over `(recentLogs, library)`, reading only log history (the completion ratio and skip rate are the consistency signals, straight off the logs per US-D02) with no clock, and needs the `library` only to resolve advancement criteria and chain position for the stall.
The **explicit lever mapping** `PlateauDiagnosis.reweighted(_:for:)` moves only the affected levers on a passed-in policy and leaves `version`/`updatedBy`/`note`/persistence to US-F03: a `.physicalStall` multiplies `progressionRate` by `stallProgressionBoost` (1.15) and widens `varietyWindow` by `stallVarietyWiden` (+1) for a capable-but-gated user, while a `.disengagement` eases `progressionRate` by `disengagementProgressionEase` (0.85) and narrows `varietyWindow` by `disengagementVarietyNarrow` (-1) - lower difficulty, less novelty - and never raises challenge; both are clamped to the rails (`minProgressionRate`/`maxProgressionRate` 0.5...2.0, `minVarietyWindow`/`maxVarietyWindow` 1...6) so recurring plateaus accelerate or ease without running away.
The session-length half of "less friction" for disengagement is delivered by Default Duration learning (US-F04) - the policy carries no session-length lever, so the mapping does not fabricate one.
US-F04 (`Services/Programmer/DefaultDurationLearning.swift`, `Services/Programmer/PolicyNote.swift`) adds the Programmer's two honesty pieces - both pure, deterministic, and left to be wired by the re-weighting service (US-F03), the same way US-F01/US-F02 provide pure logic no service yet calls.
`DefaultDurationLearning.learned(_:completing:)` (and its `from recentLogs:` convenience) folds newly-completed session `durationMinutes` - what the user *finishes*, never what they *request* (US-D02) - into `User.Duration.completedDurationEWMA` (a `smoothingFactor` 0.3 EWMA whose prior is the existing average, else `onboardingSeedMinutes`, so learning is anchored to the stated preference until real evidence accrues), then sets `defaultMinutes = snappedToChip(ewma)` against the ascending `chipValues` (5/10/15/20/30/45/60), a boundary snapping to the shorter/gentler chip; `onboardingSeedMinutes` is never mutated and empty input is a no-op, so a user requesting 20 but completing ~12 drifts the default to 10/15 while the caller (US-F03) avoids double-counting by folding each session once.
`PolicyNote.templated(policyBefore:policyAfter:durationChange:)` builds the user-visible `SessionPolicy.Note` (always `source == .template`) from the **real diff** - the direction `progressionRate`/`varietyWindow` actually moved plus any Default Duration change - never from the trigger's intent: it names a stepped-up challenge / eased intensity and fresher / familiar movement only when the levers moved that way, appends the new go-to duration only when it changed, returns `nil` when nothing observable changed (the honest outcome), and never invokes the user's `why` (no hollow motivation callback), so the note can only ever claim a change the sessions actually reflect.
US-F03 (`Services/Programmer/DeterministicSessionPolicyService.swift`, `Persistence/CoreDataSessionPolicyStore.swift`, `DI/ServiceContainer.swift`) completes Epic F with the real, persistence-backed re-weighting service that finally composes the Programmer's four pure pieces (US-F01 detection, US-F02 diagnosis + lever mapping, US-F04 Default Duration learning + templated note) into the `SessionPolicyServiceProtocol` the client will call on open.
`currentPolicy(for:)` reads the last-written policy from an injected `SessionPolicyStore` (or `SessionPolicy.default` until one exists), and `dueTriggers(user:recentLogs:asOf:)` adapts US-F01's library-taking detection to the protocol by supplying the validated catalog from the exercise service.
`reprogram(user:recentLogs:trigger:)` writes a fresh policy: it moves the optimization levers for the trigger (`physical_stall`/`disengagement` re-diagnose the freshest history through `PlateauDiagnosis.diagnose` and apply `reweighted`, so **Trigger Precedence holds end-to-end** - a stall trigger can never accelerate a now-disengaging user; `return` seeds `reentry.rampSessionsRemaining` to `ReturnOverride.rampSessions` = 3 and touches no optimization lever, discipline overriding optimization; `weekly_boundary` moves no lever on its own), folds Default Duration learning over only the non-Return sessions completed after the current policy's `updatedAt` (the `updatedAt` cutoff is a dedup boundary so the EWMA folds each session exactly once across re-programs - `default`'s 2001 sentinel epoch folds the full first history - and `wasReturn` sessions are excluded because their deliberately short, capped duration (US-E06) would drag the learned default down and soft-penalize a celebrated comeback), stamps `version += 1` / `updatedBy == .deterministic` / `updatedAt = trigger.detectedAt` (time injected, never the wall clock), attaches `PolicyNote.templated(...)` built from the real before/after diff (so the note can only claim a change the sessions reflect, and is `nil` when nothing observable moved), and persists the learned `user.duration` first (via `UserServiceProtocol`, only when it moved, since policy and user are separate aggregates) then the policy (via the store), so the policy's `updatedAt` dedup boundary only advances after the duration derived from those sessions is durably saved. The service **never generates or returns a workout**; it only writes policy. Persistence is a `SessionPolicyStore` protocol with an `InMemorySessionPolicyStore` (tests/previews/the mock container) and a `CoreDataSessionPolicyStore` (the running app, backed by `CDSessionPolicy` so the policy survives relaunch and offline use, US-J02 will sync it); the real service is wired into `ServiceContainer.mock()` in place of `MockSessionPolicyService` (the one-line swap), returning the default until it has written a policy so pre-programming behavior and `ServiceContainerTests` are unchanged. With US-F03 done the Epic F AI Programmer is complete; the on-open *client invocation* of `dueTriggers`/`reprogram` lands with the Ready Screen (US-J02).
The v6 Epic G (cold start & Variety Language) has begun: US-G01 (`Models/SessionPolicy.swift`) adds the deterministic cold-start Starting Difficulty seeding onboarding will call - `SessionPolicy.ColdStartContract.cappedMaxDifficulty(for:)` maps the self-reported `profile.fitnessLevel` to a provisional capped band (beginner 2, intermediate 3, advanced 4), deliberately at or below the steady-state `ExercisePoolFilter.difficultyCap` (it tightens only the advanced user, 5 -> 4) so an over-rated self-report still gets a winnable first day; `ColdStartContract.seeded(for:)` builds the onboarding contract (First-Week Contrast forced on for US-G02, the level's cap for US-G01) and `SessionPolicy.seeded(forFitnessLevel:)` layers exactly that contract onto the neutral `SessionPolicy.default` and moves no other lever, so once cold-start retires (US-G04 clears the contract) the engine behaves exactly as `default`. The engine already serves the gentle end of the capped band (US-E04's `ColdStartOverride.cappedPool` + Step 5's no-history entry-tier selection) and leaves upward correction to the Asymmetric Ramp (US-E05), so US-G01 is purely the write-side seed the onboarding flow (US-I01) will call with `profile.fitnessLevel`; the pure mapping/factory and the end-to-end gentle-end selection are both under test.
US-G02 (`RepTodayTests/FirstWeekContrastTests.swift`) lands the First-Week Contrast enforcement contract: the *mechanism* is US-E04's Step 0 `ColdStartOverride` pillar rotation and the *switch* is the `coldStartContract.forceContrastSpread` flag US-G01's `seeded(for:)` sets true, so US-G02 is purely the seam between them - proving the seeded flag actually produces a visible strength/mobility/primal spread across the first week with no pillar repeating back-to-back, even for a `sitsLong` desk worker whose staleness bias alone would collapse the week to all-mobility.
The new suite owns that contract at three altitudes: the pure `ColdStartOverride` rule in isolation (the desk-worker rotation spans all three pillars with no back-to-back repeat, and the flag is the on/off switch - off, a mobility-biased plan passes through untouched; the blend path alternates its lead too), the real onboarding seed end-to-end through `SessionAssembly.assemble` (the PRD validation, driven by the actual `SessionPolicy.seeded(forFitnessLevel:)` rather than a hand-built policy: a desk-worker beginner's first four sessions span >= 2 pillars, no back-to-back), and a control proving the spread is produced by the contract, not staleness (the same desk worker on the neutral `SessionPolicy.default`, no contract so Step 0 is a no-op, collapses to all-mobility every day). No production code changed - US-E04 and US-G01 already carried the enforcement; US-G02 verifies and owns it.
US-G03 (`Services/Language/VarietyLanguage.swift`, `Services/Language/VarietyLanguageResolver.swift`) adds the Variety Language slice - the short, honest line that names what today is and how it differs from yesterday ("Today's a mobility day - yesterday was strength"), so a new user feels the variety wedge from session one.
`VarietyLanguage` is the deterministic template and the single source of truth for the contrast: it reads the lead pillar straight off the engine's own output - `leadPillar(of:)` takes a `Workout`'s `focusPillar` (else its first training block, since blocks are ordered staler-pillar-first) and a `WorkoutLog`'s `focusPillar` (else, for a blend, the most-worked non-skipped pillar, ties broken by canonical pillar order) - so the line can only ever name a contrast the engine *actually produced*: the "yesterday" clause is kept only when the prior session genuinely led with a different pillar, the first session is only named (no fabricated yesterday), and a degenerate warm-up-only session yields no note; it never references the user's `why` (no hollow callback). `templatedNote(...)` returns a `SessionPolicy.Note` always `source == .template`, offline-safe and non-blocking.
`VarietyLanguageResolver` composes the optional LLM upgrade over that template via the `VarietyLanguageProvider` seam (fulfilled by the thin stateless proxy, US-N05, deferred - no provider ships in the MVP, so every note is template-sourced): it attempts the LLM at most once and only when the user is cold-start-active, the network is reachable, and a provider is wired, marking a successful line `source == .llm`; on *any* failure - offline, no provider, warmed-up user, a thrown error/timeout, or an empty line - it falls back to the template `Note`, so the app never blocks and never shows a blank. The pure logic and the fallback/LLM paths are both under test; the on-open wiring into the Ready Screen landed with US-J02 (`ReadyViewModel` resolves the note through this template-only resolver on load).
US-G04 (`Services/Engine/ColdStartHandoff.swift`) closes Epic G's cold-start arc with the **cold-start handoff** - the one-way retirement that hands a warmed-up user off from the Step 0 overrides (US-E04) to the self-driving engine once there is enough history for staleness and Adaptive Overload to steer unassisted. It is a thin, pure bookkeeping step (like US-F04's pieces, no service yet calls it; the post-session log-writer US-L01 will) touching exactly the two pieces of state Step 0 reads: `advanced(_:)` increments `user.coldStart.sessionsLogged` on each completed session and flips `active` off the moment the count reaches `handoffThreshold` (5, tunable; v6 range 5-7) - a **one-way** retirement (once inactive the state is frozen, a no-op on further sessions, so a later gap is the Return override's job, not a cold-start relapse); `reconciled(_:with:)` clears `sessionPolicy.coldStartContract` to `nil` once cold-start is inactive (a no-op while active or already absent, and it moves no other lever - the version/`updatedBy` are the Programmer's, so clearing the contract is a mechanical retirement, not a re-program), leaving the policy in exactly `default`'s cold-start shape so Step 0 (gated on *both* the `active` flag and the contract) is a no-op and the engine runs the plain US-E03 pipeline from the sixth session on; and the combined `afterCompletedSession(user:sessionPolicy:)` advances the user then reconciles the policy against that *advanced* state, returning both aggregates (persisted separately, as US-F03 does) as a `ColdStartHandoff.Outcome`. Pure and deterministic (the "how far into cold start" signal is the logged-session count, no wall clock); with US-G04 done Epic G is complete.
The v6 Epic H (real Consistency & Phase evaluators) has begun: US-H01 (`Services/Consistency/ConsistencyScore.swift`, `DI/ServiceContainer.swift`) replaces `MockConsistencyService` with the real forgiving, Return-protected Consistency Score - the deterministic, on-device measure of *showing up* that has no streak to break and no single miss that zeroes it.
The pure `ConsistencyScore.evaluate(logs:weeklyGoal:asOf:calendar:)` computes a weighted rolling average of weekly adherence: `weeklyAdherence(week) = min(1, workoutsCompleted / weeklyGoal)` (any completed log is one show-up, so a 5-minute session counts fully and duration never enters adherence), and `score = weightedRollingAverage(adherence over the last recentWeeksWindow = 8 weeks) * 100` with a linear recency weighting so recent weeks weigh more; `weeklyGoal` defaults to `defaultWeeklyGoal` = 3 and is clamped to >= 1.
Two forgiveness rules keep it honest without being punitive: the window starts no earlier than the user's first activity (a new user is never penalized for the empty weeks before they opened the app), and **a Return (US-E06) protects the Score** - the empty gap weeks a Return closed are excused from the average (an empty week is excused only when the nearest active week just after it is a Return week, tying each excusal to exactly the comeback that ended it) and the comeback week itself is credited as a full show-up, so a Return can only preserve or rebuild the score, never reduce it (PRD validation).
`longestChain` is the longest run of consecutive on-goal weeks (goal met, or a celebrated Return week) *ever* achieved across all history - a historical maximum, so a later break lowers only the unsurfaced current run and never the number surfaced as earned pride.
An empty history yields a zeroed `Consistency`.
The evaluator is a pure function of its inputs (`asOf` injected, no wall clock); the concrete `ConsistencyScoreService` (the real `ConsistencyServiceProtocol` implementation, wired into `ServiceContainer` in place of the mock) reads the clock once at the boundary and delegates every computation to it.
The visible score copy stays identity-framed ("you're someone who moves"), never loss-framed, per the `Consistency` model contract; the Progress-view strings land with that view.
US-H02 (`Services/Consistency/PhaseEvaluator.swift`, `DI/ServiceContainer.swift`) replaces `MockPhaseService` with the real, deterministic, never-user-selectable `PhaseEvaluator` - the rule that decides whether a user has *earned* the Strength Phase, so Strength is a consequence of demonstrated behavior, never a setting.
The pure `PhaseEvaluator.evaluate(logs:weeklyGoal:library:asOf:calendar:)` returns `.strength` only when **both** independent signals hold, a clean deterministic AND: **sustained consistency** - the forgiving Consistency Score (US-H01) is at or above `consistencyThreshold` (80) *and* the user has been active across a full `sustainedWeeks` window (`= ConsistencyScore.recentWeeksWindow`, 8), so a single hot week (which the window-starts-at-first-activity rule would score at 100) can never masquerade as sustained consistency - and **foundational competence** - the user has cleared the entry tier of every foundational movement pattern (`push`/`squat`/`hinge`/`core`), where "cleared" reuses the exact `AdvancementCriteria` mechanism Step 5 uses to advance a chain (a logged, non-skipped performance met the entry movement's advancement criteria), and a pattern with several chains needs only one chain's entry cleared.
Consistency-only, competence-only, and a fresh user all resolve to `.discipline`; the promotion is framed to the user as a gradual, honest ramp (stewardship of a built habit, never failure), but that framing lives in the surfacing copy - the evaluator is pure over its inputs (`asOf` injected, no wall clock).
The concrete `PhaseEvaluatorService` (the real `PhaseServiceProtocol`, wired into `ServiceContainer` in place of the mock) reads the clock once and supplies the validated library from the exercise service, delegating every decision to the pure evaluator.
With US-H02 done both Epic H evaluators are real; all MVP users still resolve to `.discipline` until they earn Strength.
The v6 Epic I (onboarding) has begun: US-I01 (`ViewModels/OnboardingViewModel.swift`, `Views/Onboarding/OnboardingView.swift`, `ViewModels/ReadyViewModel.swift`, `Views/Ready/ReadyView.swift`, `Services/Protocols/ServiceProtocols.swift`, `Services/Mock/MockServices.swift`, `Services/Programmer/DeterministicSessionPolicyService.swift`, `Views/RootView.swift`) adds the minimal v6 onboarding flow, ending by opening the Ready Screen with the first session already generated (no picker to clear).
The `@Observable` `OnboardingViewModel` drives a short step flow (welcome -> basics -> fitness level -> why -> lifestyle -> duration) collecting exactly what the engine needs - name and basic profile, `fitnessLevel`, the free-text `why`, `sitsLong`, optional injuries, and one duration question that seeds both `duration.defaultMinutes` and `duration.onboardingSeedMinutes`; on `finish()` it builds the `User` (a fresh `coldStart`, `phase == .discipline`, `primaryGoal` defaulted to `.stayActive` since v6 uses `why`), saves it via `UserServiceProtocol`, then seeds and persists the cold-start policy via the new `SessionPolicyServiceProtocol.seedInitialPolicy(for:)` seam (`SessionPolicy.seeded(forFitnessLevel:)` layering the US-G01 capped Starting Difficulty and forced First-Week Contrast onto the neutral default, written whole to the policy store so Step 0 (US-E04) applies from session one - a *seed*, not a re-program, keeping `default`'s `version`/`updatedBy`).
The offered injuries are a closed `InjuryOption` vocabulary whose engine tags each normalize onto an `InjuryContraindication` key, closing the pool filter's documented fail-open gap (an unrecognized injury tag would silently disable protection), guarded by `OnboardingInjuryVocabularyTests`.
On completion `RootView` flips `AppState` to onboarded and routes into the main tabs, whose Today tab is the minimal Ready Screen: the `@Observable` `ReadyViewModel` loads the current user and in-force policy on appear and asks the sub-100ms on-device engine for a complete session at the learned Default Duration, and `ReadyView` renders its blocks and capacity-relative targets with one always-enabled, visually dominant "Start" (never gated); the duration chip + instant regeneration (US-J01), the Variety Language / Consistency / note / re-program-on-open (US-J02), and the player Start opens (US-K01) build on this same view/view model, all `Theme`-tokened with no XP/levels/badges.
The app root (`RepTodayApp`) injects the CoreData view context, the `ServiceContainer` (via `\.services`), and `AppState`; the CoreData stack is local-only until CloudKit sync lands in US-N02.
The v6 Epic J (Ready Screen) has begun: US-J01 (`ViewModels/ReadyViewModel.swift`, `Views/Ready/ReadyView.swift`) adds the non-blocking duration chip to that same Ready Screen - the app opens straight onto a complete pre-generated session and the duration becomes a one-tap adjustment, never a gate before Start.
`ReadyViewModel` now tracks a `selectedMinutes` (seeded from `duration.defaultMinutes` on the first successful `load()` only - a re-appear refreshes the engine inputs but preserves the user's in-session chip choice rather than snapping back to the default - and distinct from the persisted learned default it never rewrites) and offers the canonical ascending `durationChips` (`DefaultDurationLearning.chipValues` = 5/10/15/20/30/45/60); `load()` caches the engine inputs (`recentLogs`, `policy`) so `selectDuration(_:)` regenerates the session in place through the shared `generate()` path without re-fetching the user/logs/policy - keeping a chip tap on the sub-100ms on-device engine, well inside the budget - and re-selecting the current chip or tapping before a user has loaded is a no-op.
`generate()` captures the requested minutes before the engine await and drops its result if the selection has since changed, so a superseded slower generation (an older chip tap still in flight) can never overwrite the session the latest selection produced; and if a chip tap's regeneration throws, `selectDuration(_:)` rolls the selection back to the previous chip (only when that tap is still current, so a later tap's choice is never clobbered) and keeps the still-displayed session rather than surfacing an error the session screen never renders while a workout is present.
`requestedMinutes` is retained as an alias for `selectedMinutes` (it is exactly what the engine and log record as requested minutes).
`ReadyView` renders a horizontal, capsule-styled `DurationChip` row between the "Today: N min" header and the block list, each chip tap firing `selectDuration` inside a `Task`; the existing session and the always-enabled, visually dominant Start stay put through the swap (Start is never disabled waiting for an answer), touch targets meet the 44pt minimum, chips carry VoiceOver labels and `.isSelected`, and there is no XP/levels/badges - all `Theme`-tokened.
US-J02 (`ViewModels/ReadyViewModel.swift`, `Views/Ready/ReadyView.swift`) completes the Ready Screen's felt-personalization surfaces and wires the on-open re-program.
`ReadyViewModel.load` now, after the session is generated and rendering (so Start is interactive from that moment - the view prioritizes the workout over the loading state, and every surface below is best-effort and never gates it), computes three read-only surfaces: `varietyNote` (the Variety Language line, US-G03 - resolved through the template-only `VarietyLanguageResolver`, the on-open wiring the resolver was built for; it names the honest contrast between today's lead pillar and the previous session's, `nil` for a degenerate warm-up-only session, and it is recomputed inside `generate()` so a duration chip that shifts today's lead pillar refreshes the line rather than leaving a stale contrast), `consistency` (the forgiving, identity-framed Consistency Score, US-H01, read via `ConsistencyServiceProtocol` over the *full* log history - not the bounded engine `recentLogs` window, since the surfaced `longestChain` is a historical maximum a run older than the lookback must still count toward - computed once per open, a `try?` best-effort that leaves the surface `nil` rather than failing the session), and `policyNote` (a computed read straight off the in-force `policy.note`, so it can only ever name a change the sessions actually reflect).
The **re-program-on-open** fires once per app open (guarded by `hasCheckedReprogramOnOpen` so a tab re-appear never re-fires it and repeatedly bumps the policy version): a fire-and-forget `reprogramTask` calls `dueTriggers(user:recentLogs:asOf:)` and, if any is due, `reprogram(...)` against the highest-precedence one - the app renders from the *existing* policy now and the newly written policy applies on the next open (US-F03). The task is never awaited by the UI (only tests await `reprogramTask` to assert it ran), and any failure is silent, so the user never waits on the Programmer or the deferred LLM slice.
`ReadyView` folds the Variety Language line into the header subtitle (falling back to a neutral line when there is none), renders a `ConsistencyCard` (identity-first copy - "You're someone who moves." plus the longest on-goal run as earned pride, the numeric score shown only once there is history so a fresh user is never greeted with a discouraging zero; no streak-to-break, no loss-framing) and a `PolicyNoteCard` (a light accent callout shown only when a note exists), between the header and the duration chips - all `Theme`-tokened with VoiceOver labels and no XP/levels/badges.
With US-J02 done the Ready Screen is feature-complete.
The v6 Epic K (active session) has begun: US-K01 (`ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Views/Ready/ReadyView.swift`) adds the active-session player the Ready Screen's Start opens - a focused, one-exercise-at-a-time screen so the user never loses their place mid-session.
The `@Observable` `ActiveSessionViewModel` flattens the workout's blocks into a single ordered list of `Step`s (warm-up -> training -> cooldown, each tagged with its block and 1-based overall position) and tracks exactly where the user is: the current step, the set they are on, and the elapsed time.
`completeSet()` records the current set's prescribed target into `completedSets` (keyed by prescription id) and advances - to the next set of the same exercise, then to the next exercise, then to the completion state on the final set; `skipExercise()` advances without recording sets, discards any sets already recorded for the exercise (a skip means abandoning it entirely, so a skipped exercise never carries completed sets into the log), and flags it in `skippedStepIDs` (the plain "move past it" path; swapping to a peer is US-K03); and `loggedExercises()` builds the per-exercise `LoggedExercise` rows (completed sets, pillar/pattern inline, skip flag) that the post-session writer (US-L01) will persist - this story records *toward* the log but never writes it.
Elapsed time is derived from an injected clock rather than an internal ticking counter: `start()` captures `startedAt` once (idempotent, so a re-appear never resets it) and the pure `elapsed(asOf:)` measures whole seconds to a passed-in date, frozen at `finishedAt` once complete, so the view re-reads it once a second through a `TimelineView` (accurate, backgrounding-resilient) and the tests drive it deterministically with no real time passing.
`ActiveSessionView` renders the current exercise's block context, an auto-playing demo, its target (reps or hold seconds), and set tracking (a filled dot per completed set), with the elapsed clock always visible (removed again by US-O03, below - the clock is still measured, just no longer displayed mid-session) and every interactive control at the 60pt active-screen touch target; `ReadyView`'s Start now sets `playingWorkout` and presents the player in a `.fullScreenCover`, and a minimal completion state (checkmark + "Done") closes the loop until US-L01 replaces it with the real celebration/summary.
The demo medium is `ExerciseDemoView`: it plays a bundled Lottie animation (looping, auto-play) when the exercise's optional `animationName` resolves to a shipped file, otherwise a movement-appropriate SF Symbol (chosen by the exercise's movement pattern) that pulses continuously via `.symbolEffect(.pulse, options: .repeating)`; under Reduce Motion the animation holds a static first frame and the symbol drops its pulse for a static glyph (the required accessible fallback), keeping the same auto-play + static-fallback contract. The Lottie SPM dependency (`airbnb/lottie-ios`, wired in `project.yml`) and the `Exercise.animationName` seam land in US-O01, but no animation files ship yet, so every exercise currently falls back to its symbol and a named-but-missing file falls back too - no demo is ever blank.
No animation ships yet. `Resources/push_standard.json` was the US-O01 validation fixture; its provenance was never recorded, and holding it out of Copy Bundle Resources only addressed the risk of shipping it inside the binary - the file was still committed to a public repository, which is redistribution in its own right. Since the provenance could not be reconstructed it was **deleted** rather than kept as a repo-local fixture, and `docs/asset-attribution.md` records the removal so the ledger stays an honest account. Nothing depended on it: no catalog entry ever named it, and `ExerciseLibraryTests.testEveryAnimationNameResolvesToABundledFile` remains the gate - wiring an `animationName` to an asset the bundle does not carry fails the suite, so the next animation must arrive with a cleared license row first.
All copy is identity-framed ("You showed up. That's the whole game."), every token comes from `Theme`, and there is no XP/levels/badges; the in-session swap (US-K03) and background/resume (US-K04) build on this same view/view model.
US-K02 (`ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Utilities/RestTimerFeedback.swift`) adds the **rest timer with haptics** so the user paces the work without watching the clock.
`completeSet()` now opens a rest for the just-completed prescription's `restSeconds` (none after the final set of the session, none when `restSeconds == 0`, and any lingering rest is ended when the next set is logged - a no-op in the overlay-gated UI); the countdown is derived from the same injected clock as elapsed time via the pure `restRemaining(asOf:)`, but with **pause semantics** (scheduled against a wall-clock deadline, with the remainder captured while paused) so it can be tested deterministically and never blows past - US-O03 lifts that mechanism into the shared `Countdown` value type both timers now run on, so the named fields here are its `deadline`/`remainingWhenPaused` rather than the view model's own.
The player's per-second timeline calls `completeRestIfElapsed(asOf:)`, which ends the rest (revealing the already-advanced next set) and fires the completion cue exactly once at zero - never before, never per-tick, never while paused; the cue is a `RestTimerFeedback` seam whose real `SystemRestTimerFeedback` fires a `.success` `UINotificationFeedbackGenerator` haptic plus a short system sound as the accessible **audio alternative**, and tests inject a spy that counts firings.
The user can `skipRest()` (ends it without the cue - the user chose to move on) or `extendRest(by:)` (adds 15s to both the remaining countdown and the ring total), and `pauseRest`/`resumeRest` (wired to `scenePhase` in the view) freeze and reschedule the remainder across backgrounding - the elapsed session time (US-K01) intentionally keeps running as wall-clock time (still true after US-O03 removed its display: the measurement is what the completion summary's total reads).
The focused `RestView` overlay (a ring countdown + "next up" preview + extend/skip controls) replaces the player while resting, fully `Theme`-tokened with 60pt touch targets and VoiceOver labels and no XP/levels/badges; `skipExercise()` also drops any active rest.
The CoreData stack is local-only until CloudKit sync lands in US-N02.
US-K03 (`ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Views/Ready/ReadyView.swift`, `ViewModels/ReadyViewModel.swift`) adds the **in-session swap** so one movement the user can't or won't do never derails the session.
`ActiveSessionViewModel.swapCurrentExercise()` runs the current slot through the injected `WorkoutEngineProtocol.swapExercise` seam (the deterministic US-C08 `ExerciseSwap.swap` behind the mock engine), which substitutes within the same pillar, pattern, difficulty band, and time budget and never duplicates a movement already in the session; the request is built from a `snapshotForSwap()` of the *current* lineup (every step's prescription in one collapsed block), so a movement swapped in earlier is visible to the engine's duplicate check and a later swap can never re-introduce it or a still-present original.
On `.substituted` the current slot is replaced in place - the substitute carries a fresh `AdaptiveOverload` target, the original slot's rest, and the set count the engine sized it at, the set counter resets to 1 (which is also what keeps a substitute with fewer sets than the slot the user is part-way through from stranding them past its end), and any sets already recorded for the replaced movement are discarded (the exercise is being abandoned, mirroring a skip, so it never carries stale sets into the eventual log); on `.noAlternative` the original slot stays and `noSwapAlternative` flips so the UI shows an honest "no alternative" state rather than an unsafe substitution, the flag clearing when the user advances off the exercise.
The swap dependencies are optional and injected (`swapEngine`/`user`/`recentLogs`), so the player still constructs without an engine (previews) with swap simply unavailable (`canSwap == false`); `ReadyView` threads the engine plus the Ready Screen's already-loaded `user`/`recentLogs` into the `.fullScreenCover`, so a swap adds no fetch.
`ActiveSessionView`'s controls gain a quiet "Swap" action beside "Skip" (disabled mid-request) and the "No safe alternative for this one - it stays in your session." notice, all `Theme`-tokened at the 60pt active-screen touch target with VoiceOver labels and no XP/levels/badges.
US-K04 (`Models/ActiveSessionState.swift`, `Services/ActiveSession/ActiveSessionStore.swift`, `Persistence/CDActiveSession.swift`, `Persistence/CoreDataActiveSessionStore.swift`, `Persistence/RepToday.xcdatamodeld`, `ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `ViewModels/ReadyViewModel.swift`, `Views/Ready/ReadyView.swift`, `DI/ServiceContainer.swift`) completes Epic K with **background and resume** - a text message never costs the user their workout.
The new `ActiveSessionState` is a `Codable` snapshot of the in-progress session - the current lineup as `slots` (reflecting any US-K03 swap, not just the generated blocks), the position (`currentStepIndex`/`currentSet`), completed/skipped work, the session-clock origin (`startedAt`), and the rest timer (US-O03 adds one more field, `hold`, carrying only the side a part-done per-side hold left the user on - never a running leg) - with timing stored as **absolute instants** (`startedAt`, the rest `deadline`) so elapsed time and a running countdown restore correctly across a relaunch that happened at an unknown-later moment, while a rest paused on backgrounding instead carries its frozen `remainingWhenPaused`.
Persistence is an `ActiveSessionStore` protocol (`load`/`save`/`clear`, keyed by the user's id) with an `InMemoryActiveSessionStore` (mock container, previews, tests) and a `CoreDataActiveSessionStore` (running app, backed by the new `CDActiveSession` entity - `userId` + JSON `stateData`, overwritten in place, `toActiveSessionState()` failing loudly on a missing blob) so an abandoned session survives a full relaunch; the mock container wires the in-memory store (consistent with the other in-memory stores, `CoreDataActiveSessionStore` tested in isolation and ready to swap in when the production container lands, US-N02).
`ActiveSessionViewModel` gains the persistence seam: `init(state:)` is the restore-capable designated initializer (the fresh-start `init(workout:)` is a convenience over it), `snapshot()` captures the current play state, and after every meaningful change - `start`/`completeSet`/`skipExercise`/`swapCurrentExercise`/the rest transitions (including `pauseRest`, just before backgrounding) - a fire-and-forget `persist()` writes the snapshot (chained behind the previous write so saves land in call order), or clears the store the instant the session completes; a no-op when no store/userId is wired (previews).
Restoring rebuilds the step list from the snapshot's lineup, keeps the persisted `startedAt` so elapsed time continues from its origin (idempotent `start()` never resets it), resumes a running rest from its absolute deadline and a paused rest from its frozen remainder, and clamps a corrupt/out-of-range index rather than trapping.
`ReadyViewModel` reads the store on open (and re-reads via `refreshResumableSession()` when the player closes) into a surfaced `resumableSession`, and `discardResumableSession()` clears it; `ReadyView` renders a `ResumeSessionCard` ("Pick up where you left off", naming the exact place - "You're on Push-up, exercise 3 of 8.") with Resume (reopens the player at that position through the `.fullScreenCover`, threading the store + userId so a resumed session keeps persisting) and Discard, identity-framed and never guilt-framed, alongside the today session whose always-enabled Start is never gated.
Backgrounding-and-reopen within a run resumes in place from the surviving view model (the wall-clock elapsed clock and `scenePhase`-driven rest pause/resume from US-K01/K02); a full relaunch is carried by the persisted snapshot.
With US-K04 done Epic K (the active session) is complete; Epic L (post-session summary, log write, and rating) is next.
The v6 Epic L (post-session) has begun: US-L01 (`Services/ActiveSession/SessionSummary.swift`, `Services/ActiveSession/SessionCompletionService.swift`, `ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Views/Ready/ReadyView.swift`, `ViewModels/ReadyViewModel.swift`, `DI/ServiceContainer.swift`) adds the **post-session summary and log write** - the wrap-up that records the session and lets the user feel the win.
The player writes the `WorkoutLog` at the `finish()` transition (the single point every finishing path - last set, final skip, a swap that finishes mid-flight - routes through), fire-and-forget through the injected `SessionCompletionServiceProtocol`, so the record is durable even if the user quits on the completion screen: `ActiveSessionViewModel.completionLog()` builds the log with `requestedMinutes`/`shape`/`focusPillar`/`wasReturn` copied straight off the played `Workout` (never re-derived), the actually-completed `durationMinutes` (whole minutes of wall-clock elapsed, floored at 1 - not the requested minutes - and capped at the session's `requestedMinutes`, since the session clock keeps running as wall-clock time while backgrounded and an unbounded value would let a long background stretch inflate the log and drift Default Duration learning's EWMA upward), and the per-exercise `completedSets`/`skipped` rows from `loggedExercises()`; the perceived-difficulty rating stays `nil` (US-L02 collects it).
The real `SessionCompletionService` composes the post-session bookkeeping over protocol seams (testable without CoreData): it saves the log, re-reads the *freshest* persisted user rather than read-modify-writing the caller's open-time snapshot (the on-open reprogram, US-J02/US-F03, is a second writer of the user aggregate and may have persisted a learned `user.duration` since the snapshot was captured; the stale snapshot would clobber it), then refreshes the forgiving Consistency Score (US-H01) onto the user aggregate over the *full* persisted log history - which now includes the just-saved log, so the new session counts exactly once and the all-time `longestChain` (a historical maximum) is never understated by a bounded window - and drives the cold-start handoff (US-G04, its single caller) - advancing `user.coldStart.sessionsLogged`, retiring cold-start at the threshold, and persisting the reconciled policy (clearing the now-inert `coldStartContract`) only when the handoff actually changed it, over the same `SessionPolicyStore` the Programmer writes through.
It deliberately does **not** fold Default Duration learning (US-F04) - that stays the Programmer's job (US-F03), which folds each completed session's `durationMinutes` into the EWMA exactly once against the policy's `updatedAt` dedup boundary; US-L01 only makes sure the log carries the completed duration for that to read.
The pure `SessionSummary.from(loggedExercises:durationMinutes:)` builds the template summary the celebration screen renders - duration, completed-set count, and the muscle/mobility coverage (covered pillars and movement patterns, de-duplicated in canonical order) derived only from non-skipped exercises with a recorded set, so it can never over-claim a movement the user passed on; `ActiveSessionView.completionState` leads with the identity-framed show-up ("You showed up. That's the whole game.") and renders that summary, all `Theme`-tokened with no XP/levels/badges.
`ReadyView` threads `services.sessionCompletionService` into the player, and `ReadyViewModel.handlePlayerDismiss(completed:)` re-reads the user and full log history to refresh the displayed Consistency Score on return, so the win shows without waiting for the next genuine open; `ServiceContainer` shares a single policy store and log/user/consistency services between the Programmer, the Ready Screen, and the completion recorder so a written session is the same history everyone reads back.
US-L02 (`ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Services/ActiveSession/SessionCompletionService.swift`) adds the **perceived-difficulty rating** - the one-tap "how did that feel?" that lets tomorrow's session adjust to the user.
The completion log is now built once at the `finish()` transition and kept as `completedLog` with a **stable id**, so the rating updates that same durable record (US-L01) rather than writing a second, un-linked log: `ActiveSessionViewModel.rate(_:)` sets the surfaced `perceivedDifficulty`, mirrors it onto `completedLog`, and persists it through the new `SessionCompletionServiceProtocol.recordPerceivedDifficulty(_:forLog:)` - a minimal, idempotent re-save of the one log by its id (`save` is an upsert) that deliberately does **not** repeat the Consistency refresh or the cold-start handoff (neither depends on the rating, and re-running the handoff would double-advance `coldStart.sessionsLogged` for a session already recorded).
The rating write is fire-and-forget and **chained behind the initial completion write** (via the shared `completionTask`), so a rating given while the first write is still in flight can never be clobbered - both target the same log id, newest last.
The value lands on the persisted `WorkoutLog.perceivedDifficulty` that `AdaptiveOverload.demonstratedCapacity` reads off the most-recent usable log (US-E05), so `too_hard` eases the next generation's targets and `too_easy` intensifies them within one cycle.
`ActiveSessionView.completionState` renders a three-pill `ratingControl` ("Too easy / Just right / Too hard", easy-to-hard) between the summary card and the always-enabled Done button: tapping a pill records it (and highlights it with the accent), re-tapping overwrites, and hitting Done without a tap leaves the session unrated (`nil`) - the control never gates Done; every pill is `Theme`-tokened at the 60pt active-screen touch target with a VoiceOver label and `.isSelected` trait, and there is no XP/levels/badges.
With US-L02 done the post-session rating loop is closed; Epic M (the Progress tab) is next.
The v6 Epic M (Progress tab) has begun: US-M01 (`Services/Consistency/ConsistencyTrend.swift`, `ViewModels/ProgressViewModel.swift`, `Views/Progress/ProgressTabView.swift`, `Views/RootView.swift`, `Services/Consistency/ConsistencyScore.swift`) replaces the placeholder Progress tab with the **reflection surface** - a session calendar marking completed days, a Consistency Score trend chart (Swift Charts), and the earned `longestChain` surfaced as a point of pride, all identity-framed and never loss-framed (no streak-to-break, no guilt).
`ConsistencyTrend.trend(logs:weeklyGoal:maxWeeks:asOf:calendar:)` is the pure trajectory the chart renders, and there is deliberately **no separate trend algorithm**: each `ConsistencyTrendPoint` is the real forgiving score (`ConsistencyScore.evaluate`, US-H01) recomputed as it *would have stood* at the end of each past week - the same logs evaluated with `asOf` walked back one week at a time - so the chart's right edge always equals the headline number and every forgiveness rule (window-starts-at-first-activity, Return excusal, recency weighting) is honored automatically; it spans from the user's first active week through the current week, oldest first, capped at `maxWeeks` (defaulting to `ConsistencyScore.recentWeeksWindow` = 8, so the chart never shows weeks the score itself has already rolled out of), and an empty history yields no points (a fresh user has no trajectory, which the view surfaces as an encouraging empty state, not a flat line at zero).
To bucket by exactly the same week boundaries the score uses, `ConsistencyScore.weeksAgo`/`startOfWeek` were relaxed from `private` to internal - a single source of truth for "a week" so the trend can never disagree with the headline about which week is which (no behavior change to the evaluator).
The `@Observable` `ProgressViewModel` reads the full `WorkoutLog` history (`from: nil, to: nil` - `longestChain` is an all-time maximum and every logged day should be markable, so it reads everything rather than the engine's bounded recent window), derives the headline `consistency`, the `trend`, and the `completedDays` (start-of-day dates a session finished on), injects a clock (`now`) for deterministic tests, and treats an empty history as a valid populated-vs-empty-state signal (`hasHistory`) - only a missing user profile sets `errorMessage`.
`ProgressTabView` (named to avoid colliding with SwiftUI's own `ProgressView` spinner used elsewhere) renders the headline card, the `ScoreTrendCard` line chart, and the `SessionCalendarCard`, all `Theme`-tokened with no XP/levels/badges; `RootView` swaps the placeholder Progress tab for it. This is a view/view-model + pure-logic story only - `ServiceContainer.mock()` still starts with empty history (real cross-launch persistence is US-N02), so a fresh launch shows the encouraging empty state until sessions are completed. The deeper, premium-gated analytics (pillar balance, chain position, bests) land in US-M02.
US-M02 (`Services/Progress/ProgressAnalytics.swift`, `ViewModels/ProgressViewModel.swift`, `Views/Progress/ProgressTabView.swift`) adds the **legibility layer** - pillar balance, progression-chain position, and personal bests from real history, with a deeper analytics layer gated behind premium (US-N04).
The pure, deterministic `ProgressAnalytics.from(logs:library:phase:asOf:calendar:)` (mirroring `ConsistencyScore`/`ConsistencyTrend`: `asOf` injected, never a wall-clock read) derives every surface from the full `WorkoutLog` history, counting coverage the `SessionSummary` way - only non-skipped exercises with a recorded set, so the tab can never over-claim a movement the user passed on.
The **free** layer is three surfaces: `pillarBalance` (each pillar's share of completed exercises across strength/mobility/primal, summing to 1), `chainPositions` (for each foundational pattern - push/squat/hinge/core, the four the `PhaseEvaluator` gates on - the frontier tier within the *active* chain, i.e. the highest-order worked movement in the most-recently-worked chain, counted only over the tiers the user's earned `phase` can reach so a Discipline user's chain never counts or points to a still-locked Strength tier (mirroring the `PhaseEvaluator` principle, US-H02), with a 1-based tier/length and a `hasNextTier` "next tier in reach" hint, or "not started" when the pattern is untrained), and `personalBests` (total sessions, longest session, busiest week, total minutes, and the best single-set reps / longest hold each naming the movement, ties broken by id for determinism).
The **premium** `deep` layer is the deeper cut: `patternBalance` (finer than pillars - the share across all seven movement patterns, omitting untrained ones), `weeklyVolume` (completed sets and sessions per week over the same `recentWeeksWindow` the score averages, oldest first, as a Swift Charts bar chart), and `difficultyMix` (the too-easy/just-right/too-hard distribution behind Adaptive Overload, with a gentle never-loss-framed calibration read).
`ProgressViewModel` computes the whole analytics struct regardless of entitlement (it is cheap, pure, on-device) and reads the entitlement from `SubscriptionServiceProtocol.currentSubscription().tier` into a surfaced `isPremium`; both are best-effort (`try?`) so a library or entitlement read that fails leaves the free surfaces rendering and simply omits or gates the deep layer rather than failing the tab.
The gate lives at the render boundary: `ProgressTabView` renders `PillarBalanceCard`/`ChainPositionCard`/`PersonalBestsCard` for **everyone** and, in the deep layer's place, shows `DeepAnalyticsSection` only when `isPremium` else a single quiet, non-nagging `PremiumUpsellCard` ("Your workouts are always free - Premium just adds the deeper view") - the core loop and the basic US-M01 history are never gated (US-N04's real StoreKit entitlement drives the same `isPremium` seam later). All `Theme`-tokened with VoiceOver labels and no XP/levels/badges. With US-M02 done the Progress tab is feature-complete.
The v6 Epic N (Apple-native integrations) has begun: US-N01 (`Services/Auth/AppleAuthService.swift`, `Services/Auth/AppleSignInAuthorizing.swift`, `Services/Auth/AuthCredentialStore.swift`, `ViewModels/OnboardingViewModel.swift`, `Views/Onboarding/OnboardingView.swift`) adds the real **Sign in with Apple** identity, private by design and never a gate to the first session.
`AppleAuthService` is a stateless, `Sendable` composition of two seams: an `AppleSignInAuthorizing` (the `ASAuthorizationController` + presentation-anchor + delegate ceremony, bridged to `async`/`await` by a self-retaining `@MainActor` `AppleSignInCoordinator` behind the real `AppleIDSignInAuthorizer`) and an `AuthCredentialStore` (persistence).
`signInWithApple()` runs the programmatic ceremony, persists the returned stable `ASAuthorizationAppleIDCredential.user`, and returns it; `completeSignIn(identifier:)` persists an identifier obtained outside that ceremony (Apple's own `SignInWithAppleButton` runs its own `ASAuthorizationController`) into the same store, so both paths share one identity; `currentUserIdentifier()` is a pure **local** read of the store (no network, no iCloud account) so it resolves instantly and offline; `signOut()` clears it.
Persistence has two implementations behind the protocol: `KeychainAuthCredentialStore` (production - the identifier lives in the Keychain, so it survives reinstall, is encrypted at rest, and never leaves the device) and `InMemoryAuthCredentialStore` (tests/previews/the mock container); `AppleAuthService.live()` assembles the Keychain-backed production service, ready to swap into the production `ServiceContainer` when it lands (US-N02), while `mock()` keeps `MockAuthService` so the suite stays Keychain-free and deterministic.
Identity is wired into onboarding **without gating**: `OnboardingViewModel` takes an optional `authService` and the welcome step offers a non-gating affordance rendered as Apple's official `SignInWithAppleButton` (App Store Review guideline 4.8 compliance) - its completion routes through `handleAppleSignIn(_:)`, which extracts the stable identifier off a successful `ASAuthorizationAppleIDCredential` and records it via `completeSignIn(identifier:)`; any failure (canceled, offline, missing entitlement) or a non-Apple-ID credential is intentionally swallowed so the flow never blocks (the programmatic `signInWithApple()` path is retained too), and `finish()` keys the saved `User` by the resolved identity (the in-flow sign-in, else a previously-persisted identifier from the store, else a locally-generated fallback), so the first session opens whether or not the user ever signs in.
The live Apple ceremony needs the `com.apple.developer.applesignin` entitlement and a signing team (`DEVELOPMENT_TEAM` is currently empty), so the end-to-end Apple sheet is a device/team-provisioned verification; in the simulator the same protocol path runs through `MockAuthService` and falls back gracefully.
US-N02 (`Persistence/PersistenceController.swift`, `Persistence/CoreDataUserService.swift`, `Persistence/CoreDataWorkoutLogService.swift`, `Persistence/RepToday.xcdatamodeld`, `DI/ServiceContainer.swift`, `App/RepTodayApp.swift`, `RepToday/RepToday.entitlements`, `RepToday/Info.plist`, `project.yml`) turns the local-only CoreData stack into the CloudKit-backed production stack and wires the production `ServiceContainer` over it, so a user's identity, history, and in-force policy sync and back up across devices while the core loop stays fully offline-capable.
`PersistenceController` splits the `NSPersistentCloudKitContainer` into two persistent stores in one container, keyed by the two model configurations: a **Cloud** store (`Cloud` config - `CDUser`/`CDWorkoutLog`/`CDSessionPolicy`) that mirrors to the user's private CloudKit database (`iCloud.com.reptoday.app`) with persistent-history tracking and remote-change notifications, and a device-local **Local** store (`Local` config - `CDActiveSession`) that never syncs because an in-progress session is transient, device-bound state (absolute wall-clock instants tied to this run) with no meaning on another device.
Sync is strictly additive: on any CloudKit setup failure (no iCloud account, offline, or an unsigned build with no entitlement) the Cloud store is retried **local-only** (the cloud options cleared and the store re-added through the coordinator) so the app always opens and every session is generated/recorded on-device; a *local* store that fails to load is recovered the standard CoreData way - the store file is destroyed and a fresh empty one re-added, since a lost in-progress session is disposable - and only escalates to a loud `fatalError` if that recreate also fails, and the in-memory test/preview path (`inMemory: true`) still loads every entity into a single `/dev/null` store with no CloudKit, so the existing suite is untouched.
The new CoreData-backed `CoreDataUserService` and `CoreDataWorkoutLogService` (`UserServiceProtocol`/`WorkoutLogServiceProtocol`) upsert by id (never duplicating on re-save - e.g. the US-L02 rating update), answer the half-open `[start, end)` date-range query the engine/Consistency/Progress read, and do every access inside `context.perform`; they join `CoreDataSessionPolicyStore`/`CoreDataActiveSessionStore` in the production `ServiceContainer.live(context:)` factory, which wires the same real services `mock()` uses (deterministic Programmer, Consistency Score, Phase evaluator, session-completion recorder) over one shared `viewContext` and one shared policy store, plus the real Keychain-backed `AppleAuthService.live()` (US-N01); HealthKit and Subscription stay mocked until US-N03/US-N04.
`RepTodayApp` swaps `ServiceContainer.mock()` for `.live(context:)`; a hand-written `RepToday.entitlements` requests the iCloud CloudKit container, Sign in with Apple, and the APNs push environment (`aps-environment`) CloudKit needs (wired via `CODE_SIGN_ENTITLEMENTS`), and a partial `Info.plist` merged with Xcode's generated keys adds the `remote-notification` background mode CloudKit push requires.
Verified in the Simulator end-to-end: the app launches cleanly, CloudKit mirroring sets up successfully (private-DB export activity registered, no background-mode warning), and both `RepToday.sqlite` (carrying the CloudKit `ANSCKEXPORT.../ANSCKIMPORT...` mirroring tables) and the device-local `RepToday-Local.sqlite` materialize in the app container; the graceful local-only fallback keeps the loop working when CloudKit is absent.
Device note: a two-signed-in-instance sync round trip and the live push path need a provisioned team (`DEVELOPMENT_TEAM` is empty), so cross-device verification is device-provisioned; the Simulator exercises store setup, the mirroring stack, and the offline fallback.
US-N03 (`Services/Health/HealthKitService.swift`, `Services/Health/HealthKitWorkoutSample.swift`, `Services/ActiveSession/SessionCompletionService.swift`, `Views/RootView.swift`, `DI/ServiceContainer.swift`, `RepToday/RepToday.entitlements`, `RepToday/Info.plist`, `project.yml`) adds the real **write-only HealthKit integration** - each completed session is mirrored into Health so the user's movement counts where they already track it, and the core loop is never gated.
`HealthKitService` *shares* (writes) an `HKWorkout` (built with `HKWorkoutBuilder`) plus an active-energy sample and never reads Health data, so only `NSHealthUpdateUsageDescription` is required and authorization is requested with `read: []`; the share prompt is requested once up front when the user enters the main app (`MainTabsView.task`) rather than mid-loop, and `saveWorkoutLog` never itself issues a blocking authorization request - it only writes when share access is already granted (a non-blocking status read), so it stays safe to `await` from the completion recorder even in a background/headless context. Every path degrades gracefully - Health unavailable (e.g. iPad), a denied/undetermined authorization, or any write failure all return quietly (the completion recorder also wraps the call in `try?`), so a completed session is never blocked.
Writes are **idempotent per session**: each workout carries the completed log's id as its `HKMetadataKeyExternalUUID`, and a metadata query short-circuits the write when a workout with that id already exists, so a resume, relaunch, or re-save never duplicates the session in Health.
The pure, HealthKit-free `HealthKitWorkoutSample.from(log:user:metValuesByExerciseId:)` (unit-tested without a live `HKHealthStore`) builds the workout's start/end instants, activity kind (mobility-focused -> `.flexibility`, else `.functionalStrengthTraining`), and active-energy estimate via the standard MET approximation `kcal = MET x weightKg x hours` - MET is the mean of the session's *actually-worked* movements (non-skipped, with a recorded set, so a skipped movement never inflates the estimate), resolved from the exercise catalog and falling back to a moderate-calisthenics default when none resolve; the `HealthKitService` shell only performs the HealthKit ceremony.
The write is driven from `SessionCompletionService.recordCompletedSession` (US-L01), the single completion seam, as a fully-isolated final step *after* the durable log write, Consistency refresh, and cold-start handoff, so a HealthKit failure never disrupts the app's own bookkeeping; the service is injected optionally (`nil` in the in-memory mock container, where completion records nothing to Health).
`ServiceContainer.live(context:)` swaps `MockHealthKitService` for `HealthKitService(exerciseService:)` (shared between the container and the completion recorder), `MainTabsView` requests share authorization once on appear, the `RepToday.entitlements` gains `com.apple.developer.healthkit`, and the partial `Info.plist` gains the write usage description (Subscription stays mocked until US-N04).
Device note: a live Health write and the authorization sheet need the HealthKit entitlement and a provisioned team (`DEVELOPMENT_TEAM` is empty), so an end-to-end write is device-provisioned; the Simulator exercises the pure mapping, the completion wiring, and the graceful skip when Health is unavailable.
US-N04 (`Models/SubscriptionPlan.swift`, `Services/Subscription/StoreKitFacade.swift`, `Services/Subscription/LiveStoreKitFacade.swift`, `Services/Subscription/StoreKitSubscriptionService.swift`, `Services/Protocols/ServiceProtocols.swift`, `Services/Mock/MockServices.swift`, `ViewModels/PaywallViewModel.swift`, `Views/Paywall/PaywallView.swift`, `Views/Progress/ProgressTabView.swift`, `DI/ServiceContainer.swift`, `Resources/RepToday.storekit`, `project.yml`, `Models/User.swift`) adds the real **StoreKit 2 subscriptions and paywall** - premium unlocks the depth layer while the free tier stays unlimited forever and the core loop is never gated.
`StoreKitSubscriptionService` composes one seam - a `StoreKitFacade` (the App Store ceremony: `Product.products` / `Transaction.currentEntitlements` / `product.purchase()` / `AppStore.sync()`, behind the real `LiveStoreKitFacade`) - and owns the pure domain mapping from raw store entitlements/products to the app's `Subscription`/`SubscriptionPlan`, so the composition is unit-testable end to end with a stub facade (mirroring `AppleAuthService`, US-N01).
It implements the four PRD methods (`currentSubscription`/`refreshEntitlements`/`purchasePremium`/`restorePurchases`) plus the paywall surface (`premiumPlans()` priced from StoreKit, `purchase(_ plan:)` returning a `PurchaseOutcome` for a selected plan) and a lifetime transaction listener (`startObservingTransactions()`) added to `SubscriptionServiceProtocol` (the listener defaulting to a no-op so mocks stay StoreKit-free); `RepTodayApp` starts it once at launch and retains it so out-of-band transactions - auto-renewals, refunds, cross-device purchases, and deferred Ask-to-Buy approvals - are finished and never linger in the queue, the entitlement-gated surfaces picking up the change on their next read.
`purchase(_ plan:)` returns `.resolved(Subscription)` on a completed purchase or a silent user-cancel (the entitlement simply unchanged) and `.pending` when a purchase awaits external approval (Ask to Buy), so the paywall can reassure the user instead of leaving the Buy tap looking dead; `purchasePremium()` stays the plan-agnostic convenience that buys the monthly plan.
Only **verified** transactions count (an unverified purchase throws `notVerified`, an unverified/revoked/expired entitlement is skipped), transactions are `finish()`ed, and any active premium entitlement grants `.premium` carrying the latest-expiring one's `expiresAt`/trial state; no entitlement is the free tier (`Subscription.free`, a new convenience).
Nothing gates the core loop: the paywall is a dismissible sheet, and a StoreKit failure anywhere resolves to free rather than blocking.
`PaywallView` (`@Observable PaywallViewModel`) is opened from the Progress tab's now-tappable `PremiumUpsellCard`; it prices the monthly (~$7.99, 14-day free trial) and yearly (~$59.99) plans, buys or restores, carries the App Store-required auto-renewal disclosure and the Guideline 3.1.2 Terms of Use / Privacy Policy links (Terms points at Apple's standard EULA; the Privacy Policy URL is a documented placeholder to replace before submission), surfaces a gentle non-blocking message on failure or a still-pending Ask-to-Buy purchase, and on a granted entitlement dismisses itself and refreshes the US-M02 gate (`didUnlockPremium`).
Entitlement drives the existing US-M02 `isPremium` gate unchanged - only the deep analytics layer is gated, never the core loop or the basic US-M01 history.
A local `Resources/RepToday.storekit` config (two auto-renewable subscriptions in one group, monthly carrying the 2-week free trial) is attached to the generated `RepToday` scheme via `project.yml`'s target `scheme.storeKitConfiguration` - and deliberately excluded from the target's sources (`buildPhase: none`) so the placeholder prices/ids never ship in Copy Bundle Resources - so purchases/restores run in the Simulator without App Store Connect.
`ServiceContainer.live(context:)` swaps `MockSubscriptionService` for `StoreKitSubscriptionService.live()`; `mock()` keeps the mock (now paywall-capable - it returns the sample plans and a purchase flips it to premium) so the suite and previews stay off the App Store and deterministic.
New tests: `StoreKitSubscriptionServiceTests` (20, the entitlement/plan mapping + purchase/cancel/pending + restore paths over a stub facade) and `PaywallViewModelTests` (10, load + purchase/restore success/cancel/pending/failure, never a blocking wall).
The full 655-test suite passes and the app boots cleanly in the Simulator with the real service wired.
Device note: the live purchase sheet against App Store Connect products needs a provisioned team (`DEVELOPMENT_TEAM` is empty), so end-to-end sandbox purchasing is exercised via the local `.storekit` config in Xcode; the unit suite covers the mapping, purchase, and restore logic, and the Simulator confirms clean boot + wiring.
US-N05 (`Services/Language/ProxyVarietyLanguageProvider.swift`, `proxy/`) completes Epic N with the **thin stateless proxy** that lets the deferred Variety Language LLM slice (US-G03) run without shipping an API key in the app - the infrastructure the `VarietyLanguageProvider` seam was built against, deploy-ready but deliberately **not wired into the shipping MVP** (the resolver's provider stays `nil`, so every note is template-sourced).
The proxy (`proxy/`) is a Cloudflare Worker (`src/worker.js` + `wrangler.toml`/`package.json`/`README.md`) that holds the Anthropic API key (a Wrangler secret, never in the app) and makes **exactly one** Claude call per request via `fetch` (default `claude-opus-4-8`, override via the `ANTHROPIC_MODEL` var; no extended thinking for a fast single line; an 8s `AbortSignal.timeout` bounds the upstream), turning the day's contrast into one short, honest line whose prompt forbids inventing a "yesterday" that was not supplied; it **stores nothing at rest** - no KV/D1/cache/scheduler and no request-body logging - and receives only the two pillar values plus labels, never user identity/profile/`why`/history, so "stores no user logs" is true by construction, and it returns `200 {"line":...}` or a `4xx/5xx {"error":...}` the client treats uniformly as "fall back".
Because a bare relay to the billed Anthropic Messages API is an open door to quota abuse, the route is gated by a **client shared secret** (`CLIENT_SHARED_SECRET`, a Wrangler secret): when set, the Worker rejects any request whose `Authorization: Bearer <secret>` header does not match with `401 {"error":"unauthorized"}` in constant time *before* it calls Claude, so unauthorized traffic never bills (unset leaves the route open for local `wrangler dev` only); the stateless Worker cannot self-rate-limit, so a Cloudflare rate-limiting/WAF rule on the route is a documented deploy requirement (`proxy/README.md`).
The client half is the pure, `Sendable` `ProxyVarietyLanguageProvider` (fulfilling `VarietyLanguageProvider`): it encodes only `VarietyLanguage.SessionContrast` (the machine `Pillar` raw values + the user-facing labels, `yesterday` omitted when there is no genuine contrast) - never the injected `user` - and POSTs it through an injected `VarietyLanguageProxyTransport` seam (`URLSessionVarietyLanguageProxyTransport` in production, a bounded `timeoutInterval`; a stub in tests) with a short default 4s timeout, attaching the optional injected `sharedSecret` as the `Authorization: Bearer` header when configured, and returns the trimmed line, **throwing on any non-2xx, undecodable body, empty line, or transport failure** so `VarietyLanguageResolver` falls back to the offline template and the app never blocks; the endpoint, wire contract, and one-line wiring are documented in `proxy/README.md` and the provider's doc comment.
With US-N05 done Epic N (the Apple-native integrations) is complete.
The post-v6 Epic O refinements have begun (US-O01, the Lottie exercise-demo seam, is described with the active-session player above; US-O03, the session timer redesign, and US-O04, imperial units in onboarding, are at the end of this document): US-O02 (`Models/SessionPolicy.swift`, `Services/Engine/ColdStartOverride.swift`, `Services/Engine/AdaptiveOverload.swift`, `Services/Engine/SessionAssembly.swift`) adds **fitness-level start seeding** - an active user's first sessions are matched to their self-reported level instead of opening at the library's absolute beginner tier, while the beginner experience is untouched.
`SessionPolicy.ColdStartContract` now carries a **Start Seed** alongside the US-G01 cap: `startingDifficultyFloor` (**beginner 1 / intermediate 2 / advanced 3**) plus the volume seed `startingRepMultiplier` / `startingSets` (**x1.0 & 3 sets / x1.15 & 3 sets / x1.30 & 4 sets**), all filled by `ColdStartContract.seeded(for:)` from `profile.fitnessLevel` (so onboarding's US-I01 `seedInitialPolicy` seeds them with no change of its own). The three fields are additive and decode to neutral values (floor 1 / x1.0 / 3 sets) through a hand-written `init(from:)`, so a policy persisted before US-O02 still loads and reproduces the previous behavior exactly, and the memberwise initializer keeps its pre-US-O02 two-argument form.
The engine reads the seed in the same Step 0 gate as the cap (cold-start active *and* a contract present), so it retires with cold-start (US-G04) and a warmed-up user runs exactly the US-E03 pipeline.
`ColdStartOverride.startBandedPool` bands the **strength and primal** pool to `[startingDifficultyFloor, cappedMaxDifficulty]` under the already-applied cap, so Step 5's lowest-eligible selection for a no-history user starts at the *band entry* rather than the chain's entry tier (an advanced user's first push is a difficulty-3 pike/diamond push-up, not a wall push-up). Mobility is never floored - the warm-up, Movement Practice, and cooldown all draw from it and a stretch's difficulty is not training load - so warm-up/mobility/cooldown are byte-for-byte identical at every level; and the band can never empty a movement pattern: the floor is clamped down per pattern to the hardest movement that pattern actually offers inside the cap.
The floor is tuned against the library a Discipline-Phase user can actually *reach*, not the nominal 1-5 scale: setting each level's floor equal to its own cap collapsed the "band" to a single tier and landed intermediate and advanced on the same movements, so the floor sits a tier beneath the cap instead, which keeps each band a real range and the two pools genuinely different (intermediate opens the whole 2-3 band, advanced only its top).
Sitting the floor beneath the cap was only half of it, because the reachable catalog itself was too short. `ExercisePoolFilter` gates `phase == .strength` out for every MVP user, and the shipped library put every gated skill at difficulty 5 with nothing Discipline-reachable above 3 - so the advanced `[3, 4]` band resolved to seven movements, and five of the six banded patterns offered exactly **one** progression chain inside it. Chains, not movements, are what the variety window rotates over: Step 5 resolves a pattern by picking one exercise per chain, so a single in-band chain hands the user the identical movement every session of the week however many tiers that chain carries. The answer was to extend the *reachable* range rather than weaken the band or unlock the Strength Phase early - earning that phase is a product principle, not a gate to route around. `Resources/Exercises.json` grew from 42 to 57 movements with fifteen zero-equipment Discipline tiers at difficulty 3-4 (archer push-up; cossack and shrimp squats; marching glute bridge and long-lever single-leg bridge; bear hold, hollow rock and tuck L-sit; a whole `pull_horizontal` chain of wall scapular pulls and supine floor rows; a `primal_ground_flow` chain of gorilla walk and lizard crawl; and, closing the last single-tier gap, an underswitch and a bear-hover shoulder tap so `locomotion` finally carries difficulty 4 too), slotted into existing chains where one continued and into new chains for the two patterns that had only one. Phase now gates *skills* rather than tiers - the three gated movements are the library's difficulty-5 summit, and every tier beneath a summit is reachable - and two standing gates hold the band open: `ExerciseLibraryTests.testAdvancedStartBandHasRoomToRotate` keeps at least two in-band *chains* per banded pattern, and `testEveryBandedPatternSpansTwoTiersInsideItsStartBand` at least two in-band *tiers*, so a `[floor, cap]` label can never sit on what is really a single difficulty.
The Zero-Equipment Floor is about the *room*, not just the kit, so four of those first thirteen were re-authored: a doorway row, two table rows and a Nordic curl negative all satisfied `equipment == []` on a technicality while quietly requiring a door frame, a table that would hold body weight inverted, or anchored ankles. They are replaced at the same difficulties by movements that need only a floor and a wall - a wall scapular pull, a supine floor row and its single-arm progression, and a long-lever single-leg bridge - so `apartmentFriendly: true` is a fact the library states rather than a flag a test had to certify.
Banding also left a cliff on the far side of the handoff, and that one is an engine flaw rather than a content gap.
A chain the band withholds is never prescribed, so it accrues no history; the session `ColdStartHandoff` clears the contract, its untouched entry tier is the only candidate the variety window has never seen, and freshness alone would hand an advanced user a difficulty-1 wall sit.
Three rounds of trying to fix it with a difficulty *floor* in Step 5 established that the approach was structurally wrong rather than merely mis-implemented: a floor is a lower bound, while the Return override, the `too_hard` de-escalation, and the skip escape hatch are all upper bounds, so putting them on the same dial produced a fresh collision every round - the floor inverted the Return cap, then permanently retired whole progression chains once a pattern reached difficulty 4, then anchored itself to an onboarding self-report nothing ever revises, then switched itself off on a routine skip.
The fix is to remove the conflict instead of refereeing it.
Freshness has exactly one blind spot - it cannot tell "the user outgrew this" from "the engine never let them see this" - so `ColdStartOverride.withheldByStartSeed` names the movements the band held out of reach and Step 5 stops counting those as new: `selectInChain` enters a no-history chain past them, and `select(pattern:)` sets them aside before consulting the variety window.
Nothing is filtered and nothing is forced, which is what makes the ceilings win for free rather than by precedence rule.
A Return caps the pool at difficulty 2, which leaves nothing in-band to prefer, so every candidate comes back into scope and the gentlest tier is served exactly as it was before the band existed - and the same is true of an injury filter or any other narrowing.
A `too_hard` session eases the seed itself, and the withheld set is resolved from that same eased seed, so the tier the seed just reopened stops being withheld in the same move.
A skip changes nothing here at all.
The withheld set is a factual claim about the user's own cold-start week rather than a policy dial, so it is recorded rather than re-derived.
While cold start is active it is the live seed; on the session that retires the band `ColdStartHandoff` writes the floor the week actually ran at - the seed as eased by every `too_hard` rating it produced - to a new additive `User.ColdStart.bandFloorAtHandoff`, and afterwards Step 5 reads exactly that.
Only that rating moves the tier: a skip is the product's escape hatch ("not this movement") and eases the seeded *volume* alone, mirroring the Asymmetric Ramp, so a routine tap cannot permanently erode a band that outlives the log window.
The contract's un-eased aim is recorded beside the floor as `bandAimAtHandoff`, because the retiring session's own rating is collected on the completion screen and therefore always lands after the handoff has run; `ColdStartHandoff.revisedBandFloor` re-resolves the recorded floor from that aim when it does, touching no counter, so an explicit `too_hard` on the fifth session still reaches the band the rest of the account is measured against.
Deriving it at selection time instead was the first attempt and it does not survive: the engine's `recentLogs` is a bounded 70-day window (`ReadyViewModel.recentLogLookbackDays`), so the cold-start sessions - and the `too_hard` rating that eased the floor - age out, after which a derivation quietly re-raises the tier the rating lowered and starts asserting a band the user never lived through.
The field decodes to `nil` when absent, which reads as "no banded cold start ever ran" and withholds nothing: correct for a record persisted before US-O02 (whose contract decodes its Start Seed to the neutral floor, so that week genuinely ran unbanded), for a contract carrying the neutral floor, and for any generation against `SessionPolicy.default`.
That closes the second half of the problem too - `coldStart.active == false` is equally true *before* a contract exists as after one retires, so gating on the flag alone had been asserting a band retroactively over users who never had one.
`StartSeedTests.testSessionAfterTheHandoffNeverRegressesBelowTheColdStartTiers` is the standing gate - it walks a full five-session cold start and asserts the sixth session regresses in no pattern, including one that week never trained.
`ColdStartOverride.volumeSeed` hands the volume half to Step 6: `AdaptiveOverload.target` gains `startingRepMultiplier`/`startingSets` parameters that scale **only the no-history default target** - the very first prescription of a movement, where there is no demonstrated capacity to be relative to - clamped to the existing `maxReps`/`maxHoldSeconds`/`maxSets` rails; a capacity-derived target (session 2+ of that movement) ignores the seed entirely, and the neutral values reproduce the pre-seed target precisely. `SessionAssembly` applies it only to the strength and primal blocks, matching the floor's scope.
The safety net for an over-reported level corrects the **tier**, not just the volume. The Asymmetric Ramp (US-E05) still eases reps eagerly on a `too_hard` rating or a skip, but on its own that would hold a user at an unwinnable tier for the whole five-session cold-start window, since the band removes every gentler tier from the pool. `ColdStartOverride.startSeed` therefore resolves the *whole* seed against `recentLogs`: it counts the same eager down-signals the ramp reacts to (a session rated `too_hard`, or one where the user bailed on a strength/primal movement) and steps the floor down a tier, drops a set, and eases the rep multiplier by the ramp's own `hardStep` per signal - never past neutral, and never touching a beginner's already-neutral seed. Tier and volume ease together on purpose: de-escalating onto an easier movement the user has never logged would otherwise re-apply the full volume seed to it.
The in-session swap (US-C08) now carries the seed across its seam: `WorkoutEngineProtocol.swapExercise` takes the `sessionPolicy`, which `ReadyViewModel` -> `ActiveSessionView` -> `ActiveSessionViewModel` thread through from the policy the session was generated against, so `ExerciseSwap` sizes a substitute with the same Step 6 levers (`progressionRate`, the Start Seed, `varietyWindow`) as the rest of the lineup instead of silently re-deriving it at the neutral seed. The default `SessionPolicy.default` argument reproduces the pre-policy swap exactly.
Finally, the seeded target is reflected in the **planned wall-clock**.
`Exercise.estimatedTimePerSetSeconds` is one constant calibrated against the movement's own default per-set value, so reading it directly assumed every set was prescribed at that default.
It is also not pure per-unit work: getting into position, bracing, switching sides and getting out again is paid once per set whether the set is 8 reps or 20, and in the catalog that fixed part is often the bulk of the estimate (`mobility_9090_hip` carries a 70s estimate on a 30s hold).
`SessionAssembly.workSecondsPerSet` therefore splits the estimate into a fixed per-set **setup** cost plus a **per-unit** cost.
Exactly one of the two halves has to be assumed and the other read off the movement's own authoring, and which way round matters.
A hold's per-unit cost is known a priori - one second of work per prescribed second, *per side* for an `isPerSide` movement, because a "20 second" side plank is 20 seconds left plus 20 seconds right - so its setup is the authored remainder `estimate - sides × defaultDurationSeconds`, which the catalog puts at 5-25s.
A rep has no authored duration to anchor that half, so for reps the *setup* is the assumed one (`setupSecondsPerSet`, capped by `maxSetupShareOfEstimate`) and the cadence is the authored remainder `(estimate - setup) / defaultReps`.
That is the right way round because cadence genuinely varies per movement - the catalog spans `hinge_glute_bridge` at 15 reps in 40s to `push_one_arm` at 3 in 50s - while getting down onto the floor and back up is much the same work whatever the movement is.
The first attempt had it backwards: it pinned the per-rep cadence at a global 3.0s and let the setup absorb the entire residual, which misclassified everything that genuinely scales with the target as a fixed cost and so *under*-charged per-side and slow-tempo movements, in places worse than the strict-proportional model it replaced (a `core_side_plank` grown to 40s was priced at 65s against 85s of real holding - a 60s under-count from a single slot over three sets, the whole ±1 minute budget).
That constant was also circular: it was calibrated from the four rep entries it then assigned zero setup to, asserting they have no setup at all.
`setupSecondsPerSet` is calibrated from a different family instead - the holds, where the setup is *observed* rather than fitted, and whose authored remainders have a median of exactly 10s.
Both halves reproduce the authored estimate exactly at the movement's own default, so a default-sized session is priced precisely as the catalog wrote it and only a moved target is re-priced - which keeps the ±1 minute timing promise honest for a x1.30 first session and for any capacity-grown one, without the strict-proportional model's systematic over-charging or the per-side-blind split's under-charging (a 40s `mobility_9090_hip` hold costs 90s; a 40s `core_side_plank` costs 85s; `push_archer` at 8 reps costs 74s rather than 59s; a hold clamped to `maxHoldSeconds` costs its setup plus the hold on every side, not a scaled estimate).
The per-side signal is a new additive `Exercise.isPerSide` rather than a parse of the user-facing `advancementCriteria` copy, so timing does not silently depend on wording; it is optional (absent means not per side) so an older `Exercises.json` and an already-persisted active session still decode, and a library test cross-checks the flag against that copy in both directions.
That cross-check originally matched a closed whitelist of per-side phrases, which meant an unfamiliar wording ("3x8 clean reps per arm", natural for the upper-body chains) simply fell through to "not per side" and the movement was silently under-timed by half - the exact failure the gate exists to catch.
It now asserts *positively* that every criteria string matches one of a small set of recognised authoring templates, with a second backstop that no criteria may name a limb at all outside a recognised per-side marker, so an unanticipated phrasing fails loudly instead of defaulting.
Making the per-side cost explicit also exposed two rails that were per-side-blind.
`AdaptiveOverload.maxHoldSeconds` is documented as a safety rail against an absurd prescription, but for a per-side hold it meant three minutes *per side* - a single set costing more than the entire budget of a 5-minute session - so `clampPerSet` now divides the ceiling by `sidesPerSet` and the rail means what its doc says for every movement.
That matters because the timing fit's only levers are set counts and item promotion/dropping: it can never shrink a per-set target, and warm-up, Movement Practice and cooldown all carry `minItems: 1`, so an unbounded per-side hold could push a short session past ±60s with nothing available to adjust.
The clamp does bound it - a fit case driving every per-side hold in the catalog to its rail now lands inside ±60s at all five durations, and fails without the clamp.
The existing ±1 minute tests could not have caught this: they run against history sitting near the movements' rep defaults with no holds at all, which is precisely the configuration where the split and the doubling are both no-ops.
Finally, the loop is closed in the player: `targetText` and `targetAccessibilityText` now read "3 × 0:30 per side" and "3 sets of 30 second holds per side", driven off `isPerSide` so the copy cannot drift from what the engine charges.
Previously the per-side instruction appeared only in `advancementCriteria`, which is progression copy rather than the prescription, so a user handed a 40s side plank who held it once did 85s less work per set than the session was planned around - over three sets the whole ±1 minute budget from one slot.
One point worth recording so it is not re-litigated: the work half of a session is never *timed*.
Nothing counts a rep or a hold down, the only countdown in the player is the rest timer, and a completed set logs the target it was prescribed rather than anything measured.
So this is a planning-only quantity whose single job is fitting a session to the minutes the user asked for; chasing per-second accuracy on a self-paced activity would be false precision.
What matters is that the model carries no *systematic* bias, because a consistent per-slot error accumulates where random error averages out - and that is exactly what both of the failed models had.
Because an advanced seed opens at `startingSets == maxTrainingSets`, the fit also lost its "add a set" lever entirely, so reserve promotion now enumerates every set count within the rails rather than only the count Step 6 prescribed; without it a 10-minute advanced session parked 91s short of the request.
The seeded **set count** remains a pre-fit starting volume - the timing fit may still trade sets against the duration the user asked for, and the requested duration wins.
The swap's slot-budget check is measured through that same model.
It resolves the candidate's `OverloadTarget` first and prices **both** sides with `SessionAssembly.workSecondsPerSet` at the target each actually carries, so `slotToleranceSeconds` bounds the session's real wall-clock rather than two movements' default-sized estimates - previously a capacity-grown 20-rep `push_diamond` slot accepted a never-logged `push_pike` opening at its 8-rep default on a reported drift of zero while the session lost minutes.
Honouring the real budget on its own, though, did not merely narrow the candidate pool - it very nearly removed the feature.
A substitute the user has never logged always opens at its own default while the slot it replaces carries a grown target, and at a fixed set count the 30s tolerance leaves only ~10s of per-set headroom: sweeping the Discipline strength/primal catalog found an in-budget peer for 42/42 movements at the default target but only 18/42 at x1.5 of it and 3/42 at x2.0 - and x1.5 is ordinary, since `push_standard` opens at 8 reps and advances at "3x12".
The fix is the **set count**, the same lever the assembler's own timing fit uses, rather than a wider tolerance.
`ExerciseSwap` now picks the substitute's set count inside `minTrainingSets...maxTrainingSets`, comparing full slot seconds (`sets × workPerSet + (sets - 1) × rest`) so the rest a set change adds or removes is counted - rest being the one part of the budget that is exactly known.
The original count wins whenever it is already in budget, so a swap restructures a slot only when leaving it alone would push the session out of its stated minutes, and `.noAlternative` is reported only when no permitted count fits.
Making that promise actually true took a second pass: `bestFitSets` settles each candidate's *own* best count, and the selection then compared drift across all of them, so drift outranked set-preservation and slots were restructured gratuitously - `push_standard` grown to 10 reps returned `push_wall` at **4 sets** (drift 18) when `push_diamond` fit at the original 3 (drift 27), and with no growth at all `mobility_pigeon` returned `cat_cow` at **2 sets** when `mobility_9090_hip` fit at 1.
Catalog-wide that was 40 such cases at 3 sets/40s rest and 28 at 1 set/15s rest.
Set-preservation is now the *first* selection criterion, so drift is compared only among candidates that fit the slot as built and a re-picked candidate is reachable only when none does; both named cases now return the same-set peer, and the gratuitous count is 0 in each configuration.
The lever is scoped as the assembler scopes it: the warm-up and cooldown are built `allowSetAdjust: false`, so a bookend slot keeps its single set and simply declines instead - which required `ActiveSessionViewModel.snapshotForSwap()` to stop collapsing the lineup into one strength block and preserve the real block structure, since the block is what makes that distinction visible to the seam.
Because a bookend has no lever *and* no rest, a flat 30s gate there is the strictest test applied to the softest quantity, so a non-adjustable slot gets `slotToleranceSeconds + 0.3 × its estimated work` instead.
The 0.3 is read off the catalog rather than picked: across all 57 movements the widest gap between a movement's own per-set work at its default and its nearest in-band peer's is 30% (`mobility_pigeon` at 100s against `mobility_9090_hip` at 70s; the 95th percentile is 14%), so the band is exactly wide enough to admit every pair the catalog itself authors as equivalent and no wider, and it is additive so a very small slot keeps the flat allowance rather than a tighter one.
Measured over the sweep in the **adjustable** 3-set/40s-rest shape, coverage recovers to 42/42, 38/42, 41/42 and 38/42 at x1.0/x1.25/x1.5/x2.0 of the Discipline strength/primal catalog, and the flat 30s tolerance is unchanged there.
That shape is the one where the set lever always exists, so it is now measured alongside the real bookend shape (1 set, `mobilityRestSeconds`, non-adjustable), where the 12 mobility movements cover 12/12, 12/12, 11/12 and 11/12 at the same four growth levels.
The single bookend refusal is `mobility_pigeon`, and it is arithmetic rather than false precision: grown to 68s per side it is 146s of work against a nearest in-band stretch of 70s, which no tolerance short of abandoning the ±1 minute promise would admit, so the sweep asserts the coverage that actually holds rather than 12/12.
The player already reset `currentSet` to 1 on any swap, so a substitute with *fewer* sets than the slot the user was part-way through cannot strand them past its end; that is now pinned by a test rather than incidental.

The swap also carries the Return / Re-entry Ramp ease (US-E06).
`ReturnOverride.reentryScale` is resolved from the session's own `wasReturn` stamp plus the policy's ramp state - never re-derived against a fresh clock - so a substitute is held below full volume exactly as much as the slot it replaces, instead of one tap handing a returning user the single hardest slot in a session whose whole point is to be uniformly gentle.
This was the second Step 6 lever found missing from the swap seam, so all of them now flow through one private `overloadTarget` helper (the seam's only call into `AdaptiveOverload`, shared by the budget check and the materialized prescription), and a test pins the general property that every lever visibly moves the substitute's target rather than testing each one in isolation.
Routing every lever through one helper turned out to fix the levers but not their **scoping**: the helper passed the Start Seed for every candidate, while the assembler applies it only to strength and primal (`Builder.mobilityItems` sizes a stretch with `progressionRate` and `reentryScale` alone) so that warm-up, Movement Practice and cooldown are identical for a beginner and an advanced user.
Since Swap is offered on every step, an advanced cold-start user swapping a warm-up stretch was handed it at x1.30 - `mobility_forward_fold` at 39s beside 30s neighbours - and the budget check was then measured against that inflated target too.
The helper now passes the neutral volume for a `.mobility` candidate, and the scoping (not just the presence) of each lever is pinned by a test.

A follow-up accessibility pass fixed the *spoken* half of that same target line (`Views/ActiveSession/ActiveSessionView.swift`, `Views/Ready/ReadyView.swift`).
`targetAccessibilityText` spelled its counts out with fixed plurals, so any single-set slot read "1 sets of 45 second holds" - and the warm-up and cooldown are always single-set, which made that ungrammatical string the first and last thing a VoiceOver user heard in *every* session, however short.
The set count, the rep noun and the hold noun now each agree with their own number - the count through a shared `setsPhrase` ("1 set" / "3 sets"), and the article moving with the noun on a single hold ("1 set of a 45 second hold") - and the agreement composes with the per-side suffix rather than being swallowed by it ("1 set of a 30 second hold per side").
Every multi-set string is unchanged, so the change is purely additive to what already shipped.
The same pass removed a duplication an earlier review had flagged: `ReadyView` carried its own copy of the visual formatter and handed *that* string to its accessibility label, so the Ready Screen spoke the "×" glyph where the player spoke the nouns.
It now calls `ActiveSessionView.targetText` / `targetAccessibilityText`, which makes the player's two formatters the single source for both the seen and the spoken prescription and removes the second copy that let them drift apart.
The bug was reproduced before it was fixed: the added `ActiveSessionViewModelTests` cases fail on the prior implementation with the exact wrong strings, and `PerSideSwapEvidenceTests` gates the property end-to-end - every slot of a generated session has to speak counts that agree with its own numbers, and the Ready Screen's row labels are read back off the hosted view's live accessibility tree (not re-derived from the formatter) and asserted to match the player's sentence with no "×" anywhere on the screen.


US-O03 (`ViewModels/ActiveSessionViewModel.swift`, `Views/ActiveSession/ActiveSessionView.swift`, `Models/ActiveSessionState.swift`) is the **session timer redesign**: the running clock comes off the screen, and a clock that actually helps goes on it.

The always-on elapsed time is gone from both mid-session surfaces.
A total ticking up in the corner turns a five-minute session into something to get through and pulls attention off the movement, which is the opposite of what a discipline-first product wants; the total now lands once, on the completion summary.
The player's top bar and the rest overlay's were near-duplicates of each other, so both now render one `SessionTopBar` carrying the close control and nothing else - there is no second place for a clock to reappear.
The clock is **hidden, not removed**: `startedAt` and `elapsed(asOf:)` are untouched, so `completedDurationMinutes()` and everything reading it (the `WorkoutLog`, Default Duration learning, the Consistency Score, the summary's duration tile) behave exactly as before.
That the removal actually holds is asserted off the *live accessibility tree* of the hosted production player and rest overlay rather than assumed from the source, so re-adding the clock fails the suite rather than passing review.

In its place, a timed (`isHold`) exercise gets a per-exercise **Hold Timer**: the user taps "Start hold", a countdown runs in the demo's slot, and at zero the same accessible `RestTimerFeedback` cue the rest timer uses fires exactly once, the set records itself, and the player hands off to the rest.
Rep-based exercises are untouched - set tracker plus a manual "Complete set" - so the change is additive to two thirds of the catalog and load-bearing for the other third.

The hold is counted **one side at a time**, and that is the part worth spelling out.
The engine charges a per-side movement for both sides (`Exercise.isPerSide` via `sidesPerSet`, the same field US-O02's timing model prices a set with), so a set of `core_side_plank` is two 20-second legs, not one.
A single countdown that recorded the set at the end of one leg would quietly halve the work the session was planned around - which is precisely the failure the per-side target copy ("3 × 0:30 per side") was added to prevent, except worse, because a timer is an instruction the user follows rather than a label they read.
So a per-side hold cues the *switch* at the end of the first leg and parks on side 2 rather than auto-starting it: the tap that begins the second leg is the user's own time to change position.
Only the last leg records the set.
Four of the library's seventeen holds are per-side (`core_side_plank`, `mobility_9090_hip`, `mobility_pigeon`, `mobility_kneeling_hip_flexor`), so this is the ordinary case rather than an edge one, and both the button ("Start hold (side 2 of 2)") and the set tracker ("Side 2 of 2") name it.

Mechanically the leg runs against an absolute deadline, `pauseHold`/`resumeHold` are wired to the same `scenePhase` transitions the rest timer uses, and `completeHoldIfElapsed` is a pure idempotent check the view's ticker may call every tick - so the cue can never fire early, per-tick, or while the app is away.
It was first written as a full mirror of the rest timer, restore-from-snapshot included; that half of the mirroring turned out to be the root error of this story and was removed, so a leg no longer survives the player being torn down at all ("A hold is not a rest" below is where that lands and why).
What *is* persisted, in `ActiveSessionState.Hold` alongside `.Rest`, is the side - optional and defaulted so a snapshot written before this story decodes unchanged and an in-progress session is never lost to an app update.
The **side is persisted independently of whether a leg is running**: without that, a user who finished side 1 and backgrounded the app would come back on side 1 and do three legs of a two-leg set, which is the same halving bug pointing the other way.

One thing the story's own promise could not survive was left implicit in it, so it is fixed here too: a hold pauses on backgrounding, and iOS auto-lock backgrounds the app, so a user who sets the phone down to plank would have the countdown freeze and the cue that ends it never land.
The player holds `isIdleTimerDisabled` for as long as a session is being played - correct for an active workout screen generally, and load-bearing for this feature specifically - and hands the screen back to auto-lock on **completion as well as dismissal**, since the player is not dismissed when the session ends and the celebration screen has no countdown to protect.

The timer is the offer, not the only way through a timed movement.
A hold's primary action is "Start hold", but the row underneath keeps a quiet manual completion beside Swap and Skip, titled by the same `completeButtonTitle` logic every other slot uses ("Complete set" / "Finish exercise" / "Finish session").
Without it a timed exercise had no way to bank work at all: "Stop hold" records nothing by design, and Skip *discards* every set already recorded for the exercise - so a user interrupted on set 3 of a 3-set plank, or who held it off-timer, would lose the work rather than log it.
Most cooldown slots are holds, which made this the last control of nearly every session.
It stays offered **while a leg is actually running** too.
Hiding it there - on the tidy rule that the countdown is the only thing that records a set while it runs - left the running state with exactly two visible exits, and both destroy work: "Stop hold" records nothing, and Skip discards every set already banked for the exercise.
A user 25 seconds into set 3 of a plank who decides to stop would reach for Skip and lose sets 1 and 2 without being told.
The two-tap escape ("Stop hold", then the manual completion that reappears) existed but was not discoverable from the state the user is in, and an escape nobody can see is not one.
So the control is visible and enabled throughout: tapping it takes the running leg down through the existing cancel path - no cue, because coming out early is the user's choice rather than the timer's verdict - and then banks the set through the ordinary `completeSet`, which records exactly one set, resets the side, and opens the rest exactly as the countdown reaching zero does.
Trading a rule for a data-loss path is the right way round.
It also simplifies the row: every slot a timed movement carries is now always occupied, so `secondaryAction` no longer needs a hide-in-place mode at all.

Which *slots* that row carries is decided by the exercise rather than by whether a hold happens to be running, so starting one dims controls in place instead of removing them and the row never reflows under a thumb mid-tap.
The same principle settles the slot above it: the countdown is drawn inside the demo's own card (one `exerciseSlotCard()` shared by both), so starting a hold changes what is in the slot and nothing else - not the card under it, not the exercise name and target below it.
A timed movement also keeps "Start hold" as its primary action while a swap is in flight, merely unstartable, rather than briefly morphing into a different button.

That last point is the visible half of a real defect the review caught.
`canStartHold` did not consider `isSwapping`, so a hold started in the window between requesting a swap and the engine answering would keep counting down against the *substitute*, and at zero record a set of a movement the user never performed.
Both halves are closed: `canStartHold` refuses a leg while a swap is awaiting, and the `.substituted` branch now calls `resetHold()` rather than clearing the side alone.
The deliberate asymmetry either side of it stands - the pre-`await` call stays `endHold(fireFeedback:)` and `.noAlternative` still leaves the side untouched, because a swap that substitutes nothing must not charge the user a side they already held.

The same class of phantom set turned out to be reachable through a second door: **closing the player mid-hold**.
The snapshot kept the leg as running against an absolute deadline that was, by the time the user tapped Resume, already in the past - so the restored player's first tick fired the cue and banked a set within half a second of reopening, untouched.
Both ends of that are independently wrong and both are fixed.
Dismissal no longer leaves a countdown running on disk - first by freezing the leg on the way out, and finally by never writing one at all (the section below is where that lands).
What survives from that round either way is the ordering: `close()` reports the dismissal to the Ready Screen only once the last queued write has landed, so the resume card reflects the position the user actually left rather than the one before it.
That report hops to the main actor explicitly (`Task { @MainActor in … }`) rather than inheriting whatever a nonisolated method gives it, because `onFinish` reaches `ReadyViewModel.handlePlayerDismiss`, which mutates `@Observable` state SwiftUI is tracking; only `body` is implicitly main-actor, so the annotation is what makes that true.
The round that fixed the write end also made restore defensive about what it read, on the reasoning that a snapshot can predate this, be killed by the OS with no scene-phase transition, or simply be resumed days later: a leg still marked running whose deadline had gone by was brought back idle rather than finished.
That guard is gone now, because there is nothing left for it to guard - `init(state:)` reads the side and discards every other key a snapshot might carry, so no restore path ever sees a countdown.
The principle it was reaching for survived and hardened into the rule: an unfinished leg is unfinished work, and the user re-starts it deliberately.

Two smaller consolidations came with it.
The countdown ring was duplicated between the rest overlay and the new Hold Timer, so both now render one `CountdownRing`, and the two timers a user meets in a session read as the same object.
And the hold's countdown stands in `ExerciseDemoView.height` exactly, so starting a hold swaps the demo for the ring without shifting the exercise name and target underneath it - a jump that was visible in the first rendered pass and is gone in the second.

Adding a third column to that secondary row also made its narrowest case worth measuring rather than assuming.
Each column is roughly a third of a 393pt row, and the manual completion carries the longest title in it ("Finish exercise"), so at accessibility Dynamic Type sizes a fixed 60pt height would have clipped away exactly the words that say what the tap does.
The label now wraps and the row grows - the same remedy the target line already documents - with 60pt as the touch-target floor rather than a ceiling, measured off the live accessibility tree at both default and accessibility type (60pt vs 144pt) instead of only rendered.

Verification ran three ways in the Simulator, all against the production `ActiveSessionView`.
Rendered PNGs of the player at a hold (idle, running, idle at accessibility Dynamic Type, per-side side 1 and side 2), of the rest overlay, and of the completion summary that finally reports the total are under `artifacts/reports/us-o03/`, and `PerSideSwapEvidenceTests` is what writes them.
That is worth stating precisely, because it was not true at first: the evidence directory fell back to a machine- and session-specific path under `/var/folders`, so the committed images had been copied there by hand and any later change to the player's layout or copy would have left a stale render still cited as evidence.
The repo path is now resolved from the test's own `#filePath` - a test bundle controls neither the working directory nor any repo-relative path, so walking up from the source file is the reliable way to find the repo root.
Writing there is deliberate rather than incidental, though, because a test suite that rewrites every tracked PNG on every run makes a clean `git status` impossible and buries a real drift in re-encoding noise.
So a plain test run renders to a per-run temporary directory and leaves the worktree untouched, and regenerating the committed images is an explicit act:

```bash
xcodebuild -project ios/RepToday/RepToday.xcodeproj -scheme RepToday \
  -destination 'platform=iOS Simulator,name=iPhone 16' test \
  -only-testing:RepTodayTests/PerSideSwapEvidenceTests REPTODAY_WRITE_EVIDENCE=1
```

That reads as a build setting rather than a shell variable on purpose: the test bundle runs inside the Simulator and does not inherit the invoking shell's environment, so the scheme forwards `$(REPTODAY_WRITE_EVIDENCE)` and `$(REPTODAY_EVIDENCE_DIR)` (both undefined, and so empty, by default) into the test process, and xcodebuild takes either on the command line.
`REPTODAY_EVIDENCE_DIR` still overrides the destination outright. Every render and every assertion runs in all three modes - only where the bytes land changes, so the gate is never weaker for being quiet.
(Superseded: `REPTODAY_EVIDENCE_DIR` is now a *root* with the story folder appended, not the final directory - see `EvidenceOutput` and CLAUDE.md for the current rule.)
One render needed pinning to be worth refreshing at all.
The running-hold capture starts the leg and then reads the real clock, so whether the ring showed N or N-1 seconds - and the arc's fill with it, since that is `remaining / total` over whole seconds - depended on how the runloop pump happened to land, and the file's bytes moved between runs for no change in behaviour.
It now waits for a specific remaining second and lets the arc's 0.5s animation finish before capturing, which makes it the steadiest file of the set.
The honest limit is that these are live surfaces, not golden images: every render that still shows the exercise demo catches that demo's continuous pulse at whatever phase it is in, so those bytes move a little on every deliberate refresh.
That churn has a recognisable signature, which is what makes it safe to tell apart from a real change: it is confined to the demo glyph's own bounding box and stays around a 1% per-channel delta, invisible to the eye, with no text or layout moving anywhere on the screen.
A refresh whose only diff is that box is noise to discard rather than a change to commit; a diff that reaches any other part of the screen is the screen actually changing.
The renders that replace the demo with something else - the running hold, whose ring stands in the demo's slot, and the rest overlay, which covers it - come back byte-identical, so they are the ones a refresh can be read straight off.
The renders are review material and a "did this screen change" prompt, and the assertions beside them are the actual gate.
The per-side and swap renders belonging to the earlier story write to `artifacts/reports/per-side-swap/`, kept apart so nothing lands among the files the US-O03 acceptance notes point a reviewer at.
The accessibility tree of those same hosted surfaces is read back and asserted, so the spoken forms and the absent clock are gated rather than eyeballed.
And four live tests close the loop by driving the real controls the way VoiceOver's double-tap does.
`testHoldTimerRunsDownAndHandsOffToRestInTheLivePlayer` activates "Start hold", lets the wall clock run out on a short-authored hold, and asserts the live player has recorded the set and moved to the rest overlay - exercising the `Timer` publisher, the deadline arithmetic and the auto-advance as one system rather than as pieces.
That one pumps the runloop until the hand-off is *observable* rather than for a fixed budget, so a loaded machine that takes longer to push the update through still fails on behaviour rather than on the clock.
`testBankingASetByHandMidLegEndsTheLegAndOpensTheRestInTheLivePlayer` drives the escape the running state has to offer: start a leg, then activate the manual completion mid-countdown, and assert the countdown is gone, the set is banked, and the rest overlay is up with the counter advanced one set.
`testClosingThePlayerMidHoldLeavesNoLegOnDisk` drives the other live path the same way - start a hold, then activate the real close control - and asserts what actually reached the store: no countdown at all, only the side of a half-done per-side set, which is what the resume then picks up with nothing banked.
The story's other half gets the same treatment in `testTheSessionTotalIsHiddenDuringPlayAndRevealedOnTheCompletionSummary`, which is the only place both halves are gated together on one surface: the live player is read for an elapsed clock *and* for the minutes worked so far in any form, the real "Finish session" control is activated, and the summary that replaces it is read for the same session's total.
Apart, either half can pass while the story fails - a player with no clock and a summary with no total is a session whose minutes simply vanished, and a summary reporting a total the player was also showing is the pressure the story removes - so the hiding and the revealing are asserted as one property rather than two.
It renders that summary too, so the screen the total finally lands on is reviewable alongside the ones it was taken off.

US-O04 (`Utilities/UnitConversion.swift`, `Utilities/PressRepeater.swift`, `ViewModels/OnboardingViewModel.swift`, `Views/Onboarding/OnboardingView.swift`) puts **imperial units** in front of a US user without letting a single one of them past the onboarding boundary.
The app is metric everywhere behind the UI - `UserProfile` holds `heightCm`/`weightKg`, and `HealthKitWorkoutSample` prices a session as `MET x weightKg x hours` - so imperial is an input concern only, converted once on the way in.
That one-way discipline is why the arithmetic lives in a value type rather than inline in the view: a pound that leaks into the kilogram field silently inflates every calorie the app writes to Health by 2.2x, and nothing downstream would flag it.
`UnitConversion` carries the exact international definitions (1 in == 2.54 cm, 1 lb == 0.45359237 kg) plus the display/spoken forms, and `feetAndInches(fromCentimeters:)` rounds *total* inches before re-splitting them, so a height that rounds up to a whole foot reads `6 ft 0 in` rather than the `5 ft 12 in` that splitting first and rounding second produces.
It is also the only place the feet/inches split lives: the step holds a height as one number and the label overloads take that number, so no caller carries its own `/ 12` to drift from.
The step's bounds - 47...87 in, 70...445 lb - are the imperial cover of the 120...220 cm / 35...200 kg sliders they replace, rounded outward at every end rather than to convenient round numbers: 47 in is 119.4 cm and 87 in is 221.0 cm; 70 lb is 31.8 kg and 445 lb is 201.8 kg.
A narrower range would have been silent: there is no profile-edit surface yet, so a weight clamped on the way in under-reports every HealthKit energy estimate for the life of the account, and the story was meant to change the units, not who the app fits.

The view model stores only the imperial answers; `heightCm`/`weightKg` are derived, so there is no second representation to fall out of sync, and `buildUser` is the single place the conversion happens.
The defaults (`5 ft 8 in`, `175 lb`) are near the US adult median and deliberately *not* the values the PRD's validation test types, so a mapping that ignored its input could not pass by coincidence - a test asserts that separation directly rather than trusting it.

The step's control changed twice, and both times because the Simulator said so rather than because the source looked wrong.
The cm/kg sliders became imperial sliders, and a hosted render of the production step showed VoiceOver reading each row three times ("Height", then "5 ft 8 in" as letters, then the control) over a slider laid out at 20pt - less than half the app's own 44pt floor.
Substituting the spoken sentence for the terse read-out fixed the speech; the platform `Stepper` that replaced the slider was still only 46.5 x 32pt, under the floor in the dimension a thumb is least accurate in.
So the row draws its own 44pt-square step buttons that repeat while held - the one thing a hand-drawn stepper otherwise gives up against the platform one - and is a single **adjustable** accessibility element rather than two buttons, which is the idiom VoiceOver already knows for a value control: it reads "Height, 5 feet 8 inches" and a swipe moves it by that row's own step (1 in, 5 lb).
Age moved onto the same row, because the touch-target miss was never specific to the units and was sitting in the step already.
A render at the largest accessibility type size then caught the last of it: the row's name, value and buttons no longer shared a line, and the +/- glyph had grown straight through its own button's background.
The row now stacks at accessibility sizes and the glyph's growth is capped, the text around it carrying the larger sizes instead.

The repeat itself moved out of the view into `PressRepeater`, because the first version of it had three defects that no assertion could reach.
A tap gesture stacked on a long press gave one press two sources of increments - the tap recognizes on touch-up whatever the long press already did - so a press-and-hold ended a step past what the read-out had shown while it was held.
The repeat loop had no termination but cancellation, so holding `+` at the top of the range kept writing the same clamped value back through the binding about eleven times a second; Observation notifies on every write regardless of equality, so that is a real invalidation loop for no visible change.
And the step committed on touch-*down*, so a finger that landed on `+` on its way to scrolling the step had already moved the value, with the opposite button as the only recovery - a stray step charged to the user's own body measurements, which nothing downstream lets them correct yet.
The step now lands on *release*, which is what every other control in the app does.
Getting there took three passes at hand-rolled gestures, and each one broke on the same thing: deciding in the view whether a lifted finger was still *on* the button.
A `TapGesture` for the release and a press tracker for the repeat enforced different radii (a tap's ~10pt is not configurable, against the tracker's 22pt), so a press that drifted between them was tracked, never repeated, and then silently dropped - a button that read as dead.
Judging the release off the drag's own translation instead fixed that and introduced worse: a radius drawn around where a finger *landed* is not a button's bounds, so "still on the button" became a circle of the view's own invention, agreeing with neither the 44pt square it was drawn over nor any other control in the app.
So the geometry left the view entirely.
The button is now a real `Button`, and the single step is the platform's own touch-up-inside: the bounds that decide it are the ones every other control in the app gets, touch slop and all.
A press therefore stays with the button it landed on - a finger that lands on `+` and lifts a little over the adjacent `-` is still inside `+`'s slop and steps `+`, never `-`, exactly as two adjacent buttons behave anywhere else in iOS - and only travel past that slop, or a press the enclosing `ScrollView` takes over, commits nothing at all.
Inheriting that rule instead of restating it is the whole point of the change; the earlier hand-rolled radii are gone rather than retuned.
Only the drawing stays hand-rolled, in a `ButtonStyle` that renders the 44pt square.

That leaves `PressRepeater` the one job the platform has no notion of, against a `step` closure that reports whether the value actually moved: a press that outlives the hold delay repeats, and the release ending it adds nothing on top of what the read-out already showed, while a refusal ends the hold.
It runs off the style's own press state - true while the finger is down on the button, false the moment it lifts, drags off, or the scroll view takes over - so the repeat cannot outlive a press the view never heard end, and the same signal tints the glyph.
Being an object with an injected clock rather than gesture code is what makes it testable, and `PressRepeaterTests` gates it: nothing commits while the finger is down, a tap is one step on release whichever order the button reports the release and the end of tracking in, a press that ends anywhere but on the button moves nothing, a hold ends where it stood when the finger lifted, and a hold at the bound stops on the refusal.
The hold flag the release reads is cleared by whichever way the hold actually finished rather than by the next press: a reported release consumes it, and a repeat that ends on a refusal clears it on its way out - which is the case no release ever reports, since a hold that walks the value into the bound disables its own button under the finger and the lift fires no action.
Neither path can leave a stale `true` behind to swallow a later step, and a test drives exactly that sequence so the guarantee is pinned rather than asserted in prose.
The repeat task is also cancelled on deallocation, so a button torn down mid-hold on a path that never delivers `.onDisappear` cannot leave an orphaned loop walking the binding to the bound with no finger on screen.
Release semantics also cost the control its feedback - the value moving *was* the acknowledgement while it committed on touch-down - so the glyph now tints to the accent while pressed, cleared on release, on cancel and on disappear, and unanimated under Reduce Motion.
The tint fades in over 100ms and out over 200ms: a tap is often under 100ms, and SwiftUI's default 0.35s meant the highlight barely registered on exactly the interaction it was added to acknowledge.

`OnboardingView` gained an initializer taking an already-built view model, which is what lets a test render a mid-flow step through the production view rather than reimplementing it.
`OnboardingBasicsEvidenceTests` hosts that view in a real Simulator key window, asserts on the live accessibility tree (the value spoken as a sentence, no metric unit anywhere on the step, the laid-out 44pt frame, the adjustable step), and writes the two renders to `artifacts/reports/us-o04/`.
Every defect above was found by those assertions, not by reading the diff.

What that hosted window cannot do is press anything: a `Button`'s press state comes from touch delivery, which only a real run of the app has, so every fix above was argued from a state machine and a screenshot.
A `RepTodayUITests` bundle closes that gap permanently - `OnboardingImperialUITests` launches the shipping app, walks it to the basics step, and presses the step buttons by coordinate (they are `accessibilityHidden`, the row being one adjustable element), asserting the imperial defaults, one step per tap, a hold that repeats, and two abandoned presses that commit nothing.
It also walks the story's acceptance path whole: the PRD's validation measurements (5 ft 7 in, 165 lb) are dialled in with those buttons, the rest of the flow is answered on its defaults, and the run has to land on a generated session - the per-control tests prove the rows read and move in imperial, and this one proves the imperial answers are *accepted* by the flow they feed, since a conversion failing at the boundary surfaces as onboarding that never reaches a session rather than as a wrong number nobody sees.
That walk runs to the end of the real first launch, which means through the Health share prompt the main tabs raise as they appear (US-N03): it is declined, its follow-up confirmation is dismissed, and only then is the ready session captured.
The prompt is a remote view presented inside the app's own process rather than a springboard alert, so an ordinary `app` query reaches it and no interruption monitor is involved; the step is bounded and optional, because a container that already answered has no sheet to dismiss and must still pass.
The capture is gated on the tab bar being reachable by a finger again rather than on the sheet leaving the element tree - a sheet is out of the tree while it is still animating off, and the first version of this render filed the permission sheet under the ready screen's name.
It is a second scheme, `RepTodayUITests`, precisely so it is not in the way: it installs and launches the app out of process, and the documented `-scheme RepToday test` stays the unit run it has always been rather than inheriting an app launch's failure modes on every invocation.
That scheme forwards `$(REPTODAY_WRITE_EVIDENCE)` and `$(REPTODAY_EVIDENCE_DIR)` exactly as the generated one does, so the screenshots each test takes follow the same evidence convention as the rendered PNGs above: a per-run temporary directory by default, `artifacts/reports/us-o04/` when the opt-in is set, and wherever the override points otherwise.
Without that passthrough the redirect would have been documented but dead - the XCUITest runner is a Simulator process and sees nothing of the invoking shell, which is the whole reason both are build settings rather than environment variables.

US-T02 (`Models/AnalyticsEvent.swift`, `Services/Protocols/ServiceProtocols.swift`, `Services/Mock/MockServices.swift`, `Services/Analytics/NoOpAnalyticsService.swift`, `DI/ServiceContainer.swift`) opens the **funnel-instrumentation** epic (a separate PRD, `.claude/agent/tasks/prd-funnel-instrumentation_260803.md`) with the analytics **seam only** - no emission call sites, no transport, no new Swift Package (Lottie stays the app's one third-party dependency).
`AnalyticsEvent` is a `Codable`, `Sendable` value: a closed `AnalyticsEventName` enum over exactly the 13 pre-registered in-app events (`gtm/06-channels/event-metric-schema.md`, snake_case raw values that are the Convex wire contract - the two web-side events are out of scope), a millisecond client timestamp, and a `[String: AnalyticsValue]` property bag where `AnalyticsValue` is a closed union of `Int`/`Double`/`String`/`Bool`.
`AnalyticsValue` encodes with an explicit `type` discriminator so every scalar case round-trips losslessly - `.int(1)`, `.double(1)`, and `.bool(true)` never collapse into one another the way natural JSON number/bool decoding would let them.
`AnalyticsServiceProtocol.record(_:)` is `async` but deliberately **not** `throws` (the one departure from the file's `async throws` house style): emission is fire-and-forget, so a call site reads `await analytics.record(event)` with no `try`, and the live transport that landed in US-T04 swallows every failure rather than surfacing it into the core loop.
`MockAnalyticsService` is an `actor` recording every event into `recordedEvents` in order with no I/O; `analyticsService` is a new `ServiceContainer` property wired in both `mock()` and `live(...)`, a required initializer parameter like every other service so each construction site wires it explicitly and a container built without a sink fails to compile rather than silently dropping telemetry.
The two containers wire *different* sinks: `mock()` takes the recording `MockAnalyticsService`, while `live(context:)` takes `NoOpAnalyticsService` (`Services/Analytics/`, the directory US-T04's real Convex-backed `URLSession` POST later landed in - where the no-op survives as what a build carrying no usable endpoint wires, which today is every Release build).
A shipping build with no transport must emit nothing **and accumulate nothing**, so production discards each event rather than appending it to a `recordedEvents` array nothing drains - otherwise the first emission story to land ahead of the transport would grow one `AnalyticsEvent` per event for the process's whole lifetime, silently.
`AnalyticsServiceTests` covers the three-event-in-order record, every `AnalyticsValue` round-trip, the whole-event round-trip, and an exactly-13-cases assertion so a missing or extra event name fails loudly.
It also exercises the seam rather than only the model: one anonymous install's whole funnel - all 13 events, in emission order, with the schema's own property names - is recorded through `analyticsService`, written the way an emission call site must write it (`await services.analyticsService.record(event)`, no `try`), so the deliberate non-throwing signature is used as a call site rather than asserted about; the test asserts the mock container hands every event back in order, and the recorded array is then encoded to the model's own JSON and decoded back losslessly, asserting the snake_case event names are what serialise and neither web-side event appears.
As US-T02 shipped it, that funnel ran through **both** containers, because production wired the discarding no-op and so cost nothing to drive. US-T04 changed that: the production container now wires a real transport pointed at a real deployment, so recording 13 events through it from a unit test would put 13 POSTs on the wire, and the test asserts the production wiring **by type** instead while emitting only through the mock. It claims nothing about the request body, which is `AnalyticsWireBody`'s shape and is covered by `LiveAnalyticsServiceTests`.
The funnel fixture's timestamps are fixed offsets from a pinned install moment rather than read from the clock, so the container test is deterministic run to run; it writes no files and touches no network.
Landing the seam also fixed a pre-existing time-bomb: `MockWorkoutEngine.generateWorkout` read the wall clock (`asOf: Date()`), violating the "engine logic never reads the wall clock inline" rule, so `PerSideSwapEvidenceTests`'s hosted Ready Screen drifted a staleness tier from its fixed-clock `generate()` baseline as real time moved past the fixture date (2026-07-30) and began failing on 2026-08-03; the engine now takes an injected `now` clock defaulting to `Date()` (production unchanged) and the evidence test pins it, so the suite is deterministic again.

US-T03 (`convex/schema.ts`, `convex/events.ts`, `convex/http.ts`, `convex/README.md`, root `package.json`) turns the `convex/` placeholder - a lone `.gitkeep` until now - into a real, deployed Convex project, and gives the funnel events a sink to land in.
It is the whole analytics backend: **one table, one mutation, one HTTP route**, and no app code changes at all (`ios/` is untouched, so the iOS build and every Simulator-hosted suite are unaffected by construction).
The `events` table carries five fields - `name`, `installId`, `clientTs`, `serverTs`, `props` - with no index beyond Convex's defaults and no funnel, cohort, or aggregate structure anywhere; `logEvent` validates, stamps `serverTs = Date.now()`, inserts one row, and returns.
The sink is dumb on purpose: every kill-criterion metric (K1-K8) is derivable from raw rows by a query written later, so a funnel baked into the write path would be a threshold decision made before there is any data to make it against - and the PRD forbids editing the pre-registered schema to move a threshold.
`convex/http.ts` is the part the US-T01 spike made load-bearing rather than optional: with `convex-swift` ruled out (arm64-only xcframework), `POST /logEvent` -> `204` is the client's **only** entry point, and US-T04's `URLSession` transport reaches it with no Convex SDK.
A third review round made that true of the sink as well as the client: `logEvent` shipped as a public `mutation`, which Convex exposes on the deployment's own `.convex.cloud/api/mutation` endpoint, so the sink had a second entry point that skipped every check in the action - and `.convex.cloud` shares its slug with the `.convex.site` origin a shipped binary carries, so it was reachable by anyone who read that URL. A direct call did insert a row with an empty `installId` and a `clientTs` of `0`. The empty `installId` is exactly what the boundary checks keep out of the column K4 counts unique installs by; the `clientTs` of `0` is not, and the docs no longer say otherwise - it is a finite JSON number, so it lands through the route as well, presence and kind being the whole of that check by design and range belonging to a query written later. It is an `internalMutation` now, reached as `internal.events.logEvent`, and nothing else calls it; `convex/_generated/` needed no regeneration because it derives the public and internal surfaces from the module's own types, and `npm run typecheck` confirms the switch by refusing `api.events.logEvent`. The identical direct call was then re-sent live at `99a11d0` and answered `Could not find public function for 'events:logEvent'` with no row inserted - the transport status being `200` in both the before and after cases, since Convex reports a missing public function in the response payload rather than as an HTTP status, so what proves the fix is that payload alongside the absent row.
Validation is exactly two rejections, because "basic input validation only" is an acceptance criterion: an unknown event name (a `v.union` of the 13 literals, generated from `EVENT_NAMES` - the same strings as `AnalyticsEventName`'s raw values, so the wire contract has one spelling per side) and an oversized property bag (>32 keys or >4096 **UTF-8** bytes serialized, both roughly two orders of magnitude above the widest real bag, `session_completed`'s four small scalars at ~120 bytes). The byte count goes through `TextEncoder` rather than `String.length`, which counts UTF-16 code units and would have undercounted a non-ASCII bag by up to a factor of three against a limit documented in bytes - the validation run gates that with 2100 `é` characters, only 2111 UTF-16 units but 4211 real bytes.
No per-key or per-type check: property vocabularies are expected to move during the PMF test, and a write-path check would turn every such move into a deploy.
The numeric convention is pinned at the wire form rather than patched up at the action - `clientTs` crosses as a plain JSON number, which already *is* the float64 a `v.number()` field stores, so the `int64`-vs-`float64` trap the spike documents never reaches Swift, while `generation_ms` rides untouched inside `props`.
The action adds one thing to that validation list and only one, and review is what put it there: the body's fields must be present and of the right kind - `name` one of the 13, `installId` a non-empty string, `clientTs` an actual JSON number - before they are handed to the mutation.
That is the same "so a malformed client cannot poison the table" criterion applied where untrusted input enters rather than a third rule, because coercing a missing field lands `String(undefined)` - the literal string `"undefined"` - and `Number(undefined)` - `NaN`, which Convex's float64 carries happily - into precisely the two columns the funnel is counted on: `installId` is the cohort key K4 counts unique installs by, `clientTs` is what K1 is timed from, and every such row would collapse into one shared phantom install.
A row that looks valid while being junk is worse than no row, and neither a bare `v.string()` nor a bare `v.number()` catches it downstream.
It constrains presence and kind only - no length cap, no format or UUID-shape check - and a numeric *string* is refused rather than coerced, because `Number("1e3")` and `Number("0x1f")` would land a silently reinterpreted value in the column K1 is timed from; a well-formed client, which sends a bare JSON number, is unaffected either way.
The two statuses are also no longer one: a rejection the sink asked for answers `400` carrying the message, while a deployment, runtime, or database failure answers `500` saying only `internal error`, with the detail left in the deployment log rather than echoed to a caller.
`logEvent` raises its own rejections as `ConvexError` rather than `Error` so the action can tell those two apart by a marker on the throw instead of by sniffing message text.
A fourth round narrowed that test to the one thing it needs: the classifier also carried a fallback for an `Error` merely *named* `ConvexError`, which no code path can produce - the SDK reconstructs a cross-isolate throw either as a genuine `ConvexError`, when it carries `data`, or as a plain `Error` named `Error` - and which would have echoed a raw internal message, stack frame and all, into a `400` body if it ever had fired. Recognising the marker is the whole test now, so only an error's structured `data` is ever echoed and everything else still falls through to the generic `500`.
A fifth round then made that single remaining test robust, because deleting the fallback left it as the only thing separating a caller's `400` from the sink's `500`. It recognised a `ConvexError` by `instanceof`, which the SDK itself pointedly does not use for this: the class stamps `Symbol.for("ConvexError")` on every instance and `registration_impl` detects one with `Symbol.for("ConvexError") in thrown`, because class identity does not survive duplicated module copies - and the package does ship two, `dist/cjs/values/errors.js` and `dist/esm/values/errors.js`, with the syscall layer reconstructing from a relative import while `http.ts` imports the `convex/values` specifier. Under a duplicated resolution `instanceof` would fail and every property-bag rejection would quietly turn into a `500`, manufacturing the outage signal the split exists to protect. The action now tests for the symbol, which every `ConvexError` carries whichever copy built it, and still reads only the error's structured `data` - a strict superset of what `instanceof` matched, so no verified response changed.
The split is entirely for a human: US-T04's client is strictly fire-and-forget and swallows every error, so an outage answered as `400` would be invisible - events would simply stop arriving and nothing would say so, and `4xx`-versus-`5xx` is the only signal anyone watching the PMF test gets.
A second review round found the split classifying by the wrong axis, which mattered because a hostile client could exploit it to manufacture what looked like a sink outage on the one signal that exists.
`ctx.runMutation` serializes its arguments with the SDK's `convexToJson` inside the action's own isolate, and that serializer refuses a field name starting with `$`, carrying a non-ASCII or control character, or over 1024 characters - so such a `props` key never reached `logEvent`, threw a plain `Error` rather than a `ConvexError`, and was reported as our `500`.
The action now runs the same serializer on the assembled arguments first and answers `400` when it refuses.
That is a classification rather than a rejection: the input was already refused either way, and borrowing the SDK's own function instead of restating its rule means the check cannot drift from what Convex enforces, while inspecting field names only - the per-key and per-type checks of `props` the story forbids stay absent.
Layout is standard Convex - the npm project at the repo root, the functions in `convex/` - because Convex bundles every file under the functions directory, so making `convex/` its own npm root swept `node_modules` into the bundle and failed the push with "Could not resolve console/timers/dns"; `convex/_generated/` is committed so `npm run typecheck` works in a fresh clone with no deployment, while `.env.local` (holding `CONVEX_DEPLOYMENT`) stays gitignored.
This was validated live against a fresh dev deployment (`hcho22/reptoday-telemetry`, `courteous-dogfish-560`) rather than on a type-check: a valid `session_completed` POST returned `204` and `npx convex data events` read back one row with `serverTs` stamped server-side and distinct from `clientTs`, and the unknown-name, oversized-bytes, and oversized-keys POSTs each returned `400` with the table then read back holding **only the valid events** - six rejected POSTs across the two deploys, zero rows, the non-insert proven by the table's contents rather than inferred from the errors. Full transcript in `artifacts/reports/US-T03/validation.md`. The iOS build and `RepTodayTests` were run green on an iPhone 16 Simulator too, so "build and tests pass" is observed rather than assumed from an untouched `ios/`.
The required-field checks, the `4xx`/`5xx` split, the JSON-number requirement on `clientTs`, and the serialization classification all landed *after* that run and were re-validated live in a second full pass: 14 rejected POSTs, each carrying the action's own sentence rather than Convex internals, and zero rows added. The transcript is marked in place where the original run's recorded bodies no longer match, and no output in it was rewritten to predict anything unobserved. The `internalMutation` change came later still and is gated by `npm run typecheck` plus its own live pass at `99a11d0`, where the bypass is recorded as a before/after pair of the same request - open at `42c1310`, closed at `99a11d0` - and the junk row the pre-fix probe inserted is recorded as deleted, by a scratch mutation written outside the repository and removed again by redeploying this branch's code, so the repo still holds exactly one mutation. The table read back at `99a11d0` holds only valid rows. Two things no live run covers, and the transcript says so rather than implying otherwise: the fourth review round's deletion of an unreachable branch in the action's error classifier, and the fifth's change of how the remaining branch recognises a `ConvexError`, both landed after that pass and are gated by `npm run typecheck` alone.
As US-T03 shipped, nothing in `convex/` had an automated behavioural test, so the live transcript was a point-in-time record that nothing re-runs and `tsc` cannot catch a regression in any of the boundary behaviour the review rounds established. That was recorded as an unchecked acceptance criterion of US-T04 rather than closed here, because a test runner and its dependency are new scope for a story whose deliverable is one table, one mutation, one route. **US-T04 closed it** (`convex/http.test.ts`, `npm test`); this repo still has no CI, so it runs when someone runs it.
Two gaps were reviewed and consciously left open, both on the reasoning that nothing could reach the endpoint until US-T04. `installId` was an unbounded `v.string()` while `props` was capped, so a hostile client could write a very large row through that one field; the bound was made an acceptance criterion of US-T04, the story that first puts a real identifier on the wire. That gap also blunted the status split beside it, since `convexToJson` validates field names and not structural limits. A second, narrower instance was a matter of ordering rather than of an unstated rule: the `props` caps live inside the mutation, so they run only after `ctx.runMutation` has serialized its arguments, and Convex bounds those arguments far above where the caps sit - a bag past that bound fails during serialization as a plain `Error` and answers `500` rather than the `400` the caps promise, letting a caller manufacture the sink's only outage signal. Both were carried on one US-T04 criterion because one size check ahead of the mutation classifies both, and **US-T04 closed both** - see its own paragraph below, including the two places where probing them live corrected what this paragraph originally predicted. What is *not* closed is the wider residual the pre-pass cannot reach: it is a field-name classifier, so any caller fault that only a write-time Convex rule catches - `props` nested past the value-depth limit, which can be under 100 bytes - still lands on the `500` branch. Left open deliberately, because closing it would mean the per-key and per-type validation of `props` the story forbids. And the route is unauthenticated and unmetered, which anonymous telemetry with no client secret cannot avoid, so its caps are per-row rather than per-caller; that is the route, not the mutation, and guarding it is US-T14.

US-T05 (`Utilities/AppState.swift`) gives the funnel the anonymous identity it is cohorted by, and nothing else: a handful of values on the existing app-state object, no transport, no emission, no `convex/` change, no touch to `Services/Analytics/`.
`installId` is a random UUIDv4 minted on first launch and kept only in `UserDefaults`; `firstLaunchAt` is when this install was first opened, or `nil` when that is unrecoverable; `lastActiveAt` is rewritten by `init`, which is its only writer - `AppState` is constructed once per process, so it moves on a cold launch and not on a resume from the background, and since iOS resumes far more often than it cold-launches, it can lag real usage by days.
It stays a settable `var` so a foreground update could move it, but nothing wires one and no story owns doing so; read it as "last cold launch" until that changes.
`installWeek` is a computed property, not a stored one, and it calls `ConsistencyScore.startOfWeek(_:_:)` - the same helper the Consistency Score's week bucketing uses - rather than re-deriving a week boundary.
The wire encoding is left to US-T07, which is where `install_week` first goes into a property bag and where `AnalyticsValue` having no date case has to be answered one way or another.
Where the id is **not** stored is the substantive part of the story. It is not derived from the IDFA, `identifierForVendor`, the Sign in with Apple identifier, or an email, and it is not in the Keychain - a Keychain item survives a plain uninstall with no restore involved, so a "deleted" install would come back wearing its old identity, which is the failure mode the account-deletion PRD names. `UserDefaults` is the mechanism, not an accident of convenience, but it is worth stating exactly what it buys rather than repeating the absolute the PRD's criterion uses: `Library/Preferences` is included in iCloud and iTunes backups and in device-to-device transfer, so the id dies with the app **absent a backup restore**, it can outlive an uninstall-then-restore, and one id can be live on two devices after a migration. K4's "distinct installs" carries that caveat; the storage is still right, because the gap between it and the Keychain is the whole point.
The clock, the calendar, and the generator are injected as `now: () -> Date`, `calendar: Calendar`, and `newInstallId: () -> String` on `init`, matching how `ConsistencyScoreService` and `OnboardingViewModel` already take their seams; `RepTodayApp`'s `AppState()` is unchanged. They are non-escaping because `init` is the only thing that uses them - `AppState` reads the clock exactly once, at construction, and never again.

Three launch states have to be distinguishable, and the first shape of this story only had two.
The id and its origin were read back as a **pair**, with anything short of both re-minted, which is right for a half-write that lost the *origin*: an id with no origin would otherwise cohort an install against a week it did not install in, silently wrong in the one column the cohort is grouped by.
But it also swept up the case that matters most at launch. An app that is *already installed* when this build ships has no `installId` key either, so it fell into the mint branch and got `firstLaunchAt` stamped with the upgrade date and `isFirstLaunch == true` - every TestFlight tester, dev device and shipped user would have been recorded as a brand-new install in the week they upgraded, inflating the K4 denominator and mis-cohorting the funnel the story exists to measure.
The discriminator is `isOnboarded == true` with no stored `installId`: such a launch mints an id, is **not** a first launch, and - absent an origin already on disk, see the second narrowing below - gets no first-launch date at all. `firstLaunchAt` and `installWeek` are therefore `Date?`.
Modelling the unknown as a real-looking date was the tempting alternative and is the worse one - a date with a caveat attached eventually gets read without the caveat, and US-T07 would emit a confidently wrong `install_week`; a `nil` forces that story to make a deliberate choice.
That optional then collides with the pair rule head-on, because an upgraded install is exactly the shape the pair rule calls corrupt (an id with no origin), and it would have re-minted its id on every single launch.
So the unknown is persisted in its own right, as `AppState.firstLaunchUnknown`, written beside the id: a stored id plus either an origin **or** that marker is a relaunch and is preserved; a stored id with neither is still corrupt and still re-mints.
The rule was then narrowed a second time, because "re-mint both" was always broader than its own justification. That justification only ever covered the case where the *origin* is the missing half; it never justified throwing away an origin that survived, and an onboarded install that reached the mint branch with a valid `firstLaunchAt` on disk had that date deleted and locked to "unknown" by the marker - destroying real cohort data to prevent a miscohort the stored date would not have caused. What the code does now, and what the rule actually is: **re-mint a missing or empty identifier, keep a usable stored origin whenever there is one, and fall back to unknown only when no usable origin exists** - which is also exactly what the `firstLaunchUnknown` marker records, rather than "the pair was incomplete". `isFirstLaunch` follows the same line: it is true only when this launch stamped the origin itself, so a re-mint that inherits an origin already on disk reports a new identity rather than a new install. The corrupt case is unchanged, because it has no stored origin to keep.
Reachability is low today - both keys are written in the same `init` and nothing clears the id alone - but a later "reset telemetry identity" affordance would clear exactly the id, and under the broader rule it would have silently wiped every onboarded user's cohort.
Keeping the origin does leave a state the emission story has to answer for, and it is now written on that story rather than left to be rediscovered: a re-minted id beside a surviving origin reports `isFirstLaunch == false` with a perfectly valid `install_week` available, so `app_install` either keys off the mint and double-counts one physical install, or keys off the origin stamp as it does today and leaves an id active in K4's numerator but absent from its denominator.
Both are lossy, neither is decided here, and US-T07 records which distortion it accepted; US-T06 carries the matching constraint not to ship a reset control before that answer exists, since that control is what makes the state reachable at all - and honoured it: its opt-out gates emission only, with two tests asserting the stored `installId` survives an opt-out untouched.
The residual, stated rather than glossed: a user who installed an earlier build but never finished onboarding still reads as a new install, because `isOnboarded` is the discriminator. Widening it to chase that case was declined.
`isFirstLaunch` is a stored `let` rather than something a caller recomputes, because construction is the only moment the fact exists - once `init` returns, a first launch and a relaunch are indistinguishable from the persisted values, and US-T07's `app_install` needs exactly that signal at exactly that point.

Two smaller corrections landed in the same pass.
`init` overwrites the persisted `lastActiveAt` with this launch's open time, so the previous launch's value was destroyed at construction and unrecoverable for the life of the process - the app paid to write it every launch and kept nothing readable from it. It is now snapshotted as `previousActiveAt` (`nil` when there was no prior launch). Nothing reads it today, and to be clear about what it is not: the day-7 and day-30 return windows are measured from `firstLaunchAt` per FR-8, not from this.
And `installWeek` resolved through `Calendar.current` in production, whose `firstWeekday` and `timeZone` are device settings - a US device produces Sunday-start weeks and a German device Monday-start ones, so one real week would scatter across buckets the moment `install_week` is grouped across users server-side. It is now bucketed in `AppState.cohortCalendar`: Gregorian, `firstWeekday = 1`, `America/Los_Angeles`. Pacific rather than UTC deliberately, because a UTC boundary lands mid-Saturday afternoon Pacific and would push a Saturday-evening install into the following week for exactly the audience this PMF test measures. US conventions are a "for now" choice, so the whole decision is one constant and the `calendar:` parameter stays a test seam that now defaults to it rather than to `Calendar.current` - two competing sources would defeat the point. `ConsistencyScore` itself is untouched and the on-device score stays on `Calendar.current`: a user's training week is their local week, and that divergence is intended rather than an inconsistency to unify.
What the pin also does is split two values that used to agree, and US-T11 now carries that as a criterion.
`week_active` reuses the `ProgressAnalytics` rollup, which is `Calendar.current`, and it carries no properties, so the server can only bucket it by client timestamp - meaning a device on a Monday-start locale or a non-Pacific zone emits its once-per-week marker on a boundary the server does not share and can land twice in one server week and zero times in another, leaving K4's numerator on device-local weeks and its denominator on pinned ones.
The consistent resolution is the same split made here, but it is US-T11's to make.

`AppStateTests` covers the PRD's whole validation test against a pinned clock and a per-test `UserDefaults` suite, never `.standard`: first launch mints and persists everything; a relaunch preserves `installId` and `firstLaunchAt` and moves only `lastActiveAt`; the reinstall leg is a wiped suite - which is exactly what an uninstall does to `UserDefaults` - and yields a new id and a new `firstLaunchAt`, so a surviving id would fail as the Keychain-persistence symptom the story's failure indicator names; a half-written pair with no origin re-mints whole, while a missing id sitting beside a stored origin re-mints only the id, keeps that origin, and writes no unknown marker; `installWeek` equals `ConsistencyScore.startOfWeek(firstLaunchAt, calendar)` and sits strictly behind a mid-week launch, so no precise install time is recoverable from it; and two installs on one device differ, which no device-derived value could.
The three behaviours above are covered too: an onboarded install with no id mints one, reports `isFirstLaunch == false`, leaves `firstLaunchAt` and `installWeek` `nil`, and keeps the same id with the same `nil` on a relaunch; `previousActiveAt` is `nil` on a first launch and equals the prior launch's stamp on a relaunch; and a Saturday-evening Pacific install - already Sunday in UTC - buckets to the Sunday its Pacific week began through the default calendar, differing from what a device-shaped calendar would produce, with `cohortCalendar`'s identifier, `firstWeekday` and time zone asserted directly so a drift away from the pin fails rather than passes quietly.
Verified by `xcodebuild ... -scheme RepToday test` on an iPhone 16 Simulator. This repo has no CI, so that local run is the whole gate.
The PRD's Validation Test was also run outside XCTest, against a real Debug install on a Simulator with every value read out of the app container's own preferences plist - install, relaunch, delete and reinstall - plus three probes the story does not ask for: a binary-safe search of the whole simulated device after the uninstall, which found no trace of the deleted install's id and, as a control, did find the live one in exactly one file; a background-and-resume that left `lastActiveAt` untouched, which is the cold-launch-only behaviour above observed rather than argued; and the upgrade path driven on a real install whose preferences were rewritten into the pre-US-T05 shape, which minted an id, wrote the unknown marker with no origin, and kept that id across the next cold launch. Transcript in `artifacts/reports/US-T05/validation.md`, with the one committed screenshot beside it.
Two limits are recorded there rather than left to be inferred: it is a point-in-time record that nothing re-runs, and the reinstall leg proves nothing survived on that simulated device at that moment - it exercises no restore path, so the backup caveat above stands beside it rather than against it.

US-T04 (`Services/Analytics/LiveAnalyticsService.swift`, `Services/Analytics/AnalyticsWireBody.swift`, `App/RepTodayApp.swift`, `DI/ServiceContainer.swift`, `RepToday/Info.plist`, `project.yml`, `convex/http.ts`, `convex/http.test.ts`, `vitest.config.ts`) joins the two ends the earlier stories built: the seam (US-T02) now has a transport, and it carries the identity (US-T05) to the sink (US-T03).
It still adds **no emission call site**. Nothing in the app calls `record(_:)` - that is US-T07 through US-T12 - so a build today carries a working pipeline that nothing triggers, exactly as US-T02 shipped a seam nobody called and US-T05 an identity nobody read.
`LiveAnalyticsService.record(_:)` reads the opt-out gate, hands the event to a detached background task, and returns; the encode, the request, and the response all happen past that point and every failure - offline, slow, non-2xx, malformed, unencodable - is swallowed. The session is ephemeral, does not wait for connectivity, and times out in ten seconds, so an offline send fails fast and is dropped rather than queueing work that outlives the reason for sending it.
The wire body is deliberately a **second** encoding of the event rather than the model's own. `AnalyticsValue`'s `Codable` form is tagged (`{"type":"int","value":38}`) so `Int` and `Double` round-trip losslessly in process - a US-T02 requirement that stays - and the sink wants the opposite: `{"generation_ms": 38}`, plain scalars, under different top-level key names (`{name, installId, clientTs, props}`). Reusing `JSONEncoder().encode(event)` would have sent a body the sink stores wrong, so `AnalyticsWireBody` keeps the two shapes apart, and `clientTs` goes out as a bare JSON number, which already *is* the float64 a `v.number()` column stores - the `int64`/`float64` trap the US-T01 spike documented, avoided by never letting anything re-tag it.
The identity is **passed down, never re-read**: `RepTodayApp.init()` builds `AppState` first and hands its `installId` to `ServiceContainer.live(context:installId:)`, so `AppState` stays the only thing that mints an id or decides which of the three launch states an install is in. A service that read `UserDefaults` for itself would be a second resolution path that could disagree with the first about whether an install is new, which is precisely the distinction US-T05 spent three review rounds getting right. That is why `AppState` moved out of a property default and into `init` - the container is built from the id it settled on.
The deployment origin is `Info.plist` configuration (`RepTodayAnalyticsEndpoint`), not a Swift literal, and a build whose value is missing or unusable is **inert, never fatal**: `LiveAnalyticsService.configured(...)` returns `nil` - the value must parse, be `https`, and have a host, because `URL(string:)` accepts almost any string as a relative URL - and the container falls back to `NoOpAnalyticsService`, which already means "emit nothing, keep nothing". A telemetry misconfiguration that trapped or logged noisily on a path the core loop shares would be a worse outcome than no telemetry, and expressing the inert state as a different wired type also makes "is this build configured to emit?" answerable by looking.
That value is **split per build configuration** rather than baked into the source: `Info.plist` carries `$(REPTODAY_ANALYTICS_ENDPOINT)`, set in `project.yml` to the **dev** deployment `courteous-dogfish-560` under Debug and to nothing at all under Release, because no production deployment has been chosen (still an open question in the PRD). A Release build therefore takes exactly the inert path above - no second notion of "unconfigured" was invented for it - so until someone deliberately configures a deployment, the worst case for a TestFlight or App Store build is silently no data rather than silently the wrong destination, and choosing that deployment is a precondition for shipping any build that emits (US-T07 onward). The split is asserted against the running app bundle in `LiveAnalyticsServiceTests`, each half compiled only into the configuration that can observe it, rather than claimed in a comment - with one limit stated rather than glossed: only the Debug half runs, because this project sets `ENABLE_TESTABILITY` on Debug alone, so `@testable import RepToday` does not resolve under `-configuration Release` and no Release test run exists (de-optimising the shipping binary to create one would be a worse trade than the gap). The Release half is verified at the artifact instead: `plutil -extract RepTodayAnalyticsEndpoint` reads the empty string out of the built `Release-iphonesimulator/RepToday.app/Info.plist` against the Debug bundle's dev origin, and the empty string's rejection is already unit-covered.
`ServiceContainer.live(context:installId:analyticsService:)` also takes the sink, defaulting to `LiveAnalyticsService.configured(installId:) ?? NoOpAnalyticsService()`, so production behaviour and `RepTodayApp` are unchanged. The seam exists because building that container is the only way to get the CoreData-backed services composed, so a test with nothing to do with telemetry (`CoreDataServicesTests`) had been staying off the wire only because no emission call site exists yet; it now passes an inert sink and is structurally unable to reach the network once US-T07 through US-T12 land. FR-13 is held by the code rather than by that reading - **in this process**. A container parameter can only bind a container this process builds, and `RepTodayUITests` launches the real app out of process, where the app resolves its own default sink and the only gate left is the `isEnabled` closure below. Closing that half needs a persisted flag a launch argument can flip, which is US-T06's `analyticsEnabled` criterion word for word, so it was left there rather than pre-empted here. **US-T06 has since closed it**, and nothing leaked in the interim because no emission call site existed.
The opt-out was a stub-then-connect seam the PRD explicitly sanctions: an injected `isEnabled` closure defaulting to enabled, read **per emission** rather than captured at construction, so US-T06 could point it at the persisted `analyticsEnabled` flag without reshaping the service - which is exactly what US-T06 then did (below). Inventing the flag, the toggle, or the disclosure here would have left US-T06's review arguing with this story's code.
On the sink side the story closes the two size gaps US-T03 shipped knowingly: `installId` is bounded at 64 UTF-8 bytes (length only - `"not-a-uuid"` is still accepted, because policing the identifier's shape would forbid a later story from changing it), and the request body at 64 KiB, measured on the raw bytes before anything is parsed. The body cap sits an order of magnitude above the 4096-byte `props` cap, so across the range a real client could send it does not pre-empt the mutation's more specific rejection - it is not an absolute ordering, and a bag past 64 KiB is answered by the body cap instead, still a caller's `400` - and far below any argument bound, so serialization can no longer be reached by size.
Probing those two live, before and after, corrected the documentation twice, and both corrections are recorded rather than smoothed over. A 300 KB `installId` did **not** fail at the insert as the docs predicted - it was accepted and inserted a 300 KB row, since Convex's document limit is around 1 MB, so the gap was a real row in the evidence table rather than a misclassified status; the false-outage `500` only began at ~1.5 MB. And the argument-bound `500` reproduced at ~24 MB, **not** the ~8 MiB the PRD and README cited: 3 MB and 16 MB bags both came back as the mutation's own `400`. The concern was real, the number was not.
`convex/http.test.ts` is the boundary's first re-runnable gate (`npm test`, `vitest` + `convex-test` in process, no deployment and no network): 51 assertions over the 13-name check, `installId`/`clientTs` presence and kind, the new size caps at their exact boundaries and in UTF-8 rather than UTF-16, both `props` caps, a `props` of `null` deliberately treated as absent while a non-null non-object is refused, the `convexToJson` field-name classification answering `400` rather than `500`, the `4xx`/`5xx` split including that a `5xx` echoes no internal detail, and `logEvent` still being internal. Every rejection asserts the **table is empty afterwards**, because a `400` that inserted anyway would be worse than no check. Adding that file under `convex/tsconfig.json` coupled the config the Convex CLI typechecks on every deploy to test-only tooling, so the two are split: the deploy config excludes `**/*.test.ts` and names no `types` array (naming one had also switched off automatic `@types` inclusion for the deployed functions), `convex/tsconfig.test.json` extends it with the test files and the `vite/client` types `import.meta.glob` needs, and `npm run typecheck` runs both so the suite stays typechecked. Two of those need care: the `500` branch cannot be provoked by input, so it is reached by handing `convexTest` a module map with `logEvent` swapped for one that throws a plain `Error`; and `logEvent` being internal is asserted on the registration object's own `isInternal` flag rather than on `api.events.logEvent`, which can say nothing, since the generated `api` is `anyApi` - a proxy that answers every property access whether the function exists or not.
On the Swift side `LiveAnalyticsServiceTests` drives the real service through a `URLProtocol` stub that intercepts in process, so no test performs a network call (FR-13). It asserts the URL, method, content type, and the exact body - flattened scalars, a bare-number `clientTs` checked against the raw bytes rather than a parsed value, since a re-tagging encoder would still produce valid JSON - plus that `record(_:)` returns while the stub holds the request open, that failures and non-2xx answers are swallowed and do not poison the service, that a closed gate produces no request at all, and that unusable configuration resolves to `nil`. The stub reads `httpBodyStream`, not `httpBody`: `URLSession` moves the body into the stream before a `URLProtocol` sees it, and reading the wrong one would have made every body assertion silently vacuous.
No new Swift Package: `ios/RepToday/project.yml` still lists Lottie alone, and `convex-swift` is absent. `convex-test`, `vitest`, and `@edge-runtime/vm` are devDependencies of the **root npm project**, which is `convex/`'s toolchain and reaches no iOS target.
Validated live against the dev deployment, with the PRD's Validation Test deliberately re-framed rather than run as written - it asks for events from onboarding and a session, and this story adds no emission site to produce them. A temporary, non-shipping probe stood in for the sites that do not exist yet, driving the real touch path (onboarding, Ready Screen, a regeneration, Start, a session to completion) in the real installed app while emitting from the main actor; it was removed before the commit. Rows landed carrying the app's own `installId` read out of its preferences plist, both timestamps, and `props` holding plain scalars. Against a black-holed endpoint - the Simulator has no airplane mode, so the substitute hangs every send to its timeout, which is harsher than an instant offline failure for the question being asked - the six timed phases of the loop were within ~1.5% of the online run, `record(_:)` returned in a median of 0.072 ms, no rows were written, nothing spun or errored, and emission resumed on the next online run. Full transcript, including what each leg does *not* prove, in `artifacts/reports/US-T04/validation.md`.

US-T06 (`Utilities/AppState.swift`, `Utilities/LegalLinks.swift`, `Views/Settings/SettingsView.swift`, `Views/RootView.swift`, `Views/Onboarding/OnboardingView.swift`, `Views/Paywall/PaywallView.swift`, `DI/ServiceContainer.swift`, `App/RepTodayApp.swift`, `Services/Analytics/TelemetryUITestHarness.swift`) gives the user the answer the pipeline has been asking for and connects the gate US-T04 stubbed.
It still adds **no emission call site**: nothing in the app calls `record(_:)`, so a build today carries a complete, consented, gated pipeline that never fires. The 13 sites are US-T07 through US-T12.

The flag is `AppState.analyticsEnabled`, keyed `AppState.analyticsEnabled` beside the US-T05 keys and written through `didSet` like `isOnboarded`.
The **read** is the load-bearing part, and it is deliberately not `bool(forKey:)`: that answers `false` for a key that was never written, so the obvious spelling would have shipped every fresh install opted *out* while the code read as though it defaulted to on.
`AppState.isAnalyticsEnabled(in:)` therefore checks `object(forKey:)` for presence first and answers the shipped default, coercing only a value that is actually there - which is also what makes the launch argument work, since the argument domain stores `NO` as a **string** an `as? Bool` cast would miss entirely.
The fresh-install default is an *absence* rather than a stored `true`, confirmed on a real install whose plist carried `installId` and `firstLaunchAt` and no `analyticsEnabled` at all.
`analyticsEnabledKey` is the one non-private key on `AppState`, because three things must name it: the object, the gate, and the XCUITest launch argument.

The gate the transport holds is `AppState.analyticsGate`, a closure re-read on every emission rather than captured at construction - US-T04 built it that way for exactly this - so turning the toggle off takes effect on the next event instead of the next launch.
It reads `UserDefaults.standard` rather than capturing an `AppState`, because `ServiceContainer.live(...)` is built from an install id and not from the state object, and a captured object would outlive whatever built it.

The Settings surface is a real one, per the captain's answer to the story's Open Question: `SettingsView` with a sectioned `List`, pushed from a plain navigation row on the Profile tab, carrying a `Privacy` section as its only content today.
Shaped so a broader Profile/Settings story extends it with another `Section` rather than replaces it; the Profile placeholder around it is untouched.
A prominent row rather than a toolbar gear, because a control that has to be *found* to be worth anything should not sit behind an icon.
The onboarding disclosure sits on the **welcome** step, not the last one, because US-T07's `app_install` fires at app entry - a disclosure at the end of the flow would arrive after the thing it discloses.
Both link to `LegalLinks.privacyPolicy`, lifted out of `PaywallView`'s private static so the App Store Review 3.1.2 link and the consent surfaces cannot drift onto two placeholders; it is still an obvious placeholder and a test asserts it still says so.

The hard part was not any of that. It was making the out-of-process proof mean something.
`RepTodayUITests` launches the real app, so the test process never builds the container and US-T04's `analyticsService` parameter cannot reach it - the `isEnabled` closure is the only gate there, which is why this story rather than US-T04 owns that half of FR-13.
But **no emission call site exists**, so "zero network calls with telemetry off" is trivially true for reasons that have nothing to do with the flag: the assertion would pass with the flag deleted, and would keep passing when US-T07 starts POSTing on every launch.
Two things were missing and neither could come from the test process: something that *attempts* an emission, and somewhere another process can *count* attempts from.
Both come from `TelemetryUITestHarness`, a Debug-only file whose every entry point is additionally inert without `-RepTodayTelemetryProbe YES`.
Under it the app clears its persisted flag (so a run never inherits the last one's toggling), swaps the transport's `URLSession` for one whose only protocol is a counting `URLProtocol`, emits one `app_install` from `RepTodayApp.init()` - the exact name and place US-T07's first real site will use - and renders the count in a HUD the suite reads.
A Release build compiles none of it, and an ordinary Debug run without the argument is byte-for-byte the app it was.

The suite has a **positive control**, without which the zero is the vacuous case wearing a disguise: the identical launch with the flag at its default reports one attempt.
Its honesty was then checked by breaking the thing it tests - `isEnabled: AppState.analyticsGate` replaced by `isEnabled: { true }` makes two of the four fail with the messages they were written to print, and restoring it makes them pass.
What it proves is bounded and stated rather than implied: the count is taken at the `URLProtocol` boundary - the same boundary `LiveAnalyticsServiceTests` already treats as authoritative for "a POST happened" - so an attempt means the transport built and dispatched a request, **not** that bytes reached Convex.
That is deliberate; a run that let real bytes out would break FR-13.
The bytes-actually-reach-Convex half is the live validation instead, and it does not re-run.

`OnboardingImperialUITests` now passes `-AppState.analyticsEnabled NO` as well, so the pre-existing suite is off the wire *before* US-T07 makes that matter rather than after.
Its private Health-prompt helper moved to a shared `HealthAccessPrompt.swift`, since two suites now have to get past the same sheet for unrelated reasons.

Validated live against the dev deployment `courteous-dogfish-560`, with the Validation Test re-framed the way US-T04 re-framed its own: steps 2 and 4 ask for events produced by starting a session, and this story adds no site to produce them.
A temporary one-line edit let the committed probe reach the real deployment instead of its interceptor, and was reverted before the commit along with two throwaway XCUITest drivers.
Telemetry on put one row in Convex carrying the installed app's own `installId`; `-AppState.analyticsEnabled NO` put none; and four attempts in **one launch** with the toggle driven through the real Settings screen - on, on, off, on - produced exactly three rows, the gap being the attempt made while it was off.
All four probe rows were deleted afterwards by a scratch mutation deployed, run, and removed.
So the live legs prove the opt-out decides whether a real POST reaches a real deployment; they do not prove the shipped build emits anything, because it does not.
Transcript, renders, and what each leg does not prove: `artifacts/reports/US-T06/validation.md`.

No "reset telemetry identity" control was added, and that is a constraint rather than an omission: clearing `installId` alone is exactly the re-minted-identity state US-T07 has to decide about, so the opt-out gates emission and touches nothing else - asserted in two places rather than intended.
Verified by `xcodebuild ... -scheme RepToday test` (877 tests, 0 failures, from 863) and `-scheme RepTodayUITests test` (9 tests, 0 failures, from 5) on an iPhone 16 Simulator. This repo has no CI, so those local runs are the whole gate.

### A hold is not a rest, and modelling it as one cost three review rounds

The Hold Timer was written by mirroring the rest timer: same absolute deadline, same pause-on-background, same restore-from-snapshot, same on-appear resume.
That was wrong in one specific way, and the way it was wrong kept producing the same defect through different doors.

A rest genuinely continues while the app is away - the user *is* resting - so restoring one mid-countdown is correct, and it is long-standing US-K04 behaviour.
A hold does not: the user stopped holding when they left the screen.
Because the hold restored the same way, every path that brought a snapshot back brought back a countdown already at or past zero, and the player's ticker then did exactly what it is built to do - fire the cue and bank a `CompletedSet` - for work nobody performed.
Three guards were written against three spellings of that state: a deadline already in the past, a frozen remainder of zero, and a frozen remainder that `onAppear` obligingly un-froze eighteen seconds after the screen appeared.
Each was a real fix; none was *the* fix, because the shape of the persisted state was never the problem.

The rule now is that **a hold leg never survives the player being torn down**.
Nothing about the countdown is persisted - only the side the user still owes - so a resumed session always comes back idle offering "Start hold" and the leg restarts on a deliberate tap.
There is no door left to guard because there is nothing to restore.
`ActiveSessionState.Hold` is a single field, and both a pre-US-O03 snapshot and one written by the earlier shape decode to the side they recorded, the running leg such a snapshot carries being exactly the thing that must not come back.
In-session backgrounding still freezes and resumes the leg through `scenePhase`, which is the interruption the user is actually present for; only the teardown boundary drops it.

The second half of the same lesson is why the rest timer had a milder version of the same bug - an expired rest firing its cue on the first tick after a resume - that no fix to the hold had ever touched.
`holdRemaining`/`pauseHold`/`resumeHold`/`endHold` were line-for-line copies of their rest equivalents, so a fix landed on whichever copy it was written against and survived in the other.
Both timers now run on one `Countdown` value type: absolute deadline, `pause`/`resume`/`extend`, and a `hasElapsed` that is false while paused, which is what makes the completion check safe to call on every tick.
Roughly eighty mirrored lines went with it.
What the two genuinely do differently stays in the view model where it is visible - a hold has sides and banks a set at zero, a rest has extend/skip and only cues.
The view half of this duplication had already been collapsed into one `CountdownRing`; collapsing the state half is what made the rest fix a consequence of the hold fix rather than a second thing to remember.
